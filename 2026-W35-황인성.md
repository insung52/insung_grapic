# 작업 내역

> 기간: 2026-08-21 ~ 2026-08-27
> 담당: 황인성

---

## 1. 레벨 디자인 / 신규 레벨 (`New_kadex_0811`)

### 목적
새 레벨 `New_kadex_0811`(PCG 숲 기반, 예전 `kadex_demo_0716`와 지형 구성 자체가 다름)에
UGV/적군 자율주행을 위한 내비메시 인프라를 처음부터 구축하고, 심각한 성능 폭락을 해결하며,
프로토콜(상위체계-통제기-에뮬레이터 구조)을 고려한 3단계 전투 시나리오를 설계·구현한다.

### 결과

**내비메시 3층 구조**: 예전 레벨은 실도로 메시가 있었지만 새 레벨은 "PCG 나무가 안 심긴
영역"이 곧 주행 가능 구역이라 접근법이 통째로 다름. 세 겹으로 분리:
1. **나무 콜리전 프록시**(원기둥 ISM) — PCG가 뿌린 나무 위치에 보이지 않는 콜리전을 복제해
   UGV·보병을 물리적으로 막고 내비메시를 침식.
2/3. **`RoadCenterline`/`RoadCenterline2` + 엔진 `SplineNavModifierComponent`** — 디자이너가
   그린 스플라인에 UGV/적군 각각의 경로 가중치를 자동 추종시킴(예전 레벨은 Landscape
   Spline이라 이 엔진 기능을 못 썼는데, 이번엔 평범한 SplineComponent라 가능해짐).

적군 경로에 `NavArea_Road`를 그대로 쓰면 안 되는 것도 확인 — 적군이 쓰는
`UNavQueryFilter_Infantry`가 이 area 비용을 무효화하도록 이미 설계돼 있어서(2026-07-29
사고 방지용), `NavArea_EnemyPath`(중립 비용) + `UNavQueryFilter_Enemy`(이 area만 할인)로
분리해 "area는 중립, 할인은 필터에서만" 원칙을 재사용했다.

> **트러블슈팅 - MCP 인스턴스 대량 쓰기로 에디터를 두 번 얼려먹음**
>
> 나무 콜리전 프록시 인스턴스 2,218개를 `set_properties`로 한 번에 쓰다 329초 무응답 후
> 에디터가 완전히 멎어 강제 종료. 근본 원인은 이 API의 배열 갱신이 **차분(diff) 방식**이라
> "빈 배열에 수천 개를 넣는 것"이 원소를 하나씩 추가하는 것과 같아지기 때문(에러 메시지
> `ArrayRemove: elements changed alongside the size change` 로 확인). 컴포넌트당 150개
> 이하로 쪼개고 호출당 300개 안팎을 상한으로 잡아 우회. 이 함정은 나중에 나무 프록시를
> 에디터 네이티브 툴로 완전히 대체하는 계기가 됐다(2절 참고).

**PIE 성능 폭락(2.3fps) 조사·해결**: 원인은 나무 인스턴스 58,400개(자작나무 25,780개 포함)의
`WorldPositionOffsetDisableDistance=0`(거리 무관 전 인스턴스 바람 흔들림 WPO 연산)과 자작나무
LOD 전환 거리 문제 두 겹. RTSP/지형 텍스처/그래스/PCG 재생성 비용/그림자 등은 전부 A/B 실측으로
배제. `WorldPositionOffsetDisableDistance=3000`(30m) + 자작나무에만
`InstanceLODDistanceScale=0.5` 적용으로 **2.3fps → 31fps(13.5배)**. **나나이트는 무관함을
전수 조사로 확인** — 이 레벨에서 나나이트가 켜진 유일한 나무(소나무)를 통째로 지워도 2~3fps
밖에 안 올라, "나나이트 on/off가 이 레벨 성능에 2~3fps 차이밖에 안 만든다"는 디자인팀 전달용
결론을 정리했다. `Titan_Truck`(98만 tris) LOD 생성도 시도했으나 트럭이 UGV 바로 옆이라
LOD가 개입할 여지가 없어 효과 없이 revert, `forcedLodModel` 강제는 ISM에서 0.2fps로 폭락하는
것을 확인하고 사용 금지로 기록.

**3단계 전투 시나리오 구현**: `1차 전투지(UGV 단독) → 도주 → 2차(UGV+아군) → 도주 → 3차
(이동형지휘소 RCWS)` 구조를 요구사항 정리(`scenario.md`)부터 DataTable(`DT_ScenarioSteps_
ThreeStage`, 17행) 구현까지 완료. 신규 트리거 5종(`EnemyCasualtyCountAtLeast`,
`LeaderDistanceFromEnemyAtLeast`, `UGVFiredNearEnemy`, `CommandPostFiredNearEnemy`,
`AllyFireStarted`)과 이펙트 다수를 추가, "언제/무엇을(DataTable)" · "어디로/무엇을
(`ScenarioConfig` 액터+마커)" · "어떻게 보이는가(컴포넌트 프로퍼티)" 세 축으로 저작 구조를
분리해 코드 수정 없이 새 레벨에 이식 가능하게 만들었다. `kadex_test`에서 스텝 발동 순서 자체는
검증 완료(2026-08-23) — 전술적 배치(거리감/시야)는 지오메트리가 안 맞아 새 레벨에서 재작업
필요.

**흐름 설계 규칙(실사용에서 발견)**: 적군 행동 스텝(교전 돌입/도주)을 UAV/UGV 진행 스텝의
`PrerequisiteStepId` 체인에 걸어두면, 테스트로 그 체인 중 하나만 꺼도 적군이 영원히 반응 안 함
— 적군 행동 행은 Prereq를 비우거나 **다른 적군 행동 행에만** 걸어 순서를 잡아야 한다는 규칙을
정리해 `scenario_authoring_guide.md`에 남겼다.

**변경/신규 파일**: `Source/titan_example/Soldiers/NavArea_EnemyPath.h/.cpp`,
`NavQueryFilter_Enemy.h/.cpp`, `Vehicles/UGVAIController.h/.cpp`(스플라인 도로 소스,
목적지 내비메시 투영), `UI/ScenarioStepTypes.h`, `UI/ScenarioStateSubsystem.*`,
`UI/ScenarioConfig.h`, 신규 DataTable `DT_ScenarioSteps_ThreeStage`,
`Content/New_kadex_0811.umap`, `PCG_SplineForest_plant/tree2.uasset`.

---

## 2. UGV 자율주행 (`path/`) — 이번 주 최대 비중

### 목적
`New_kadex_0811`의 나무 장애물 구간에서 UGV가 부딪히는 문제를 근본 원인까지 추적해 해결하고,
공중에 뜬 바퀴가 구동계 전체를 잠그는 오래된 버그(README에 남아있던 이슈)를 해결하며, 궤도
비주얼을 물리 기반으로 이관한다.

### 결과

**커브 선행 감속(제동 곡선)**: 기존 조향 로직(`AngleError` 기반 스로틀 컷)은 사후 반응이라
커브 진입 *전에* 줄이지 못했음. 1차 구현("프리뷰 거리 안에 커브가 있냐/없냐" 이진 판정)은
사용자 지적대로 "정속 달리다 갑자기 풀 브레이크"가 되는 계단식 문제가 있었음 — **거리를
판정 조건이 아니라 출력에 직접 넣는 제동 곡선**(`v² = v₀² + 2ad` 역산, 열차 제동 곡선과
동일 원리)으로 재작성해 목표 속도가 연속적으로 내려오게 함. `CornerScanDistance` 안의 모든
커브에 슬라이딩 윈도우로 꺾임각을 묶어 계산 후 최솟값 채택. 사용자 확인: "아주 깔끔하게
주행된다"(2026-08-22).

**장애물 회피 — 4겹 원인, 전부 규명·수정**:
| 증상 | 진짜 원인 | 조치 |
|---|---|---|
| 나무에 계속 부딪힘 | 조향 PI 게인이 낮아 요레이트가 목표의 57~83%만 나옴(`steer`가 0.45에서 멈춤, 즉 제어기가 덜 명령) | `YawRateKp` 0.04→0.08, `YawRateKi` 0.02→0.05, `LookaheadDistance` 800→500 |
| 커브 감속 부족 | 각도→속도 매핑이 물리적이지 않음(반경 9.5m 이하에서만 최저속) | 곡률 반경 역산 `v=√(a_lat·R)`으로 교체, 튜닝값 3개→1개(`CornerMaxLateralAccelMps2`) |
| 엉뚱한 데서 과감속 | 차량 뒤쪽 커브까지 세면서 제동거리를 0으로 잡음("지금 당장 그 속도여야 한다") | 거리 ≥0인 정점만 계산 포함 |
| 경로가 나무를 겨냥 | 도로 중앙 당김이 A*의 안전 여유(에이전트 반경만큼 침식된 내비메시)를 깨뜨림 | `bEnableRoadCentering` 기본값 false |

부수적으로 잡은 것: 목적지 Z를 마커 큐브 중심(꼭대기 근처)으로 잘못 주면 `DefaultQueryExtent`
(Tank는 수직 2.5m뿐)투영이 조용히 실패 → `MoveToDestination`이 `ProjectPointToNavigation`으로
직접 투영하도록 수정. **부분 경로(무증상)** — Recast가 목적지에 못 닿아도 갈 수 있는 데까지의
경로를 정상(`IsValid()=true`)으로 돌려줘서 UGV가 중간에 그냥 멈추는데 로그엔 아무 이상이
없었음, 원인은 `DefaultMaxSearchNodes`(A* 노드 예산) 기본값 2048 소진 — 65536으로 상향 +
`IsPartial()` 경고 로그 추가. `AgentRadius`는 레벨 액터 Details에서 고쳐도 에디터 재시작 시
프로젝트 세팅으로 되돌아가는 것을 확인, `DefaultEngine.ini`에서 직접 수정(200→300).

**나무 콜리전 프록시 자동 생성 툴(`ATreeCollisionProxyBuilder`) 신규**: MCP 스크립트 방식이
에디터를 두 번 얼려먹은 뒤, 언리얼 네이티브 에디터 툴(`AddInstances()`로 배열을 한 번에
넣어 수천 개도 밀리초 단위)로 대체. 레벨에 액터 하나 배치 후 Details 버튼(`Preview Counts`/
`Rebuild Proxies`/`Clear Proxies`)으로 조작, 경로(스플라인) 여러 개와 나무 종별 실측 줄기
치수를 등록해두면 스케일까지 반영해 자동 생성. 경로 4개 기준 5,643개를 즉시 생성.

**UGV 궤도 잠금(Track Lock) — README에 오래 남아있던 버그 근본 해결**: 원인은 3단 연쇄 —
① RPM 소스가 배열 마지막 바퀴(`R_Wheel_08`) 하나뿐(덮어쓰기 구조), ② 임계 초과 시 16개 전부
토크 0, ③ 공중 바퀴 각속도 적분에 감쇠가 없어 한 번 걸리면 안 풀림. 해결은 `ApplyWheelFriction
Forces`를 override해 궤도 단위로 ① 구동토크를 하중 비례로 재배분, ② 궤도 각속도를 접지 바퀴의
**하중 가중 평균**으로 통일(전부 공중이면 관성 적분+감쇠), Level 1(감속 방향만 변화율 제한)로
착지 팝을 방지. 콘솔 변수 4종으로 런타임 A/B·킬스위치 구성.

구현 중 **가장 오래 걸린 것**: 제자리선회가 0.5~1초 만에 멈추는 버그 — 엔진브레이크, ABP 캐스트
순으로 두 번 헛짚은 뒤 진짜 원인 발견: `UChaosVehicleMovementComponent::ProcessSleeping`이
조향 입력만 유독 "값"이 아니라 "변화량"을 보고, 잠들 조건은 선속도만 봐서(제자리선회는 각속도만
있음) 15프레임 뒤 차량이 잠들어 시뮬 자체가 스킵되고 있었음. `ProcessSleeping` override로
조향 유지 중 슬립 무효화(`p.UGV.KeepAwakeWhileSteering`)로 해결.

스킨스티어(진짜 좌우 궤도 차동 토크로 회전)도 함께 구현 — 기존 A/D는 차체에 직접 요 토크를
거는 방식이라 접지와 무관했고, 차량에 세팅된 `SteeringCurve`가 한 번도 안 쓰이고 있었음을
확인. `TorqueControl` 비활성화 후 궤도 단위 차동 토크로 대체.

**궤도 비주얼 C++ 이관 + 슬랙(처짐) 물리**: 기존 BP는 차체 이동 거리를 적분해 궤도 거리를
표현해서 제자리선회·공중 헛돌기를 표현 못 했음 — `GetTrackLinearSpeed()`(신규 물리 접근자)로
교체. 서스펜션 반영 스플라인 상수가 stale하던 것(휠 프로퍼티가 바뀌었는데 하드코딩 유지)도
발견, 스플라인 구축/갱신을 통째로 C++로 이관하며 로드휠 위치에서 절차적으로 생성하도록 정리.
사인파 새그는 **길이 고정 닫힌 루프의 여유 길이(슬랙) 보존**을 표현 못 해 물리 기반 슬랙
재분배 모델(가속/속도/슬립 3항으로 앞뒤 슬랙 쏠림 계산)로 교체.

> **트러블슈팅 - `Wheel.Setup()` 호출 시 LNK2019**
>
> `TVehicleSystem`이 API 매크로 없는 순수 템플릿인데 이를 상속하는 `FSimpleWheelSim`이
> export되면서 MSVC가 베이스 템플릿 멤버까지 dllimport로 취급, 그 인스턴스화가 DLL에
> export 안 돼 있어 링크 실패. 데이터 멤버라 링키지가 없는 `Wheel.SetupPtr`을 직접 사용해
> 우회(`VehicleSystemTemplate.h` 주석이 이 사용법을 공식 안내).

**빌드/검증 절차**: 원본을 리페어런트하지 않고 복제본(`BP_UGV_Vehicle_new` 등)을 만들어
A/B 비교 가능하게 구성. ABP가 구 클래스로 하드 캐스트하고 있어 복제본은 "자식이 아니라
형제"라 캐스트가 실패해 바퀴 회전/터렛이 통째로 멈추는 함정도 겪고 해결.

**성공 주행 실측(659샘플)**: 최고 34.6km/h, 평균 19.0km/h, **조향 포화 0회**. 궤도잠금/
스킨스티어/궤도 비주얼 C++ 이관/슬랙 물리 전부 구현 완료(실기 확인).

**변경/신규 파일**: `Vehicles/UGVAIController.{h,cpp}`(커브 감속, 조향 게인, 목적지 투영,
부분 경로 감지), `Vehicles/UGVWheeledVehicleSimulation.{h,cpp}`(신규),
`UGVWheeledVehicleMovementComponent.{h,cpp}`(신규), `UGVWheeledVehiclePawn.{h,cpp}`(신규),
`Tools/TreeCollisionProxyBuilder.{h,cpp}`(신규), `Config/DefaultEngine.ini`
(`DefaultMaxSearchNodes`, `SupportedAgents` Tank radius, `FrameRateLimit`).

---

## 3. 적군 애니메이션 · 전투 · 피격 물리 (`soldiers/`)

### 목적
적군 발걸음이 배속 걸린 것처럼 빠른 버그를 해결하고, 리지드바디 기반 피격 리액션을 절차적
스프링 시스템으로 완전히 교체하며, 3단계 전투 시나리오(1세트 → 3세트 확장)에 맞춰 적군 행동을
분대단위 이동·단계적 도주·타겟 전환까지 고도화한다.

### 결과

**로코모션 — 완전히 다른 두 원인이 겹친 2배속 버그**:
- **원인 A**: 아군은 "gait 안에서의 상대속도(0~1)를 고정 앵커(걷기 300/뛰기 600)에 싣는"
  2단 구조(`BP_ThirdPersonCharacter::EventTick` + `ABP`)인데, 적군은 `GaitTopSpeed`에
  상황별 임의 속도를 그대로 넣어 수식상 축이 실제 cm/s가 돼버려 블렌드스페이스와 어긋남 —
  아군과 동일 구조로 통일(`ApplyGaitForDesiredSpeed()` 신규).
- **원인 B(진짜 2배속)**: 원인 A를 고쳐도 남아있던 리포트를 재조사해 발견 — `ABP_Enemy_
  kadex2` AnimGraph에서 스테이트머신 출력이 캐시 없이 **두 갈래**로 갈라져 있어, "포즈 출력이
  N개 입력에 연결되면 그 상류 체인이 프레임당 Update를 N번 받아 시간이 N배로 흐른다"는 엔진
  특성 때문에 정확히 2배속이 되고 있었음(이전 세션에서 Slot 갈래를 추가하며 기존 직결을 안
  끊은 게 원인). `Save Cached Pose` 삽입으로 스테이트머신 소비자를 하나로 정리해 해결.

> **오진 방지 메모(문서에 직접 기록)**: 애니메이션이 이상하게 빠르면 재생 배율(playRate 등)
> 보다 **포즈 fan-out(출력 핀 연결 2개 이상)을 먼저 확인**할 것 — 단, 시간이 흐르는 노드가
> 포크 상류에 있을 때만 문제가 되고 캐시 하류의 포크는 무해함(아군 그래프에도 3갈래 포크가
> 있지만 무해한 사례로 대조 확인).

**피격 리액션 — 감쇠조화진동자로 완전 교체**: 처음 시도한 `UPhysicalAnimationComponent`
기반 진짜 리지드바디는 스프링/자기충돌/솔버 반복을 다 바꿔봐도 원인불명의 떨림이 계속돼
폐기(Hips 킨네마틱 고정 + 조인트 체인 구성 자체가 문제였던 것으로 추정). 물리 엔진을 전혀 안
건드리는 순수 계산 스프링(`Accel = -Stiffness·Offset - Damping·Velocity`)으로 교체 — 애니메이션
위에 오프셋만 얹어 발산·충돌·조인트 문제가 구조적으로 없음. 몸통/다리(좌우)/팔(좌우)/Hips
부위별 스프링 + "지렛대"(관절에서 피격점까지 거리 비례 배율) + "채찍"(몸통 피격 시 팔다리에
반대 부호 킥) 절차적 장치로 사용자 요구("끝단을 맞을수록 크게 튀어나옴")를 구현. 팔 IK와
안 겹치도록 IK **출력 본**(`LeftHand`)을 킥해서 자연 감쇠+IK 재수렴을 동시에 얻는 트릭도 적용.

**사망 임펄스 — 두 겹 버그**: `DeathImpulseMagnitude`를 400→1억5천만까지 올려도 안 보인다는
리포트. ① 실제 스폰 클래스(`BP_Enemy_kadex`) CDO가 이 값을 0으로 독립 오버라이드하고 있어
부모값이 안 먹힘(자식이 부모 CDO를 그림자화하는, 이번 주에만 3회 반복된 패턴). ② Chaos 타이밍
버그 — `SetAllBodiesSimulatePhysics(true)` 직후 같은 프레임에 임펄스를 걸면 Chaos가 전환을
아직 처리하기 전이라 조용히 씹힘, `DelayUntilNextTick` 삽입으로 해결.

**3단계 전투 확장(Part A~G, 계획보다 넓게 전부 완료)**: 사망 시퀀스를 스크립트 몽타주에서
`UPhysicalAnimationComponent` 액티브 랙돌로 교체(Part A), 전투지 zone을 1세트→3세트 배열로
확장(Part B), 분대단위 저속·총내림·숙임 접근 이동 + Perlin 노이즈 기반 사주경계(Part C),
단계적 도주(개체별 랜덤 지연)+타겟 후보 랜덤 풀(상위 5명 중 무작위, 근접후보 1500cm 이내로
제한)+선호도(`EEnemyTargetPreference`: Vehicle/Infantry/SpecificActor)로 국면별 타겟 전환
(Part D), 시나리오 트리거/이펙트 배선(Part F), `bEngaged` 게이트로 경계 이동↔교전 전환을
명시적으로 분리(Part G) — "UGV RCWS 최초 사격 전까지 적군은 교전이 아님"이라는 요구사항을
정확히 구현.

**둘러보기 연출 — 몽타주 시도 폐기 경위**: 전용 몽타주를 얹었으나 처음엔 슬롯이 안 맞아
안 보이고, 슬롯을 만들어도 애니메이션 원본이 전신 모션이라 웅크린 이동 자세와 안 맞았음.
새 애셋/몽타주 없이 원래 있던 스윕 메커니즘의 각도만 ±20°→±60°로 키우는 게 정답이었음
(배선 자체는 남겨서 나중에 상체 전용 애셋이 생기면 재활용 가능).

**적군 고개 각도(`PatrolAimPitchDeg`)**: 적군엔 `AimPitch`를 갱신하는 주체가 아예 없어(아군의
`EventTick`에 대응하는 게 없음) 사실상 항상 0이었던 것을 발견, 경계 이동 중엔 12°를 쓰고
교전/전투지 도착 시 0으로 되돌리도록 수정.

**남은 이슈**: `LayeredBoneBlend_0`의 `BranchFilter`가 `blendDepth -1`이라 `Hit_Reaction`
몽타주가 실제로는 화면에 반영되지 않고 있을 가능성 발견, 미수정. 팔 부호값(`HitReactionArm*
Sign`) 3종은 플레이스홀더(1.0) 상태로 미실측. 이동 중 피격 비틀거림 애니메이션 미구현.

**변경 파일**: `Soldiers/EnemyCombatComponent.h/.cpp`, `BP_Enemy_Base`/`BP_Enemy_kadex`
EventGraph, `ABP_Enemy_kadex2` AnimGraph, `UI/ScenarioStepTypes.h`,
`UI/ScenarioStateSubsystem.cpp`.

---

## 4. 피격 이펙트 (`hit_effects/`)

### 목적
`New_kadex_0811`의 지형·바위·나무·PCG 프록시 전체에 재질별(Wood/Hard/Dirt/Metal/Glass) 피격
이펙트가 실제로 나오도록 물리재질을 배선하고, VFX 노출(밝기) 문제를 해결한다.

### 결과

**핵심 발견 — 물리재질은 컴포넌트 오버라이드로 걸어야 함**: `p.RCWS.ImpactDebug` 로그로 실측
비교한 결과, 컴포넌트 `PhysMaterialOverride`는 항상 동작하지만 머티리얼의 `PhysMaterial`은
에셋에 따라 다르고(트럭은 되는데 바위/나무는 안 됨), 메시 `BodySetup.PhysMaterial`은 바위/
나무에서 아예 안 됨. 원인은 `RCWSProjectile::HandleImpact`의 재질 판정이 `Hit.PhysMaterial`
(`bReturnMaterialOnMove=false`라 항상 null)이 아니라 **탄도 방향 ±50cm 보조 콤플렉스
라인트레이스**로만 이뤄지기 때문 — 이 경로가 `PhysMaterialOverride`만 안정적으로 읽음.
**머티리얼/BodySetup에 걸었던 44개 에셋 전부 시도 후 효과 없음이 확인돼 revert.** 바위 69개 +
나무 213개 + PCG 프록시(ISM 23개)에 컴포넌트 오버라이드로 재배선 완료 — 레벨(`__ExternalActors__`)
에 저장되므로 디자이너가 아트 에셋을 새 버전으로 올려도 안 날아감(원래 걱정거리가 저절로 해결).

**지형은 별도 경로**: 랜드스케이프 머티리얼의 `Physical Material Output` 노드가 있으면
Landscape 액터의 `DefaultPhysMaterial`은 무시됨. 게다가 베이크된 물리재질 데이터가 어디를
쏘든 인덱스 0(`PM_Cliff`)만 반환(레이어 가중치 미반영)해서, 처음엔 절벽만 Hard로 뒀다가 평지도
Hard로 나오는 문제가 생겨 결국 5종 전부 Dirt로 통일.

**PCG 나무 콜리전 프록시**: `ObjectType`을 `WorldDynamic`(프로젝타일이 이 채널을 총알끼리 안
부딪히려고 Ignore함)에서 `WorldStatic`으로, `Visibility` 응답을 Block으로(재질 조회 트레이스가
이 채널을 씀 — 나무가 시야를 가리는 것도 의도된 동작) 바꿔서 해결. 이후 종별 실측 줄기 치수 ×
개체별 원래 스케일로 반경을 재산정해(지름 16.5~39.0cm) 과도하게 굵던 보이지 않는 기둥을
현실적인 크기로 축소, 월드 원점 유령 인스턴스 23개도 같이 제거.

**VFX 밝기(nit 스케일) — 16개 이미터 노출 문제**: 이 레벨은 노출이 고정(지면 ≈200 nit)이라
`Unlit` 머티리얼 이미터의 색이 그대로 nit 값이 되는데, 스파크/화염 이미터 값이 1~20 수준이라
200 nit 배경에 완전히 묻혀 검게 보이고 있었음 — 스파크류는 ×200(4000/1650/187.6), 화염은
2500으로 상향. RCWS "작은 화염"은 `MI_ExplosionRoil_8x8`의 블랙바디 발광 자체는 켜져 있었으나
`Emissive Gain=1`이라 안 보인 것 — 공유 자산이라 직접 고치지 않고 복제(`MI_RCWS_ImpactFire`,
Gain 2000)로 우회, 혈흔이 같은 자산을 공유해서 원본을 고쳤으면 "피가 빛나는" 부작용이 났을
것을 사전에 피함. 총구화염/트레이서/포신연기 등 같은 세션에서 발견한 다른 VFX 밝기 문제도
같이 정리.

**남은 것**: 숲 안쪽 PCG 나무·바위는 성능상 의도적으로 `NoCollision`이라 총알이 통과함(길가
나무만 프록시로 커버). Dirt/Wood 재질은 탄흔 데칼이 비어있음(파티클/사운드는 나옴).

**변경 파일**: `RCWSProjectile.cpp`(진단 콘솔 변수 `p.RCWS.ImpactDebug`, 주의 주석), 레벨
`New_kadex_0811.umap`(컴포넌트 오버라이드 282개), `Land_scape_m/PhysicalMaterials/` 5종,
신규 `VFX/RCWS/MI_RCWS_ImpactFire`, `VFX/Rifle/MI_Rifle_ImpactDust`(+`_Wispy`).

---

## 5. 드론 (`drone/`) — 신규 비행 물리 모델

### 목적
기존 `AUAVPawn`의 운동학 근사 비행(자세 개념 없음, 기울기가 가속도의 사후 사상)을 실제 드론과
같은 원인-결과 순서(로터 추력 → 토크 → 강체 운동)로 재구현한다. 기존 UAV와 코드/에셋 완전
분리, 짐벌/시나리오 등은 의도적으로 이식하지 않고 구동계 자체만 검증.

### 결과

**기존 방식의 한계(왜 새로 만들었나)**: 실측 비교 결과 기존 코드는 순항 정착 시 필요각의
1/8(2° vs 실제 15~25°), 최대가속 시 1/3(6° vs 17°), 선회 시 롤 자체가 없음(0°), 기울기 도달도
5배 느림(1초 vs 실제 0.2~0.3초) — "기울여서 가는 게 아니라 가니까 기울어 보이는" 위화감이
근본적으로 해결 안 되는 구조였음.

**제어 흐름(순서를 뒤집음)**: 스틱 입력(스로틀/피치/롤/요) → 자세 P 루프(목표 추력축 방식,
오일러각 대신 축각 벡터로 짐벌락/부호 사고 방지) → 각속도 PID(게인을 토크가 아니라
각가속도 단위로 잡아 콜리전 도형/기체 제원이 바뀌어도 조종 반응 유지) → 믹서(의사역행렬,
포화 시 자세 우선 총추력 양보) → 모터 1차 지연(τ=0.06s) → 로터별 힘을 Chaos에 직접 인가.
적분·접촉은 전부 Chaos가 담당 — 자체 6-DOF 적분기로 처음 구현했다가, 콜리전이 없어 경사면/
구조물 착지가 불가능함을 확인하고 물리 모델은 그대로 두고 적분·접촉만 Chaos로 이관.

**로터 자동 인식**: 암 길이·배치를 코드에 하드코딩하지 않고 메시 본(`Wing_` 접두사) 위치에서
`BeginPlay`에 자동 수집, 방위각 순 정렬 후 회전방향을 번갈아 배정해 호버 중 총 반토크가 정확히
0이 되도록 구성 — 디자인팀이 메시를 옮기거나 로터 개수를 바꿔도 물리가 자동으로 따라감.

**현재 상태**: 구동계 + 수동 조종 완성, **조이스틱(Logitech Extreme 3D Pro) 실기 조종 검증
완료**. IMC 등록을 `NotifyControllerChanged`/`SetupPlayerInputComponent` 양쪽에서 시도하도록
이중화(초기 빙의 시점의 호출 순서가 상황마다 달라 한쪽만 믿으면 조용히 실패하는 문제 방지),
테스트 레벨(`L_DroneTest`)에 착지대/게이트/슬라롬/계단탑 등 6그룹 29개 장애물 배치.

**남은 이슈**: 관성텐서 보정이 목표에 못 미침(`InertiaTensorScale`이 두 번 곱해지는 것으로
추정, 조종감 영향은 작음). 제어 루프가 60Hz(이 프로젝트는 UGV 거동 편차 때문에 물리
서브스테핑이 꺼져 있어 서브스텝 단위로 못 돌림). **IMC/IA 등 신규 에셋은 MCP로 만들면 안
됨** — `.uasset`엔 정상 반영돼도 에디터 편집 창엔 비어 보이는 원인 미상 버그를 겪어 "새 에셋은
수동 생성" 원칙으로 정리. 리플리케이션/사운드/짐벌 미구현.

**다음 단계(자율비행)**: 위치 P → 속도 PI → 목표 경사각을 수동 조종과 동일한 입구
(`SetStickInput`)로 투입하면 되는 구조로 이미 설계해둠. `BeginMissionToTarget`/`EnemyCube`
태그/`DT_ScenarioSteps` 인터페이스는 기존 UAV 것을 그대로 재사용 가능 — 완성 전까지는 기존
`AUAVPawn` 경로를 살려두는 게 전시 일정 리스크상 안전하다고 판단, 교체는 보류.

**신규 파일**: `Source/titan_example/Drone/DroneFlightComponent.h/.cpp`,
`DronePawn.h/.cpp`, `DroneTestGameMode.h/.cpp`, `/Game/Drone/`(BP_Drone, 입력 액션 6종,
`L_DroneTest` 레벨).

---

## 기타 메모

- 이번 주에만 "자식 블루프린트 CDO가 부모값을 독립 오버라이드해서 부모만 고치면 조용히
  무효화되는" 패턴이 최소 3회(히트본 콜리전, 사망 임펄스, 죽는 순간의 자세값) 반복됨 —
  다음에 비슷한 증상(부모 값을 고쳤는데 안 먹힘)을 만나면 자식 CDO부터 확인할 것.
- MCP 대량 배열 쓰기(차분 방식) 함정이 나무 콜리전 프록시(1절)에서 두 번 에디터를 얼려먹은
  뒤, 아예 네이티브 에디터 툴(`ATreeCollisionProxyBuilder`)로 대체하는 결정으로 이어짐 —
  앞으로 수백~수천 개 단위 배열 수정이 필요하면 MCP 스크립트 대신 처음부터 C++ 에디터 툴을
  고려.
