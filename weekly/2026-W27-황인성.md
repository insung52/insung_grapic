# 작업 내역

> 기간: 2026-06-26 ~ 2026-07-03
> 담당: 황인성

---

## 1. Filament v1.72.0 머지 및 전체 플랫폼 빌드 이슈 해결

전체 수정사항 설명 문서 : [260629_황인성_filament_v1.72.0_merge_report.md](/2026-W27-황인성/260629_황인성_filament_v1.72.0_merge_report.md)

### 목적
사내 filament 미러(`code.grapicar.com/engine-dev/core/filament`)를 v1.71.x 기반에서
upstream `rc/1.72.0`으로 머지하고, grapi-base 엔진 전체 플랫폼(Windows / Linux Desktop /
Android / WebGL / Embedded)에서 빌드 및 샘플 실행이 정상 동작하도록 한다.

### 진행 계획
1. 내부 브랜치(`merge_v1.72`) 기준으로 upstream과 머지 충돌 해결
2. filament 자체 빌드 에러 수정
3. grapi-base 엔진 레이어에서 발생하는 API 변경 대응 및 플랫폼별 빌드 이슈 해결
4. Linux WSL 환경에서 backend_test_linux(OpenGL + Vulkan)로 테스트 검증

### 결과

#### 1) 머지 충돌 해결 (22개)

| 파일 | 충돌 내용 | 해결 방법 |
|---|---|---|
| `VulkanDriver.cpp` | `prepareDraw()` vs `draw2()`, COMPUTE API 함수군 | v1.72 `prepareDraw()` 채택, 사내 COMPUTE 코드 유지 |
| `PostProcessManager.cpp` | `getMaterial()` 호출 방식, `vsmMipmapPass` 함수 | v1.72 채택 + `vsmMipmapPass` 제거(v1.72에서 삭제됨) |
| `ShadowMapManager.cpp` | `if (UTILS_UNLIKELY(blur))` 블록 | QNX 워크어라운드 포함 삭제(v1.72에서 제거됨) |
| `OpenGLContext.h` | inline 구현 블록(핵심 충돌) | Block 1은 HEAD 유지, Block 2는 최초 잘못 해결 -> 수동 재수정 |
| `presubmit.yml` | Windows runner 이름 충돌 | 사내 `win-2019-16core` 유지 |

#### 2) filament 빌드 에러 수정 (15건)

**핵심 수정 - OpenGLContext.h 아키텍처 변경 대응**
- v1.72.0에서 `activeTexture`, `bindSampler`, `setScissor`, `viewport`, `depthRange` 등
  20여 개 함수가 `OpenGLContext`에서 `OpenGLState` 클래스로 이동됨
- 머지 과정에서 HEAD의 inline 구현을 그대로 유지했으나, 이미 `OpenGLState`로 이전된 함수라
  중복 정의 에러(`out-of-line definition does not match any declaration`)가 발생
- 해결: 해당 inline 구현 전부 제거 (함수 자체는 `OpenGLState.h`에 이미 존재)

**Scene.cpp 자동 머지 아티팩트 3건 (렌더링 버그)**

| 버그 | 증상 | 원인 | 수정 |
|---|---|---|---|
| `tangents` 포인터 선언 누락 | 빌드 에러 (`use of undeclared identifier 'tangents'`) | 머지 중 선언 줄 삭제됨 | 포인터 선언 복구 |
| `TANGENT` 쓰기 누락 | area_light 샘플에서 에어리어 라이트가 방향성 없이 포인트 라이트처럼 보임 | upstream에는 TANGENT 쓰기 없고, grapi-base 쪽은 있었는데 자동 머지가 누락시킴 | TANGENT 쓰기 복구 |
| GPU 라이트 타입 패킹 오류 | 셰이더가 area light를 인식 못 함 | 자동 머지가 `static_cast<uint8_t>(lcm.getType(li))` 라는 양쪽 어디에도 없던 코드를 생성 - `LightManager::Type` enum(AREA=5)을 그대로 셰이더에 넘기면 GPU 패킹 필드 기대값(0=point/1=spot/2=area)과 매핑이 다름 | 수동 매핑 복원 |

**그 외 빌드 에러 수정**
- `DriverEnums.h`, `ostream.cpp`, `VulkanHandles.cpp`, `GLDescriptorSet.cpp`,
  `OpenGLProgram.cpp`, `GLUtils.h`, `VulkanCompute.cpp`: 사내에서 추가한 enum 값
  (`STORAGE_IMAGE`, `INDIRECT`, `NONE`)이 v1.72 신규 switch 문에서 누락되어 발생한
  "enumeration value not handled in switch" 에러들을 해당 케이스 추가로 일괄 수정
- `test_MipLevels.cpp`: 자동 머지로 살아남은 stale 코드(`descriptorSetLayout` 미선언) 제거,
  `#ifdef` 블록 내 변수 스코프 버그(`descriptorSet13`) 수정

**WebGL(Emscripten) 전용 수정 (4건)**

| 에러 | 원인 | 수정 |
|---|---|---|
| bluegl x86_64 어셈블리 컴파일 시도 | grapi-base가 `WEBGL=1`을 쓰지만 filament는 `WASM` 변수로 데스크탑 전용 라이브러리를 제외 - `WASM` 미설정으로 bluegl이 포함됨 | 루트 `CMakeLists.txt`에 `if(WEBGL) set(WASM TRUE)` 브리지 추가 |
| spirv-cross `throw` 불가 | grapi-base WebGL 빌드 전역 `-fno-exceptions` + filament가 기본으로 `SPIRV_CROSS_EXCEPTIONS_TO_ASSERTIONS=OFF` | `CMakePresets.json` webgl-base에 `FILAMENT_ENABLE_EXCEPTIONS: OFF` 추가 |
| `actor_exporter.cc` try/catch | `-fno-exceptions` 환경에서 `try` 불가 | 파일 쓰기 블록 try/catch 제거(반환값 검사로 대체), `copyTextureFile`은 `std::error_code` 오버로드로 교체 |
| `custom_material_provider.cc` try/catch | `Material::Builder::build()`는 throw하지 않아 try/catch가 불필요했음 | try/catch 제거, nullptr 검사로 대체 |

**Embedded(Telechips TCC803x, GCC 9.2.1 AArch64) 전용 수정 (5건)**

| 에러 | 원인 | 수정 |
|---|---|---|
| `std::countr_zero` / `std::popcount` 미선언 | C++20 `<bit>` 헤더가 GCC 9에 없거나 미구현 (`__cpp_lib_bitops` 미정의) | `PagedArenaBitset.cpp`에 `__cpp_lib_bitops` 분기 + GCC builtin(`__builtin_ctz` / `__builtin_popcount`) 폴백 구현 |
| `vcleq_f32` 반환 타입 오류 (`uint32x4_t` -> `float32x4_t` 불가) | GCC는 벡터 타입 간 암묵적 변환 불허. Clang(데스크탑/Android NDK)은 허용했기 때문에 GCC 첫 빌드에서야 드러남 | `ColorGradingNeon.h` 비교 결과 변수를 `uint32x4_t`로 수정 |
| GCC 9 ICE (`internal compiler error: cp/typeck.c:2384`) | 함수 템플릿 + lambda 내 designated initializer 조합에서 GCC 9 컴파일러 자체 버그 | `ShadowMapManager.cpp`에서 lambda 안의 designated initializer를 미리 선언한 변수에 필드 대입 방식으로 우회 |
| `std::ranges::find_if` 미선언 | C++20 ranges가 GCC 9에 미지원 | `TextureCache.cpp`에서 `std::find_if`로 교체 |
| `R_AARCH64_ADR_PREL_PG_HI21` 재배치 에러 (shared lib 링크 불가) | Yocto SDK `CFLAGS`에 `-fPIC` 미포함 + `-fstack-protector-strong`이 `__stack_chk_guard`에 대해 page-relative 재배치 생성 -> `-fPIC` 없는 정적 라이브러리는 shared object에 링크 불가 (x86_64에서는 RIP-relative 방식이라 해당 없음) | `CMakePresets.json`의 `telechips-tcc803x-base`에 `CMAKE_POSITION_INDEPENDENT_CODE: ON` 전역 설정으로 일괄 해결 |

#### 3) grapi-base 빌드 에러 수정 (8건)

**API 변경 대응**
- `ktx2_reader.cc`: `get_format()` -> `get_basis_tex_format()`으로 이름 변경
- `settings.cc`: `sunAngularRadius` -> `sunAngularRadiusDeg` (2곳, 단위 명시로 이름 변경)

**RTTI 혼재 링크 에러**
- v1.72.0에서 `VulkanPlatformLinux`가 별도 클래스로 분리됨 - filament 전체는 `-fno-rtti`로 빌드되어 typeinfo가 없음
- `context.cc`는 RTTI on 상태로 `VulkanPlatformLinux`를 상속하는 파생 클래스를 정의 -> 부모 typeinfo 심볼 없음 -> 링크 에러
- 해결: `base/CMakeLists.txt`에서 `context.cc`에만 `-fno-rtti` 설정 (`context.cc`는 `dynamic_cast`/`typeid`를 실제로 사용하지 않으므로 동작 변화 없음)

**샘플 관련 이슈**
- `ai_studio.cc`: `<unistd.h>` 누락으로 `STDIN_FILENO` 미선언 + `FILAMENT_SUPPORTS_X11` define 누락으로 `SDL_SysWMinfo` 구조체의 `info.x11` 멤버가 조건부 컴파일로 제외되던 문제 수정
- `.filamat` 바이너리 버전 불일치: 기존 `car_paint.filamat`, `sky.filamat`, `water.filamat`가 material version 70으로 컴파일된 반면 v1.72.0은 version 72 요구 -> `matc`로 재컴파일. 빌드 시 자동 재컴파일되도록 `custom_materials` CMake 타겟 추가
- 병렬 빌드 복사 레이스 컨디션: 20개+ 샘플이 각자 `grapi-base.so` 등을 동시에 복사하다 충돌 -> `copy_runtime_libs` 단일 커스텀 타겟으로 통합 (`copy_if_different`로 증분 빌드 성능도 개선)
- Embedded: `svg_test` / `lottie_player`의 `target_link_libraries` 호출이 `FILAMENT_EMBEDDED_BUILD` 시 타겟 없이 실행되던 문제 -> `if(TARGET ...)` 가드 추가

#### 4) 테스트 결과

| 플랫폼 | 빌드 | 샘플/런타임 | 비고 |
|---|:---:|:---:|---|
| Windows (MSVC x64) | O | O | 샘플 실행 성공 |
| Linux Desktop (Clang) | O | O | lavapipe 소프트웨어 렌더러, `area_light`/`car_paint`/`skywater` 등 정상 |
| Filament backend_test (OpenGL) | O | O | 71개 중 65 PASS / 3 SKIP / 3 FAIL (모두 llvmpipe 환경 한계, 코드 문제 아님) |
| Filament backend_test (Vulkan) | O | O | 71개 중 63 PASS / 8 SKIP / 0 FAIL |
| Android (arm64/arm/x64/x86) | O | - | `.so` 생성 확인, 실기기 테스트 미실시 |
| WebGL (Emscripten) | O | O | 브라우저 실행 확인 |
| Embedded (Telechips TCC803x) | O | - | `.so` 생성 확인, 실기기 테스트 미실시 |

> **OpenGL 3건 실패 상세**: `BackendTest.FeedbackLoops` / `BlitTest.ColorResolve` / `LoadImageTest.UpdateImage3D` - 모두 llvmpipe(CPU 소프트웨어 렌더러)의 하드웨어 기능 부재(MSAA 미지원, RGBA16F+2D array 미지원)로 인한 환경 한계. upstream 구글 버그 트래커(`b/453756688` 등)에 이미 등록된 known issue.

> **미검증 영역**: VulkanDriver COMPUTE API(GPU 실기기 필요), QNX 빌드(QNX 툴체인 필요), Embedded 실기기 실행(Telechips TCC803x 보드)

---

## 2. MISRA 검증 조사 및 grapi-base 정적 분석

전체 탐지된 버그/경고 정리 문서 : [260702_황인성_clangtidy_cppcheck_verification_report](/2026-W27-황인성/260702_황인성_clangtidy_cppcheck_verification_report.md)

### 목적
MISRA / ASIL 기반 코드 검증 요구 대응을 위해 관련 개념과 도구를 조사하고,
무료 정적분석 도구 2종(Clang-Tidy, Cppcheck)을 grapi-base에 실제로 돌려
현재 코드의 품질 이슈를 파악·수정한다.

### 진행 계획
1. MISRA / ASIL / ISO 26262 / 검증 단계 등 배경 개념 정리
2. 정적분석 도구 조사 (유료/무료 15개 이상 비교)
3. 무료 도구(Clang-Tidy + Cppcheck) grapi-base `base/` 모듈(131개 파일)에 적용
4. 발견 이슈 분류·수정 및 재스캔 반복으로 최종 0건 달성

### 결과

#### 1) 개념 조사

- **MISRA vs ASIL**: MISRA는 코드 레벨 규칙(pass/fail 목록), ASIL은 기능의 위험도를 HARA(위험분석)로 미리 산정하는 등급(QM~D) - 같은 선상의 비교 대상이 아님. ASIL 등급이 부과한 요구를 MISRA 준수로 만족했는지 도구로 검증하는 구조
- **검증 단계 분류**: 정적분석(코딩 표준 + 결함탐지) -> 형식 검증(추상해석 기반 수학적 증명) -> 동적분석(ASan/UBSan/Valgrind) -> 커버리지(MC/DC 등) -> 추적성 -> 인증(TUV/exida)
- 그래픽 엔진 특성(SIMD/템플릿/멀티스레딩/GPU API)과 MISRA 충돌 가능성이 높은 영역 파악 (동적 메모리 할당, 가상함수, 재귀, 예외처리 등)

#### 2) 도구 조사 (15개)

도구를 4그룹으로 분류하고 MISRA C/C++ 지원 버전, ASIL Tool Qualification(TUV/exida) 인증 여부, 가격 구조를 비교했다.

| 그룹 | 특징 | 해당 도구 |
|---|---|---|
| **A. 풀스택 상용** | MISRA/AUTOSAR/CERT 동시 지원 + ASIL TQSK 제공 | Polyspace, Coverity (~$5만~40만/년), Klocwork, Parasoft (~$5.6만/년), Helix QAC, LDRA, Axivion, CodeSonar |
| **B. 린트 계열** | 가볍고 빠름, MISRA C++:2023까지 지원 | PC-lint Plus (636유로/개발자/년~, exida 인증) |
| **C. 형식 검증** | "런타임 에러 없음"을 수학적으로 증명, ASIL D급 | Astree(AbsInt), Polyspace Code Prover, TrustInSoft |
| **D. 무료/오픈소스** | 비용 없이 1차 필터, MISRA 커버리지는 상용 대비 제한 | Cppcheck, Clang-Tidy, PVS-Studio(OSS 무료), SonarQube(Community) |

- **Clang-Tidy**: 공식 MISRA 체커 없음, 모던 C++ 품질 관리에는 강력함 (LLVM 최신 C++ 파서 정확도 우수)
- **Cppcheck**: 기본 MISRA 패턴 체커 보유(오픈소스). MISRA C 규칙 문구 원문은 MISRA C 문서(PDF 15파운드~/건) 별도 구매 필요.
- 그러나 MISRA C++ 은 지원 x
- **권장 접근**: 무료 도구로 자사 코드베이스 위반 규모를 먼저 가늠 -> ASIL 적용 대상 명확하면 TUV/exida 인증 + TQSK 보유 도구로 PoC 비교

#### 3) grapi-base 정적분석 수행 및 이슈 수정

**스캔 환경**: LLVM 19 / Cppcheck 2.21.0 / `base/` 모듈 131개 파일

**발견 이슈 분류 요약**

| 등급 | 건수 | 내용 |
|---|---|---|
| **A. 잠재적 버그** | 17건 | 실제 런타임 오동작 가능성 - 전 건 수정 완료 |
| **B. 타입/안전성 문제** | ~50건 | 타입 변환 오류, 부호/비트 연산, C 스타일 캐스트 등 |
| **C. 코드 설계 문제** | ~20건 | API 설계, 재귀, Rule of Five, virtual 소멸자 등 |
| **D. 성능/스타일 개선** | ~1,836건 | `[[nodiscard]]`, `= default`, unused 파라미터, getter const& 리턴 등 |
| **F. 인프라 패턴 (오탐)** | ~420건+ | pointer-arithmetic, `malloc`/C API, glm 생성자 오탐 등 불가피한 경고 |

**주요 A등급 버그 수정 내역**

- **A-1**: `vehicle_component.cc:940` - `if` 조건문에서 `!=` 비교여야 할 자리에 `=` 대입 (`setRearSuspension`). 위아래 줄이 모두 `!=`인데 해당 줄만 `=` - 조건이 항상 true가 되어 나머지 비교가 단락(short-circuit)되고 이중 대입 발생. `=` -> `!=` 수정
- **A-2**: `vehicle_component.cc:144` - `isCar() ? car_wheel_scale[w] : car_wheel_scale[w]` (true/false 양쪽 동일). `local_rotation` 분기를 복붙 후 false 브랜치를 `mortor_wheel_scale`로 수정하지 않은 복붙 버그. 모터사이클 휠 스케일이 항상 car 기준 적용됨
- **A-3**: `geometry_component.cc:554~568` - `attribute_indices_`가 비어있을 때 진입하는 `else` 분기에서 `attribute_indices_[i]`로 빈 배열에 OOB 접근. 상단의 동일 패턴 복붙 후 인덱스를 `i`로 바꾸지 않은 것. `attribute_indices_[i]` -> `i`로 수정
- **A-4**: `ktx2_reader.cc:566,647` - `malloc` 반환값 null 미체크 후 즉시 `memcpy`에 전달 (UB). null 검사 추가
- **A-15/A-16/A-17**: Cppcheck/Clang-Tidy v9 재스캔에서 추가 발견된 미사용 변수 및 헤더 파일 버그 - 전 건 수정 완료

**스캔 반복 과정에서 발견한 주요 이슈**

> **[트러블슈팅] Cppcheck 억제 주석 ID 오류 (A-5)**
> - glm 생성자 인자 구분자(`,`)를 쉼표 연산자로 잘못 해석하는 오탐에 `// cppcheck-suppress suspiciousCommaExpression`을 추가했으나, 이는 실제로 존재하지 않는 ID - 억제가 조용히 실패하고 `Unmatched suppression` 메시지만 늘어남
> - **원인 확인**: `--template` 옵션 없이 실행하면 메시지 끝에 `[constStatement]`처럼 정확한 ID가 표시됨. `--errorlist`로도 확인 가능
> - **교훈**: cppcheck 억제 ID는 메시지 문구만 보고 추측하지 말고 실제 ID를 반드시 확인해야 함

> **[트러블슈팅] Clang-Tidy HeaderFilterRegex 설정 버그 (v9 재스캔, 중요)**
> - `.clang-tidy`의 `HeaderFilterRegex: '.*(grapi-base/(base|samples)).*\.(h|hpp)$'`가 슬래시(`/`) 기준인데, Windows 컴파일 DB 경로는 백슬래시(`\`) - **처음부터 헤더 파일 진단이 전부 숨겨져 있었음**
> - 증거: `clangtidy_v2.txt` 1852건 전체가 `.cc` 파일 경고뿐, `.h` 파일 경고 0건
> - **수정**: `HeaderFilterRegex`를 `'.*grapi-base[/\\](base|samples)[/\\].*\.(h|hpp)$'`로 변경 (양쪽 슬래시 모두 매치)
> - 재스캔(v9) 결과: 헤더 전용 신규 경고 1,833건 노출 -> 잠재 버그 후보 6건 전수 검토(2건 실버그 확인·수정, 1건 오탐, 3건 Rule of Five로 통합) + 대량 카테고리(D-22~D-27) 전부 분류·코드 **수정 진행중**

**최종 스캔 결과**

| 도구 | 최종 스캔 | 결과 |
|---|---|---|
| Cppcheck (v8) | `base/` 131개 파일 | 경고 **0건** (정보성 메시지 21건만 잔존, 의도적 보류) |
| Clang-Tidy (v6, `.cc` 기준) | `base/` 131개 파일 | 유저 코드 경고 **0건** |
| Clang-Tidy (v9, 헤더 포함) | `base/` `.h`/`.cc` 포함 | A등급 버그 2건(A-16/A-17) 추가 발견·수정, 대량 카테고리 처리 진행 중 |

### 이후 작업
- Clang-Tidy 헤더 포함 후 발견된 버그 및 경고들 수정 진행 중
- Clang-Tidy/Cppcheck 스캔 범위를 Linux, Android 등 다른 빌드 환경으로 확장 예정 (현재 Windows MSVC 기준만 완료)
- 각 플랫폼별 빌드 경고 종합 분석 및 수정 예정
