# 작업 내역

> 기간: 2026-08-28 ~ 2026-09-03
> 담당: 황인성

---

## 1. LIG 프로토콜 — 1차 답변 반영 + 재매핑/확장

### 목적
LIG(원격통제기 개발사)로부터 1차 문의 회신을 받아 그동안 추정으로 구현해뒀던 UDP/JSON
프로토콜 항목들을 정정하고, LIG가 재량으로 넘긴 탐지 분류(`ObjectClass`)를 최종 확정한다.

### 결과

**LIG 1차 답변(2026-08-28) 핵심 내용**: ICD 오기입 정정 — `UGV_Period_Basicinfo`의
`OperationMode`는 `RC_EmergencyStop`/`RC_EmergencyStopRelease`로 설정/해제되는 것으로 확인
(별도 `EMERGENCY` 전용 필드가 아니었음). `RC_FireWeapon`의 PRESSED/RELEASE는 조이스틱 이벤트
발생 시에만 전송(프레임 단위 아님), RELEASE 유실로 인한 폭주 사격은 "이번 사업에서는 고려하지
않은 것으로 안다"는 답변 — 워치독 자체 구현 검토는 보류. `RC_ActivateFire`/`RC_ActivateMovement`
용도가 재정의됨: `ActivateFire`=조정간 안전, **`ActivateMovement`는 구동계 시동이 아니라
"RCWS 조향 활성/비활성"** — 기존 구현이 오매핑이었던 것을 확인, 2026-08-31에 재작업 완료.
`RC_Movement`의 `BrakeButton`은 `RC_ActivateMovement`(조향 모터 활성)와 AND 조건으로 RCWS
제어 가능 여부를 결정한다는 것도 확정. `ObjectClass`(Human/Car 2분류)는 LIG가 "구분 가능하면
구분해서, 아니면 Car로만 보내도 됨"으로 재량을 넘김.

**RC_ActivateMovement 재매핑**: 차량 시동/이그니션으로 구현했던 것을 RCWS 조향 게이트로
정정 — 2026-08-31 UGV축 세션에서 재구현 완료.

**ObjectClass 6분류 확장**(2026-09-02): 기존 판정 로직이 `Cast<ACharacter>` 성공 여부만으로
Human/Car를 가르는 삼항연산자 한 줄이었는데, 실측 중 **낙하산이 `Car`로 표시되는** 버그를
발견. 조사 결과 낙하산(`BP_Parachute`)은 부모가 `AActor`(대응 C++ 클래스 없음)라 캐릭터가
아니었던 것 — 버그가 아니라 2분류 로직의 필연적 결과였음. `UDetectableTargetComponent::
Faction == EnemyEvidence`(2026-08-03에 "적의 흔적"용으로 추가된 값)가 낙하산의 유일한
안정적 식별자임을 확인하고, 이를 근거로 판정 순서를 명확히 한 **플랫 6값**
(`Ally`/`Enemy`/`UGV`/`MobileCommandPost`/`Drone`/`Parachute`)으로 확장. 2단계 계층
(`Human`→Ally/Enemy, `Car`→UGV/MobileCommandPost/Drone)은 드론이 "Car"에 안 들어간다는
문제로 기각. LIG가 이미 재량이라 답한 사안이라 답변 대기 없이 구현, 통보만 남겨둠. `New_kadex_
0811` 레벨의 탐지 대상 액터 전수 확인 결과 fallback으로 떨어지는 액터 없음을 검증.

**변경 파일**: `Network/UGVRemoteControlTypes.h`(ObjectClass 상수 6종),
`Network/UGVRemoteControlSubsystem.cpp`(`ClassifyDetectedObject` 신설),
`protocol/protocol_icd.md`, `protocol/ugv_rc_feature_gap_analysis.md`, `protocol/lig_questions_
0816.md`(§5-2를 질문→결정 통보로 재작성).

---

## 2. 드론이 구 BP_UAV를 완전히 대체 — 이번 주 최대 비중

### 목적
2026-08-27에 구동계·수동 조종만 완성돼 있던 새 물리 드론(`ADronePawn`)에 자율비행·짐벌·
사운드·바람 반응·단계별 탐지·시나리오 연동·리플리케이션을 전부 이식해 구 `AUAVPawn`/`BP_UAV`를
대체한다.

### 결과

**갭 분석과 처리 방침**: 구 UAV 기능을 전수 비교해 이식/재설계/폐기를 분류. 자율비행은
`EnemyCube` 태그 직선 이동에서 아티스트가 그린 스플라인 경로 추종으로 재설계 이식, 짐벌 자동
정찰(낙하산 관측)은 이식하되 **역할이 승격**(아래), 지형 회피(라인트레이스 추력 보정)와
`EnemyCube` 목적지, 적 탐색 단계·아군 집결 대기는 폐기. 구 클래스는 아직 안 지웠다 — 시나리오
연동 지점이 전부 "드론 우선, 없으면 구 UAV" 폴백 구조라 공존 중, 2프로세스 검증 후 함께 제거
예정.

**자율비행 — UGV 로직을 "구조가 아니라 의미"로 옮겨야 했던 3연속 실패**: UGV `BP_UGV_Vehicle_
new`에서 검증된 Pure Pursuit + 제동 곡선을 이식했으나, 그대로 갖다 붙였다가 세 번 크게 틀렸다.
① 자율주행 속도가 9~10km/h에 고정 — Pure Pursuit을 위치 목표로 잘못 배선해서 목표속도 =
전방주시거리×PositionP로 계산되고 있었고, 저속→짧은 당근→더 낮은 목표속도의 자기강화 루프까지
있었음. 당근은 방향만 주고 크기는 거버너가 준다는 원형으로 되돌려 해결(`bFlyThroughAtSpeed`).
② 45km/h에서 감속이 아예 안 걸림 — 커브 통과속도를 `√(측면가속도/곡률)`로 계산한 식이 상한이
없어 반경 39m 이상에서 사실상 무제한이 됨, UGV의 유계 선형 매핑으로 교체. ③ 바람에 너무 잘
버팀(사용자가 먼저 지적) — 항력 피드포워드가 대기속도(속도−바람) 기준이라 코드가 바람 값을
직접 읽어 지연 없이 완벽히 상쇄하고 있었음(실제 드론엔 풍속계가 없음), 대지속도 기준으로 교체 +
적분기 추가.

> **교훈(문서에 직접 기록)**: "UGV에서 검증된 로직"을 옮길 때 구조가 아니라 의미를 옮겨야
> 한다 — 같은 이름의 게인이 다른 역할을 하는 걸 못 보고 배선하면 이런 실패가 반복된다.

**짐벌 — 밖에서 보면 팬은 되는데 짐벌 시점에선 안 먹는 기묘한 증상(2회 재작업)**: 원인이
두 개 겹쳐 있었음 — ① 델타 쿼터니언을 오른쪽(본 로컬 축)에 곱해서 틸트 축이 엉뚱해짐(컴포넌트
공간이므로 왼쪽에 곱해야 함), ② `SetBoneRotationByName(ComponentSpace)`가 델타가 아니라 절대
CS 방향을 받는데 부모 yaw를 빼고 넘겨서 엔진이 부모 yaw를 상쇄해버림. 구 UGV/UAV가 같은 문제를
어떻게 풀었는지 확인하고서야 정리됨.

**시나리오 재배선 — 낙하산 관측 성공이 UGV 출발 트리거로 승격**: 사용자 요구(08-29): 낙하산
관찰 단계는 무조건 있어야 하고, 그 관찰 성공이 UGV 출발 트리거가 된다. 구 흐름의 "UAV 적 탐색
단계"와 "아군 집결 대기"는 제거. 신규 트리거 `UAVParachuteObserved`, 이펙트
`EnableDroneEnemyDetection`/`MoveDroneToPath` 신설. `UAVSpotted` 행의 트리거를
`UAVEnemyDetected`→`UAVParachuteObserved`로 교체, `DroneSeeEnemies`/`DroneWideView` 행 신설.
드론은 도착 후 스스로 탐지단계를 FriendlyOnly→PlusEvidence(낙하산 보이기 시작)→All(UGV가 적
포착하면)로 단계적으로 넓힌다. `UAVEngagementZoomOut`의 프레이밍 대상에 UGV를 추가(예전엔
아군만 넣어서 정작 교전 주체인 UGV가 화면 밖으로 나갔음).

**리플리케이션 — 클라이언트(자체방호축) 권위로 전환**: 구 UAV는 서버 권위였으나, 새 드론은
조종 주체가 서버(UGV축)가 아니라 클라이언트(자체방호축)라 방향을 뒤집었다. UE5.8의 Chaos
Resimulation을 먼저 검토했으나 기각 — 아직 WIP인 데다 `bTickPhysicsAsync`를 요구하는데, 켜면
441~484ms→68ms까지 최적화해둔 RTSP 종단 지연이 +33ms 늘고 튜닝 끝난 UGV Chaos 차량 거동까지
바뀜. 전용 LAN의 전시 장비 2대뿐이라 클라이언트를 신뢰해도 위협 모델상 문제없다고 판단. 배선은
비신뢰(Unreliable+WithValidation) RPC로 30Hz 위치/회전/속도/짐벌/줌을 서버에 보고하고 서버가
전원에 재전파, 명령류(`OnRep_CommandedPath` 등)는 카운터를 같이 올려 값이 안 바뀌어도 강제
발동시키는 방식. 시뮬 주체가 아닌 프로세스는 물리를 아예 안 돌리고 `RemoteInterpSpeed=12/s`로만
보간.

> **트러블슈팅 - 데모 모드에서 UGV가 영영 출발 안 함**
>
> 데모 실행 모드(자체방호 클라이언트가 안 붙는 전시용 1PC 구성)에서는 클라이언트 권위 규칙의
> 시뮬 주체가 세상에 존재하지 않아 드론이 영영 안 날았고, `HasObservedParachute()`가 계속
> false라 `UAVParachuteObserved`→`MoveUGVToZone1Destination` 체인이 통째로 멈췄다(2차 이동은
> 다른 트리거를 써서 멀쩡해 더 헷갈렸음). 데모에선 아무도 수동 조종을 안 하므로 클라 권위를
> 유지할 이유가 없다는 점에 착안해, 축 판정에 "데모 실행 모드의 리슨서버면 서버가 시뮬한다"는
> 3번 규칙을 추가. 실행 모드는 `GameState::bDemoRunMode`로 리플리케이트되므로 나중에 클라가
> 붙어도 판정이 안 갈린다.

> **트러블슈팅 - 시뮬 주체 미접속 시 드론이 원점으로 빨려들어가 사라짐**
>
> 시뮬 주체가 아직 접속 안 했으면 `RepLocation`이 초기값 `(0,0,0)` 그대로라, 남은 프로세스가
> `RemoteInterpSpeed=12`로 그쪽을 향해 보간하며 드론이 배치 위치에서 월드 원점(지형 아래)으로
> 0.3초 만에 사라짐. `RepLocation`이 초기값이면 배치된 트랜스폼을 그대로 심어 "제자리 보간"이
> 되도록 수정.

**소유권 함정**: 클라이언트가 Server RPC를 보내려면 그 PC가 대상 액터의 Owner여야 하는데,
드론은 레벨에 직접 배치돼 기본 Owner가 없음 — `GameMode::PostLogin`에서 축이 SelfDefense인
PC에게 `SetOwner`로 넘겨줌(빙의는 안 함, 트럭 RCWS를 이미 빙의 중이라 컨트롤러 라우팅으로
드론을 조종). 소유권을 못 넘기면 증상이 "조용히 상태만 안 올라감"이라 경고 로그를 남겨뒀다.

**유니티 빌드 상수 재정의**: `DroneAutopilotComponent.cpp`를 만들 때 `DroneFlightComponent.cpp`의
단위 상수 블록을 복사해왔는데 둘 다 익명 네임스페이스라, 유니티 빌드가 두 `.cpp`를 한 번역
단위로 합치며 `CmPerMeter` 재정의 오류가 났다(파일별 컴파일 시엔 안 드러나서 한동안 잠복).
`DroneUnits` 네임스페이스로 통합해 해결.

**남은 일**: 2프로세스(2대 PC) 실환경 검증 — 단일 프로세스/PIE 2클라이언트까지만 확인됨. 검증
후 구 `AUAVPawn`/`BP_UAV`와 폴백 분기 제거, 2·3차 전투지 추격 스플라인 추가(코드 준비, 레벨
작업만 남음).

**변경/신규 파일**: `Source/titan_example/Drone/`(`DroneFlightComponent`, `DroneAutopilotComponent`,
`DronePawn` 등), `UI/ScenarioStepTypes.h`, `UI/ScenarioStateSubsystem.cpp`,
`DT_ScenarioSteps_ThreeStage`(드론 행 2개 추가).

---

## 3. UGV — 속도 제어기 재작성, 적 거리 기반 속도제한, 신규 6×6 모델 교체, BP→C++ 이관

### 목적
자율주행 속도 제어의 구조적 결함(목표 속도에 한참 못 미침, 기어 헌팅)을 해결하고, 시나리오
요구사항(적과의 거리에 따른 유동적 속도 제한)을 구현하며, 디자인팀이 새로 제공한 6×6 차륜
UGV 모델로 전체 교체하고 남은 블루프린트 로직을 C++로 이관해 정리한다.

### 결과

**속도 제어기 — P 배율에서 PI 절대출력으로**: 기존 제어기는 목표-현재 속도차를 스로틀에
**곱하는** P 하나뿐이라, 목표에 가까워질수록 스로틀이 0으로 빠져 항력·경사와 균형 맞는 지점에서
목표보다 한참 아래로 수렴하고 있었다(로그 증거: 목표 도달 순간 스로틀 0). 처음엔 "경사 감지 후
보정 로직"을 새로 설계하려 했으나, 사용자 지적("목표-현재 속도차를 실시간으로 보고 스로틀을
주면 되는 것 아닌가")으로 방향 전환 — PI 제어기로 교체하니 적분항이 "그 속도를 유지하는 데
필요한 스로틀"을 알아서 쌓아서, 경사 감지 로직 자체가 통째로 불필요해졌다. 조향컷도 스로틀이
아니라 목표 속도에 곱하도록 이동(스로틀에 곱하면 적분과 싸워 커브 빠져나오는 순간 적분이
"터짐"). 실측 결과 미달이 11.97→9.96km/h로 줄었으나, 스로틀 포화(≥0.99)인데도 3km/h 이상
미달인 샘플이 50%(230/458) — **병목이 제어기에서 구동력 자체로 옮겨감**을 실측으로 확인, 기어
헌팅도 이 구동력 부족의 2차 증상으로 설명됨.

**적 거리 기반 속도 제한(15/7/3km/h)**: memo.md 요구사항 — 전투 거리보다 멀면 15km/h, 전투
거리면 7km/h, 가까우면 3km/h로 선형 제한. 기존 거버너가 이미 "여러 입력 중 최솟값이 이기는"
구조였던 자리에 세 번째 입력으로 끼워 넣음(스로틀에 배율을 곱하지 않고 목표 속도 자체를
낮춰서 PI 적분과 안 싸우게). Near~Far를 한 직선으로 이으면 교전 거리에서 9km/h가 나와 요구
(7km/h)와 어긋나서, 꺾은선(Near→Engage, Engage→Far 두 구간)으로 설계. 적이 하나도 없으면
제한을 놓아주는 옵션 기본 켬 — 이 작업 중 "유효 목표 속도를 낸 입력이 하나도 없으면
TargetSpeedKmh가 float::Max로 남아 조향컷마저 무시되는" 기존 잠재 버그도 같이 막음. 스텝
테이블에 `UGVSpeedLimitOn` 행 1개 추가(2차 전투지 이동 시작 시점과 정확히 일치).

**UGV 신규 모델 교체 — SK_UGV_0901/BP_UGV_0901(6×6)**: 디자인팀이 궤도 없는 일반 차륜(6×6)
장갑차 형태 + 단발 중기관총(개틀링 아님) 모델을 새로 제공, 기존 궤도 16륜 `BP_UGV_Vehicle_new`는
그대로 두고 별도 계보로 신규 제작. 스키드 스티어/궤도 잠금(TrackLock) C++는 좌우를 서스펜션
Y좌표 부호로만 나누는 구조라 바퀴 개수 가정이 없어 **그대로 재사용 가능**함을 확인. 블렌더
리깅에서 FBX 익스포트 시 스켈레톤 루트에 Scale 100이 붙는 문제를 겪었는데(익스포터가 m→cm
변환을 좌표가 아니라 노드 스케일로 넘김), 블렌더 씬 자체를 cm 단위로 베이크하는 방식으로
근본 해결 — 2026-07 UGV 스켈레탈 메시 시도 때 겪었던 지뢰(본 축 컨벤션, Root Body 75cm
오프셋, 바퀴 16개 ContactPoint 붕괴)도 이번엔 본을 전부 블렌더 +Y/roll 0으로 만들고 root/Hull1을
둘 다 원점에 배치하는 방식으로 전부 회피됨.

> **트러블슈팅 - 코너에서 속도를 주체 못 하고 부딪힘(휠 반지름·개수 변경의 파급)**
>
> 엔진 소스 확인 결과 원인 3개: ① 제동력(`BrakeTorque`)은 기어박스를 안 거쳐 휠에 직접
> 걸리므로 기어비 보상을 못 받음, ② 코너링 강성이 휠당 상수라 총 횡력이 휠 수(16→6)에 비례해
> 62.5% 감소, ③ 자율주행 컨트롤러의 물리 가정(제동 곡선 감속도 등)이 옛 차량 기준으로 남아
> 있음. 1차로 `MaxBrakeTorque`를 반지름 비율만 적용해 50→105로 올렸는데 부족했음 — 제동
> 토크는 휠당 값이라 총 제동력이 휠 수에도 비례하므로, 반지름 비율×휠 수 비율(2.107×2.667=
> 5.62배)이 맞는 계산이었음. 사용자가 실주행으로 400/600을 찾았고 계산상 등가(281)보다 여유
> 있는 값이라 잘 동작.

총열 회전 제거(개틀링 게이트를 `bUseBarrelSpin` 플래그로 분기해서, 트럭과 컴포넌트를 공유하는
`URCWSFireControlComponent`를 통째로 안 걷어내고 UGV만 안전→사격 즉시 전환되게 함), 과열
연기/아지랑이 이펙트는 유지. 발견된 함정: MCP 배열은 크기와 내용을 동시에 못 바꿈, ABP를
Retarget 복제해도 EventGraph의 캐스트 대상은 원본 클래스 그대로 남아 포탑이 안 돎(옛 핀에
물린 게터를 지우고 재생성해야 함), UGV를 코드에서 찾는 유일한 진입점(`BP_TestPlayerController.
UGVVehicleClass`)을 안 바꾸면 시나리오 자율주행이 통째로 안 붙음.

**BP_UGV_0901 → C++ 이관(`AUGV0901Pawn`)**: 신규 모델 교체 시 남겨뒀던 "궤도 계산 고아 노드
~250개" 정리 과제를 처리하던 중, 실제로는 4개 그래프에 걸쳐 **고아 노드 427개 + 변수 39개**
(추정보다 훨씬 많음 — `UpdateTurretVisuals` 안에만 168개, 살아있는 체인의 중복 복제본이
통째로 남아 있었음)로 드러나 남은 BP 로직 4개(`UpdateTurretVisuals`/`SetManualControl`/
`SetBraking`/Tick)를 전부 새 부모 `AUGV0901Pawn`(`AUGVWheeledVehiclePawn` 서브클래스)으로
내림. 구형 `BP_UGV_Vehicle_new`는 안 건드림(부모에 넣었으면 같은 니아가라 파라미터를 매
프레임 두 번 쓰게 됐을 것이라 서브클래스로 분리).

> **트러블슈팅 - 죽은 노드를 "연결된 노드"로 판정하면 안 됨**
>
> 처음에 이벤트 노드에서 모든 연결(exec+데이터 선 둘 다)을 타고 도달 가능한 노드로 살아있는
> 집합을 구했더니 EventGraph 249개 중 230개가 살아있는 것으로 나왔다(실제로는 4개). 죽은
> 궤도 체인들이 exec 선만 끊겼고 데이터 선은 아직 `EventTick`의 `DeltaSeconds` 출력 핀에
> 물려 있었던 것 — 무향 그래프 도달성으로는 전부 "살아있음"이 됨. 올바른 판정은 2단계:
> ① 엔트리 노드에서 exec 출력 핀만 따라간 도달집합, ② 그 노드들의 입력 핀을 거슬러 올라간
> 생산자 전이 폐포. 이 둘의 합집합만 살아있는 노드.

> **트러블슈팅 - BP의 기본 float은 실제로 double**
>
> 리페어런트 후 ABP 컴파일 에러 — 옛 BP 변수(`TurretYaw`/`GunPitch`)는 double인데 C++를
> float로 잡아서 타입 불일치. UE5에서 블루프린트의 기본 float 타입은 실제로 double이라,
> BP가 핀으로 **직접 읽는** 프로퍼티는 타입이 정확히 맞아야 함(리플렉션으로만 불리는
> `SetManualControl(float,float)`은 float으로 둬도 무방 — `FNumericProperty`가 정밀도를
> 안 따짐). C++를 double로 교체해 해결.

결과: `.uasset` 1,150KB→67KB(-94%), 그래프 노드 472→0, 변수 44→0 — BP는 컴포넌트 22개와
튜닝 값만 든 순수 데이터 에셋이 됨. 삭제 전후 DSL 문자열이 완전히 동일함을 확인해 로직 변경
없음을 검증. 과열 연기 실사격 확인/실주행 확인만 남음.

**변경/신규 파일**: `Vehicles/UGVAIController.h/.cpp`(PI 제어기, 적 거리 속도 제한),
`Vehicles/UGV0901Pawn.h/.cpp`(신규), `/Game/Vehicles/UGV/UGV_0901/`(신규 에셋 일체),
`UI/ScenarioStepTypes.h`, `UI/ScenarioStateSubsystem.h/.cpp`, `DT_ScenarioSteps_ThreeStage`
(`UGVSpeedLimitOn` 행), `kadex_test_Blueprints/BP_TestPlayerController`.

---

## 4. 자체방호축 카메라 떨림 / UGV 발사 셰이크 누출 (버그 2건)

### 목적
자체방호 PC에서 RCWS 조향 시 환경카메라/CCTV가 떨리는 버그와, UGV 발사가 자체방호 3화면
전부를 흔드는 버그를 원인 규명하고 수정한다. 사용자 최초 가설(공유 트랜스폼 리플리케이션
노이즈 전파)은 컴포넌트 계층 확인 결과 반증됨.

### 결과

**두 버그는 같은 지점(`GetLocalViewShakeOffset()`)에서 만난다** — 자체방호 PC의 환경카메라·
CCTV 4분할·RCWS 조준경 캡쳐 전부가 이 함수를 거쳐 "로컬 플레이어 카메라에 걸린 셰이크"를
자기 캡쳐 트랜스폼에 얹는다(2026-08-20 RTSP 화면 흔들림 해결책으로 도입됨).

**버그 1 — 셰이크가 아닌 것을 셰이크로 오인**: `GetLocalViewShakeOffset()`이 "이번 프레임
즉석 계산 트랜스폼"과 "직전 프레임 POV 캐시"의 차이를 셰이크로 역산하고 있었음 — 엔진의
`APlayerCameraManager` POV 캐시 갱신이 모든 액터 틱이 끝난 뒤에 도는데, 씬 캡쳐 코드는 액터
틱 안에서 돌아서 두 값의 샘플 시각이 항상 한 프레임 어긋나는 구조적 문제. 2-PC에서만 보이는
이유: 리플리케이트된 마운트 회전이 클라이언트에서는 패킷 도착 프레임에만 계단식으로 바뀌어
프레임 델타가 크게/0으로 번갈아 실리며 떨림이 되기 때문(호스트는 매 프레임 조금씩 적용돼
매끄러워서 PIE/단일 프로세스로는 재현이 안 됐음). 해결: 카메라 매니저의 모디파이어 체인 맨
앞(`Priority=0`)에 신규 `UViewShakeProbeCameraModifier`를 꽂아 "아직 아무 모디파이어도
안 걸린 POV"를 같은 프레임에 기록, 그 값과 최종 POV의 차이만 사용 — 카메라 자체의 이동/조향은
정확히 상쇄되고 모디파이어(셰이크)분만 남는다.

**버그 2 — UGV 발사가 자체방호 3화면 전부를 흔듦**: 프로젝트 전체에서 카메라를 흔드는 코드는
`ARCWSProjectile::PlayImpactEffect`의 명중 셰이크 하나뿐(전수 확인). `PlayWorldCameraShake`가
"누가 쐈는지"를 전혀 안 보고 월드의 모든 `PlayerController`를 거리만으로 훑어서, 리슨서버
2프로세스에서 두 갈래로 샜다 — (a) 각 프로세스 로컬 경로(자체방호 PC에서도 UGV 착탄이 트럭
반경 35m 안이면 걸림, UGV와 트럭이 같은 전개지에 있어 사실상 항상 걸림), (b) 서버→원격 클라
RPC 경로(서버가 원격 클라의 부정확한 카메라 위치로 거리 판정). 수정: `PlayWorldCameraShake`를
버리고 로컬 플레이어만 훑는 헬퍼로 교체, `CameraManager->GetViewTarget() != ShooterActor`면
스킵 — 발사 주체를 지금 보고 있는 화면에만 셰이크. 의도된 부수효과: 발사 주체를 안 보고 있는
화면은 착탄이 아무리 가까워도 안 흔들림(적 병사가 내 차량 근처를 쏴서 나던 피격 흔들림도
같이 사라짐 — "자체방호축은 UGV 발사에 전혀 영향받지 않아야 한다"는 요구를 만족시키려면
거리 기반 규칙 자체를 버려야 해서 이렇게 됨, 필요하면 피격 대상 게이트를 추가하는 게 다음
단계).

**검증 상태**: 코드 수정만 완료, 빌드/2-PC 실환경 확인 미실시.

**변경 파일**: `Plugins/QuadCamModule/{Public,Private}/SceneCaptureViewParity.{h,cpp}`,
`Vehicles/RCWSProjectile.cpp`.

---

## 5. 적군 AI — 3분대 재편(경로 가중치 근본원인 수정), 전투 로직 수정 3건

### 목적
memo.md 요구사항 — 적 15명을 5명씩 3분대로 나누고 분대별로 다른 경로를 따라가며, 각 분대는
지정된 전투지에서 전멸하고 도주 대상에서 제외된다. 구현 과정에서 "적군이 경로 가중치를 전혀
안 받는다"는 훨씬 근본적인 버그를 발견해 함께 해결했다. 이어서 09-03에 사격 정지/사격선/IK
관련 전투 로직 버그 3건도 수정.

### 결과

**요구사항 재해석 — "전멸"은 트리거가 아니라 결과**: 처음엔 "N분대 전멸"을 새 트리거 타입으로
구현하려 했으나, memo.md를 다시 읽고 이게 트리거가 아니라 **도주를 안 해서 생기는 결과**임을
깨닫고 그 코드를 전부 되돌렸다. 대신 개체별 `LastStandZoneIndex`(이 개체가 마지막까지 버틸
전투지 인덱스) 게이트를 추가 — `BeginFlee(N)`이 `N > LastStandZoneIndex`면 조용히 거절한다.
도주 트리거(`EnemyCasualtyCountAtLeast`)는 그대로 두고 임계값만 kadex_test용(1/3)에서 원안
값(3/7)으로 복원.

**★ 근본 원인 발견 — 적군 경로 가중치는 도입 이래 한 번도 동작한 적이 없었다**: "보병들이
enemypath 가중치를 안 받고 일직선으로 간다"는 리포트를 조사한 결과, 엔진 소스
(`NavigationQueryFilter.cpp`)에서 `Filter.SetAreaCost(id, FMath::Max(1.0f, TravelCostOverride))`
— **필터 오버라이드로는 비용을 올릴 수만 있고 내릴 수 없게 하한이 1.0으로 클램프**되어 있음을
확인. 2026-08-22 도입한 `UNavQueryFilter_Enemy`의 0.2 할인이 조용히 1.0(중립)이 되어 아무
효과가 없었던 것. 왜 여태 안 드러났나 — `UNavQueryFilter_Infantry`의 두 오버라이드는 전부
1.0 이상이라 정상 동작해서 "필터 오버라이드는 잘 먹는다"는 인상만 남았고, 1.0 미만인 값
하나만 죽어 있었음. 수정: 방향을 뒤집어 **"경로를 싸게"에서 "경로 밖을 비싸게"**로 — area는
전부 중립(1.0)으로 두고 필터에서 `NavArea_Default`/`NavArea_Road`를 5.0으로, 다른 분대 경로는
2.0으로 올림(자기·공용 경로 1.0 < 다른 분대 경로 2.0 < 그 외 지형 5.0).

> **트러블슈팅 - 다른 분대 경로도 5.0으로 잡았다가 2·3분대가 가중치를 통째로 잃음**
>
> 세 경로 스플라인이 거의 같은 줄기(2·3분대 경로가 1분대 경로와 87~96% 겹침)라, Recast가
> 폴리곤 하나당 area 하나만 가질 수 있는 구조상 겹치는 구간 전체가 먼저 칠해진 1분대 area로만
> 남았음 — 2·3분대는 그걸 "남의 경로(5.0)"로 봐서 경로 밖과 동일 취급, 1분대만 우회하고
> 2·3분대는 전부 직선으로 갔다. PIE 로그의 평균 area 비용(1분대 4.5~4.67 vs 2·3분대 5.22~5.32)
> 이 그대로 증거였다. "겹치더라도 자기 분대 경로를 정상 가중치로 따라갈 것"이라는 요구를
> 만족시키려면 가운데 단계(다른 분대 경로=2.0)가 필요했다.

두 번째 원인(2026-09-01, 가중치 수정 후에도 15기 중 10기가 경로를 아예 못 받음): `FindPath
ToLocationSynchronously`가 양 끝점을 `DefaultQueryExtent`(수평 50cm)로만 투영하는데, 엄폐
마커가 나무 콜리전 프록시가 만든 내비메시 구멍 가장자리에서 50cm만 벗어나도 질의가 통째로
실패 — 실측 스냅 거리 65~196cm, 전부 초과. UGV의 `MoveToDestination`이 이미 `Destination
ProjectionExtent`로 해결해뒀던 문제를 적군 컴포넌트에도 동일 적용(`NavDestinationProjectionExtent`,
(300,300,400)). 세 번째로, 경로 질의 실패에 대한 재시도가 없어서(정체 재탐색만 있었음)
실패하면 가중치 없는 직선 이동으로 조용히 끝까지 가는 문제도 발견 — 경로 요청 3곳을
`TryBuildNavPath()`로 통일하고 `PathRetryIntervalSeconds`(1초, 실패 반복 시 최대 8초까지
백오프)마다 재요청하도록 수정. 진단 로그(`detour 배율`/`평균 area비용`)를 상시 켜서 "가중치가
안 걸린 것"과 "경로 실패"를 구분 가능하게 함.

**전투 로직 수정 3건(2026-09-03)**: ① Firing 상태에만 시간 제한이 없어 일부 적군이 영구히
멈추던 버그 — 버스트 도중 타겟이 죽어 카운트가 0이 안 되는 경로, 발사 게이트(마커 미도착/
조준 미정렬/사격선 차단)가 지속되는 경로 둘 다 탈출 조건이 없었음. `FiringStuckGraceSeconds`
(3초) 안전망 추가로 다른 세 상태와 구조 통일, PIE 확인 완료. ② 사격선 안전장치의 검사
시작점이 캡슐 중심(`GetActorLocation()`)이었던 것을 실제 총구(`MuzzlePoint`)로 교정 — 바로
앞 오른쪽에 선 적군이 검사에서 빠지는 등 검사선과 실제 탄도가 달랐음. 버스트 도중 재검사
안 함은 이번 범위에서 제외(개선 폭 먼저 확인 후 판단). ③ 왼손 무기 파지 IK를 양쪽 ABP에서
완전 비활성화 — 디자이너가 애니메이션 시퀀스 자체를 수정할 예정이라 IK가 가려주던 원본 포즈
오차가 그대로 드러나는 게 오히려 수정 기준이 됨.

**이동 중 피격 시 몸이 회전하는 현상 — 보류**: 두 가설(총구 Yaw 보정 되먹임 루프 / 소총
Pitch 적분기 폭주)을 세우고 진단 로그를 넣었으나 두 차례 PIE 모두 재현 실패(샘플 7,719건
분석해도 발산/스핀 없음). 특정 조건 조합에서만 나오는 것으로 추정, 코드 변경 없이 로그만
비활성화하고 재발 대기로 보류.

**변경/신규 파일**: `Soldiers/EnemyCombatComponent.h/.cpp`, `Soldiers/NavArea_EnemySquadPath.
h/.cpp`(신규), `Soldiers/NavQueryFilter_EnemySquad.h/.cpp`(신규),
`Soldiers/NavQueryFilter_Enemy.h/.cpp`, `ABP_Ally_kadex2`/`ABP_Enemy_kadex2`(왼손 IK).

---

## 6. 동적 바람 시스템(`AWindSource`) 신규 구현

### 목적
레벨 전역에서 나무/풀/Niagara 이펙트/드론 물리가 하나의 바람 값에 일관되게 반응하도록
`AWindSource` 액터를 신규 구현한다.

### 결과

액터 하나가 Perlin 노이즈로 방향·세기를 천천히 계산해 세 가지 경로로 밀어 넣는 구조 —
① MPC(Material Parameter Collection) 직접 푸시(식생 머티리얼, 전역이라 배치 위치 무관하게
자동 적용), ② 컴포넌트 `"Wind"` 태그 + `User.WindVectorCms`(Niagara 이펙트, 표준 경로),
③ 드론 물리용 정적 조회 함수.

> **트러블슈팅 - RCWS/UGV VFX가 "이미 연동됐다"는 착각으로 몇 턴을 보냄**
>
> RCWS 임팩트류는 원래부터 `WindForce` 모듈이 있고 `Wind Speed`가 `System.WindDirection`에
> 링크돼 있어 연동된 줄 알았으나, 이 변수가 엔진 자동 바인딩이 아니라 완전히 별개의 정적
> 애셋(`NPC_NiagaraExamples`, Epic 예제 콘텐츠)에서 오는 고정값이었음 — 이 NPC를 매 틱
> 갱신하는 코드가 프로젝트에 전혀 없어 사실상 무동작. 자체 태그+`WindVectorCms` 경로로 전부
> 재작업.

식생 튜닝에서 잎만 유독 심하게 떨리는 원인을 머티리얼 함수 그래프 끝까지 추적해 잎 전용
오버라이드 파라미터(`MW_WindInitialIntensity`)를 발견, 4배 감산. `WindForce` 모듈 추가 시
겪은 함정들도 정리 — `System.WindStrength`류는 자동 바인딩이 아니라는 것, `WindForce`가
`ParticleState` 뒤·`SolveForcesAndVelocity` 앞에 있어야 한다는 순서 제약(어기면 컴파일이
깨지거나, 컴파일은 통과하지만 그 프레임 힘이 반영 안 되는 무동작 상태가 됨), 상속 이미터는
부모가 이미 그 순서를 고정해둬서 도구로 우회 불가 — 4개 이미터 전부 상속을 끊고 로컬 모듈로
재배치해 해결.

**남은 것**: 낙엽/먼지 파티클이 배치된 자리 기준 고정 반경에서만 스폰돼 넓은 숲 레벨을 못
덮는 문제 — 카메라(또는 조종 중인 폰) 추적형으로 바꾸는 방향 합의만 됨, 미구현.

**변경 파일**: `Environment/WindSource.h/.cpp`(신규), `Vehicles/RCWSProjectile.cpp`.

---

## 7. 인게임 Settings — Graphics 탭 조사 팔로업 + 문서 정리

### 목적
8/21에 해둔 Graphics 탭 사전조사를, 그사이 있었던 변경(드론 교체·프레임 상한 60 도입·UGV BP
교체·`SceneCaptureViewParity` 재작성)에 맞춰 재검증하고 구현 스코프를 확정한다.

### 결과

`DumpCVars -csv` 전수 덤프로 선행 과제였던 cvar 우선순위를 실측 — **`sg.*` 12개가 전부
`SystemSettingsIni`로 고정돼 있어 `UGameUserSettings` 품질 변경 경로가 완전 no-op**임을 확정
(예상했던 "반만 먹는다"보다 나쁜 결과). 우회하려면 `Scalability::SetQualityLevels(bForce=true)`
필요. 정정 4건 중 가장 큰 것: 프레임 상한(`t.MaxFPS`)은 스케일러빌리티 그룹과 달리
`UEngine::SetMaxFPS`가 cvar의 기존 SetBy 이유를 승계해서 GameUserSettings 경로가 실제로
이긴다 — 다만 계획은 오히려 강화됐다: **`t.MaxFPS=60`은 화질 옵션이 아니라 물리 결정성
대책**(`bTickPhysicsAsync=false`라 물리 dt가 프레임 dt를 그대로 쓰고, 고사양 PC에서 UGV가
속도를 못 이기고 장애물에 부딪히던 증상의 원인이었음)이라 **탭에서 60 위로 못 올리게 해야
한다**는 결론. 창 모드는 실제 운용이 런치 인자(`-fullscreen`)라 후순위로 하향.

**확정 스코프**: 품질 프리셋(그림자·GI·반사 최우선) + 카메라 캡쳐 주기(CCTV/드론/전장을
따로 — 셋의 라운드로빈 메커니즘이 다르고 슬롯 엇갈림이 설계의 일부라 묶으면 깨짐) + AA 방식 +
VSync. 제외: 프레임 상한, 창 모드, 레벨 PPV. 보류: 렌더 스케일·캡쳐 해상도(축/RTSP와 얽힘).
설계는 신규 `UTitanGraphicsSettings`(`UDeveloperSettings`) 단일 소스로 확정, 구현은 사용자
검토 대기.

**문서 관리 체계 개편(2026-08-31)**: 여러 세션이 독립적으로 문서를 만들며 폴더가 뒤섞이는
문제를 해결하기 위해 `CLAUDE.md`(문서 작성 규칙 — 날짜 접두 파일명, 1줄 헤더, 시스템당
폴더 하나)를 도입하고, `CURRENT_STATE.md`(현재 상태 스냅샷)/`DOCS_INDEX.md`(전체 카탈로그)/
`WORKLOG.md`(시간순 서사) 3종 신규 관리 문서를 만들었다. `guide/` 폴더를 신설해 레퍼런스성
dev guide 9개를 이동(전부 "최신화 필요" 경고 배너 부착 — 내용 자체는 아직 옛 것), 레거시/
중복 문서 16개를 `_archive/`로 이동, 중복 코드베이스(`structure/rc_gui/`)와 `README_save.md`
삭제. 이후 신규 문서는 이 규칙을 따라 작성 중.

---

## 기타 메모

- 이번 주에도 "블루프린트가 부모/원본 참조를 그림자화하거나 캐스트가 안 갈아끼워지는" 계열
  함정이 여러 건 반복됨(ABP Retarget 후 EventGraph 캐스트 미변경, MCP `set_parent`가
  dirty를 안 세워 저장 누락) — 새 BP 계보를 만들 때는 캐스트/저장 여부를 디스크 바이너리로
  재확인하는 습관이 굳어지는 중.
- `WORKLOG.md`/`CURRENT_STATE.md`/`DOCS_INDEX.md` 3종 문서가 이제 titan 폴더 전체의
  1차 참고 지점 — 다음 주부터는 이 문서들을 기준으로 주간 보고를 작성하면 더 빠르게 정리 가능.
