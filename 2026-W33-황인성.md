# 작업 내역

> 기간: 2026-08-07 ~ 2026-08-13
> 담당: 황인성

---

## 1. BP_UGV_Vehicle → Genesis 물리 엔진 전환

### 목적
`titan_example_genesis`(P4V 원본과 분리된 별도 워킹 카피)에서, 기존 Chaos 차량 물리 기반
`BP_UGV_Vehicle`은 그대로 두고 별도 복사본 `BP_UGV_Vehicle_Genesis`를 외부 Python 물리
서버(Genesis)로 구동되게 전환한다. 최종 목표는 기존 `AUGVAIController`의 C++ pure-pursuit
자율주행을 걷어내고, NavMesh 경로를 Genesis 서버가 직접 실시간 추종(PathFollowing)하도록
바꾸는 것. 테스트는 격리된 레벨 `kadex_test_genesis`에서만 진행해 운영 시나리오/레벨에는
영향 없음.

### 결과

**전체 구조**: `Plugins/GenesisOSCBridge`(UE↔Genesis OSC 브릿지) — `AGenesisWheeledVehiclePawn`
(`AWheeledVehiclePawn` 상속) + `UGenesisWheeledVehicleMovementComponent`
(`UChaosWheeledVehicleMovementComponent` 상속이라 기존 `SetThrottleInput`/`GetWheelState` 등
Chaos 호환 BP 노드를 그대로 재사용 가능). 별도 Python `physics_server` 프로세스가
`genesis-world` SDK + URDF로 차량을 재구성해 시뮬레이션하고 OSC로 상태/입력을 주고받음 —
**Genesis는 UE 메시나 PhysicsAsset을 전혀 모름**, 질량/충돌형상은 `ugv.urdf` 하나가 유일한
소스.

**무게중심(CoM) 버그 — 핵심 사고 사례**: 수동 조향으로 가속하다 특정 속도에서 차량이 뭔가에
부딪힌 것처럼 붕 뜨며 튕겨나가는 증상. 기어비 배열 오류(8개→정상 5개), 로그 grep 검색어 자체에
`gear=-1`이 포함돼 "후진 기어만 나온다"고 오판, 두 번째 UGV 인스턴스 중복 의심 등 여러 잘못된
원인 추정을 거쳤으나 전부 아니었음(라이브 PIE 액터 트랜스폼 직접 조회로 반증). **실제 원인
(사용자가 직접 찾음)**: 섀시 collision box가 포탑 주포까지 포함해 X: -1.81~+3.03로 잡혀
있어서 실제 바퀴 범위보다 훨씬 앞으로 튀어나온 형상 때문에 무게중심이 앞으로 쏠린 것처럼
계산되고 있었음. 바퀴 위치 기준 바운딩 박스 + 여유 0.2m로 재설정해서 완전 해결. 교훈: Genesis
충돌 형상은 비주얼 메시와 무관하게 URDF가 전부라, "비주얼 메시 기준으로 이 정도면 맞겠지"가
아니라 실제 바퀴 배치 기준으로 역산해야 함.

**그 외 URDF/설정 버그**: 롤링 저항 누락으로 브레이크 없이 계속 굴러가던 문제
(`rollingResistance=0.02` 추가), A/D 조향이 반대로 걸리던 문제(후진 시 조향 반전 로직 추가),
PIE 반복 재시작 시 차체가 붕 뜬 채 조종 불가(근본 원인 미해결, 에디터 재시작으로 회피),
URDF 주석 안 리터럴 더블하이픈이 `ParseError`를 일으키는 문제(총 3번 반복 발생).

**교수님 URDF 리뷰 피드백(2026-08-11) 확인**: `tank_skid_belt` 프리셋이 40톤급 탱크 기준
상수라 4.3톤 UGV엔 안 맞는 항목 점검. ① **`TopSpeed` 미설정 — 적용 완료**:
`bUseTopSpeedOverride=false`라 탱크 기본 최고속(67km/h)이 그대로 걸려있었고 Sweep Table은
40km/h로 측정해둔 상태라 불일치했던 것을 확인, `bUseTopSpeedOverride=true`+
`TopSpeed=11.111 m/s`(40km/h)로 `GenesisOSCBridge_0.VehicleSetup`에 반영,
`kadex_test_genesis` 레벨 저장까지 완료(W32엔 에디터 연결 문제로 미적용이었던 항목).
② 발진 시 휠스핀 — 접지 마찰 한계의 약 2배인 프리셋 토크값이지만 마찰원에서 잘려 폭주는
없음, 정보성으로 보류. ③ 섀시 visual 박스가 옛 바운딩 박스로 남아있는 것 — collision과
달리 visual만 안 갱신됐고 물리엔 영향 없는 디버그 렌더링 문제, 급하지 않음. ④ 서스펜션 —
강성/감쇠비/승차 주파수 전부 정상 범위 재확인, 조정 불필요.

**메인 작업 — NavMesh 경로 → Genesis PathFollowing 파이프라인**: `AUGVAIController::
MoveToDestination`이 NavMesh 코너 포인트(성긴 배열)를 구하면, 플러그인 내장
`BP_WaypointPath`로 Coarse→Dense 리샘플링(Catmull-Rom 스플라인 보간, Divisions=8) 후
`SetTargetPathFromWaypointArray`로 Genesis 서버에 전달, 서버가 매 프레임 Sweep Table
캘리브레이션 기반 lookahead로 좌우 트랙 속도차(S/T)를 직접 계산.

Dense 리샘플링이 필요했던 이유: 처음엔 NavMesh 코너점을 리샘플링 없이 그대로 넘겼더니 코너에서
장애물에 계속 부딪혔음 — Lookahead(350cm) 추종 알고리즘이 중간 안내점 없이 코너를 크게
잘라 들어간 것. 플러그인 원 설계(`GENESIS_UE_USER_GUIDE.md` 확인)가 "Coarse→스플라인→Dense"
파이프라인을 전제하고 있었던 것을 발견, `BP_WaypointPath.GetWaypoints()`를 그대로 재사용해
해결. 구현은 `BeginPlay`에서 임시로 `BP_WaypointPath`를 스폰→세팅→결과 추출→즉시
`DestroyActor`로 정리(레벨에 시각적 잔여물 없음).

NavMesh 에이전트 반경 확인: 레벨에 사람 크기(radius 30)와 탱크용(radius 200) NavMesh 두 개가
있어 잘못된 걸 쓰는지 의심했으나, UE 5.8 엔진 소스 확인 결과 `APawn::GetNavAgentPropertiesRef()`가
"먼저 등록된 컴포넌트"(베이스 클래스의 네이티브 `VehicleMovementComp`, `BP_UGV_Vehicle`에서
물려받은 탱크용 `NavAgentProps` 보유)를 그대로 쓰고 있어 처음부터 정상 동작 중이었음을 확인.

**PathFollowing 튜닝**: 자율주행 중 제자리에서 빙글빙글 도는 문제로 `SkidSteerGain`
1.0→0.45, `YawGain` 1.5→0.6로 1차 완화. 그래도 남아있던 "꼬리치는"(회전 시 뒷부분이
미끄러지며 좌우로 흔들리는) 현상은 `WheelOverrides`에 횡방향 마찰(`MuLat`)이 전혀
오버라이드 안 돼 있던 것이 원인 — `MuLat=2.2`(전역 기본 2.0보다 살짝 높게) 추가해 완전히
해결. 부작용으로 회전이 둔해져 목표 지점 근처에서 overshoot 발생.

**게인 재상향(이번 주 진행)**: MuLat이 횡방향 미끄러짐을 물리적으로 억제해주고 있어
게인을 다시 올려도 예전처럼 쉽게 진동하지 않을 거라는 판단으로, overshoot 대응을 위해
`SkidSteerGain` 0.45→0.65, `YawGain` 0.6→1.0, `ApproachGain`(도착 직전 구간 전용 반응성)
1.0→1.8, `SteerCap`(도착 직전 급조향이 캡에 안 잘리도록) 0.5→0.65, `ArrivalGoalCm`(도착
판정 범위) 150→500으로 함께 완만하게 재상향 — **PIE 재검증 대기 중**(overshoot 개선 및
꼬리치기 재발 여부 확인 필요). `MuLat` 변경으로 캘리브레이션이 stale해진 상태라 Sweep
Table(`path_profiles/ugv.csv`)도 동일 조건(`--top-speed 11.111`)으로 재측정 완료 —
`CalibrationProfileId="ugv"`가 그대로 이 파일을 참조하므로 별도 설정 변경 불필요.

**자율주행 트리거를 `EventBeginPlay` 자동 실행 → `I`/`O` 키 입력으로 전환**: 처음엔
`BP_UGV_Vehicle_Genesis` 자체에 키 입력 노드를 붙였는데 전혀 반응 안 함 — 원인은
**이 Pawn이 플레이어가 직접 Possess하는 대상이 아니기 때문**(`BP_TestGameMode_Genesis`의
`DefaultPawnClass`는 `BP_Ally_kadex`이고, UGV는 `Atitan_examplePlayerController`가
Enhanced Input으로 WASD를 받아 `AUGVAIController::DispatchSetManualControl()`로 원격
호출하는 기존 RCWS 패턴 — 언리얼은 Possess된 Pawn+Controller에만 입력을 라우팅하므로
UGV Pawn 자체에 입력 노드를 붙여도 절대 안 옴). 해결: `BP_UGV_Vehicle_Genesis`에 Custom
Event(`StartAutoDrive`/`StopAutoDrive`)를 만들어 기존 "Genesis 상태 대기 →
MoveToDestination → 경로 리샘플링 → PathFollowing 시작" 체인을 그대로 연결하고,
`BP_TestPlayerController_Genesis`에서 `I`/`O` 키를 받아 `GetActorOfClass`로 UGV 인스턴스를
찾아 이 Custom Event를 호출하는 구조로 전환. `O`(자율주행 중지→수동 복귀)는 최초엔
`StopTargetPathFollowing`만 호출했는데, `I` 트리거 시 이미 `SetDriveMode(Idle)`로 바뀐
상태가 남아있어 PathFollowing만 꺼지고 차량은 풀브레이킹 상태에 머무는 문제가 있었음 —
`SetDriveMode(Manual)`을 먼저 실행한 뒤 `StopTargetPathFollowing`을 호출하는 순서로 수정해
WASD 수동 조향이 바로 복귀되도록 함. 목적지는 여전히 하드코딩(`ugv_move_point`,
6460,830,0) — 콘솔 커맨드화는 아직 미착수.

**알려진 이슈 / 다음 단계**: 재상향한 게인 5종의 PIE 재검증(최우선), `Divisions=8` 튜닝
여지(급코너 구간 스플라인 부풀림 가능성, 실측 문제는 아직 없음), 에디터 장시간 유지 시
물리 꺼짐 근본 원인 미확인, 자율주행 목적지의 콘솔커맨드화(`MoveUGVGenesisTo` 등) 미착수,
발진 휠스핀/URDF visual 박스는 낮은 우선순위로 보류.

---

## 2. 피격 이펙트 시스템 — 후속 작업 (도탄/VFX/사운드/차량 재질 판정)

### 목적
`hit_effects_idea.md`(초기 기획)/`hit_effects_implementation.md`(1차 구현) 이후 진행분 —
도탄(리코셰) 시스템, 혈흔/화염 VFX, 사운드 연결 및 어테뉴에이션, 연사 중 오디오 채널 고갈
버그, 총알 휘바람 소리, UGV/TitanTruck 차량 피격 재질 판정을 정리한다.

### 결과

**도탄(Ricochet) 시스템**: 반사속도 = 입사속도 × 그레이징팩터(각도) × 재질 흡수율 × 난수
(0.3~1.0)로 계산, 기준속도의 일정 비율 이상이고 반사 횟수 제한 이내면 표준 반사 공식으로
방향 계산. 재질별 확률을 따로 두지 않고 반사속도 계산 자체에 난수를 곱해 매번 다른 결과가
나오게 하는 방식을 채택(최초 설계보다 단순화). 이펙트 크기는 속도 손실 비율에 비례해 스케일.
`MaxRicochetReferenceSpeedCmS`는 RCWS는 실제 탄속 850m/s를 씬 스케일 보정해 역산했으나,
**Rifle은 실제 탄속을 코드 어디서도 못 찾아 미검증 placeholder로 남김**(소총 발사가 전부
블루프린트 이벤트 기반이라 값 확인 불가, 추후 재조정 필요). 멀티플레이 설계: RNG는 서버
권위 분기 안에서만 실행, 도탄 자식 투사체는 풀에 안 넣고 독립 스폰(기존 "위치기반 멀티캐스트가
풀 인덱스 동기화보다 견고" 철학 유지), 아군/적군은 발사가 블루프린트 이벤트 기반이라
투사체 클래스를 파라미터로 명시 전달.

**VFX 추가**: 혈흔(`NS_Blood_Splat`/`NS_Blood_Splat_RCWS`, 라이플/RCWS 세기 구분) 신규 제작.
나무 화염(`NS_RCWS_Wood`)에서 머티리얼 버그 발견 — 처음 쓴 `MI_FireRoil_8x8`이 이름과 달리
스모크용 마스터 머티리얼이라 색만 바꿔선 불처럼 안 보였음, 셰이더 그래프 추적 끝에 실제 불
이펙트가 쓰는 `M_Fire_01`+`T_Flame_01`로 교체해 해결. 연사 시 렉 방지로 5개 재질 파티클
시스템 공통 개수 축소.

**SFX 연결 및 어테뉴에이션 재조정**: 재질별/적 피격/도탄 MetaSound를 이름 매칭으로 연결.
기존 어테뉴에이션 값이 심하게 어긋나 있던 걸 발견 — RCWS는 1.6m 밖이면 안 들리던 걸
약 265m까지("폭발음답게")로, Rifle은 반대로 1.5km까지 들리던 과도한 값을 약 33m
("소화기답게")로 재조정.

**트러블슈팅 - 연사 중 UGV 엔진/UAV 프로펠러 루프 소리가 끊김**: RCWS 최대 1200rpm 연사 시
발사음이 concurrency 제한 없이 `Priority=1`(기본값, 엔진/드론 루프와 동률)로 순식간에 전역
채널을 채우면서 voice-stealing으로 엔진/프로펠러 루프가 정지되는 문제. ① 에셋 레벨(리빌드
불필요): 두 MetaSound의 `Priority`를 1→90, `bBypassVolumeScaleForPriority=true`로 설정.
② C++ 셀프힐(리빌드 필요): 절대 끊기면 안 되는 지속 루프이므로, 매 틱 `IsPlaying()` 체크해서
꺼져있으면 즉시 `Play()`로 재시작(`VehicleEngineAudioComponent::TickComponent`,
`AUAVPawn::UpdatePropellerAudio`). 나머지 사운드(발사/피격/도탄/발소리)는 "절대 끊기면 안
되는" 성격이 아니라 이번엔 손 안 댐.

**총알 휘바람(Whiz-by) 소리**: 언리얼이 fire-and-forget 사운드엔 도플러를 자동 적용 안 해서,
실제 슈팅 게임(Squad/Insurgency류)과 동일하게 **근접 통과 감지** 방식으로 구현 — 매 틱
투사체의 "직전→현재 위치" 선분과 로컬 플레이어 카메라 사이 최근접 거리를 계산, 2단계 컷
(넓은 반경 먼저 싼 거리제곱 비교로 스킵 → 통과 시에만 정밀 최근접점 계산)으로 성능 확보.
로컬 전용(리플리케이션 없음) — AI/서버는 들을 필요 없고 데디케이트 서버는 로컬 플레이어가
없어 루프가 자동으로 비어 공짜로 스킵됨. 남은 작업: 실제 녹음된 휘바람/크랙 SFX 에셋이 아직
없어 사운드 미연결, 빌드 후 감지 반경/사운드 튜닝 필요.

**차량 피격 재질 판정 완료**:
- TitanTruck: 피직스 매터리얼을 스킨 머티리얼에 채워도 총알이 완전히 무시하고 통과하던
  버그. `BodyMesh`+RCWS 터렛 부품 4종의 콜리전 프리셋이 Datasmith 임포트 기본값으로 추정되는
  `BlockAllDynamic`(`WorldDynamic` 채널)이었는데, 투사체 콜리전 컴포넌트가 `WorldDynamic`을
  명시적으로 Ignore하고 있어서 애초에 부딪히지도 않았던 것. `BP_UGV_Vehicle`은 원래부터
  `Vehicle` 프리셋이라 정상 작동 중이었음 — 트럭만 프로젝트 컨벤션에서 벗어나 있었던 것.
  5개 컴포넌트 프리셋을 `Vehicle`로 통일해 해결(스태틱 메시라 Complex 트레이스로 재질별
  판정 자동 해결, 콜리전 도형 자체는 손댈 필요 없었음).
- BP_UGV_Vehicle(스켈레탈 메시): 기본적으로 삼각형 단위 Complex 콜리전이 없어 재질 판정
  해상도가 피직스 에셋 바디 개수(차체 박스 1개+총열 캡슐 1개뿐)로 제한되던 문제.
  `bEnablePerPolyCollision`은 검토 후 기각 — UE 5.8 엔진 소스(`SkeletalMeshComponentPhysics.cpp`)
  확인 결과 이 옵션이 `InitArticulated`(피직스 에셋 기반 바디 생성, Chaos Vehicle 서스펜션/
  차체 시뮬레이션이 의존)를 통째로 대체해버려서 켰으면 차량 물리가 깨졌을 것. 대신 피직스
  에셋에 재질 영역별 바디를 추가하되 `Kinematic`+`Query Only`+`Use Simple Collision As
  Complex`로 완전히 수동적(passive)이게 만들어 기존 시뮬레이션 바디와 안 부딪히게 처리,
  바디별 `Phys Material Override`로 재질 지정. Query Only라 헐 개수가 많아도 매 틱 비용 없음.
  실제 바디 배치는 사용자 직접 작업.

**남은 것**: 유리(Glass) 파손 연출 방향(스프라이트 위장 vs Chaos Destruction) 미결정 —
현재 유일하게 남은 항목.

**변경 파일**: `RCWSProjectile.h/.cpp`, `RCWSFireControlComponent.h/.cpp`,
`AllyFormationComponent.h/.cpp`, `EnemyCombatComponent.h/.cpp`,
`Vehicles/VehicleEngineAudioComponent.cpp`, `Vehicles/UAVPawn.cpp`,
신규 `/Game/VFX/Blood/NS_Blood_Splat`(+`_RCWS`), `NS_RCWS_Wood`.

---

## 3. 구조 개편 현황 — 리플리케이션 실기 테스트 통과, RTSP 완료 (스냅샷)

### 목적
`replication_audit.md`(W32 감사)를 기준으로 진행한 리플리케이션 구현이 이번 주 실기(2-PC
성격) 테스트까지 통과했고, RTSP 송출이 완료됐다. 현재까지의 구조 개편 전체 상태를 한 번에
점검하기 위한 스냅샷 기록.

### 결과

**한눈에 보기**

| 영역 | 상태 |
|---|---|
| RTSP 송출 | **완료** |
| 리플리케이션 — 기반 플러밍 | **완료** |
| 리플리케이션 — RCWS | **완료** (투사체 판정 서버 권위화까지 8/13 마무리) |
| 리플리케이션 — 시나리오 | **완료** |
| 리플리케이션 — 아군/적군 전투 | **완료 + 실기 테스트 통과** (8/11, 버그 3개 발견·수정) |
| 리플리케이션 — UGV/차량 | 미착수 |
| 리플리케이션 — UAV | 미착수 |
| UGV축 LIG 프로토콜 | 미착수 |
| 자체방호축 단일화 | 미착수 |
| LIG 참조구현(`udp_test`) 검토 | 완료 |
| 좌표계(WGS84 vs UTM) | 이중 지원 설계로 헤지 |
| NATS | **폐기 결정**, UDP로 전면 통일 |

**RTSP 송출 완료**: W32에 진행 중이던 UE NVENC(zero-copy) + GStreamer `gst-rtsp-server`
구현이 완료됨 — 5스트림 검증 완료(`rtsp_viewer_test/`).

**리플리케이션 — 8/6 감사 → 구현 → 8/11 실기 테스트 → 8/13 RCWS 투사체 판정 마무리**:
- 기반 플러밍: `ATitanTruck::BeginPlay` 하드코딩 possess 제거, `AUGVAIController::
  IsLocalController()`를 `NM_Standalone`(항상 true)/그 외(`HasAuthority()` 기준)로 네트워크
  모드 분기, 접속 옵션(`?Axis=UGV`/`?Axis=SelfDefense`) 기반 축 판별, 커스텀 GameState 도입.
- RCWS: 상태(`CurrentMode`/탄약/줌/마운트 회전)를 `Replicated`+`OnRep`로, 조작 트리거를
  `Server_*` RPC로 전환. **투사체 판정(8/13 확정)**: 히트스캔 전환도 액터 리플리케이션도
  둘 다 기각하고, 궤적은 각 프로세스가 결정론적으로 로컬 재생하되 **명중 판정만 서버가
  확정해서 위치 기반 `Multicast_PlayImpactEffect`로 이펙트/사운드/데미지를 전파**하는
  방식으로 확정(풀 인덱스 동기화 불필요, 패킷 드롭에도 누적 어긋남 없음). RCWS pan/tilt
  클라이언트 예측은 보류하되, 이건 정확성 문제가 아니라 순수 체감지연 문제임을 확인
  (발사 방향은 서버가 자기 시점 각도로 한 번만 계산해 뿌리므로 각도 divergence로 인한
  발사 오차 시나리오 자체가 없음).
- 시나리오: GameState에 거친 단계(`EScenarioPhase`) 동기화, 트리거 Exec 커맨드 전부
  `Server_*` RPC화.
- **아군/적군 전투(가장 회귀 위험 크다고 봤던 항목) — 실기 테스트로 발견해 고친 버그 3개**:
  ① 아군 `CurrentEnemy`/`HasTarget`가 클라이언트에서 안 잡히던 문제 — 원인이 C++ 컴포넌트가
  아니라 **캐릭터 블루프린트 자체**의 오버랩 감지/`EventTick`이 게이팅 없이 로컬 실행되던
  것(감사 당시 우려했던 "C++만 봐선 못 보는 Blueprint 사각지대"가 실제로 나온 사례) —
  `Switch Has Authority`로 서버 전용화해서 해결. ② 적군 낙하산이 클라이언트에서 안 사라지던
  문제 — `IsParachuting`을 `RepNotify`로 전환해 해결. ③ 발사 트리거를 `NetMulticast`로
  감싸 총알/이펙트/사운드 클라이언트 반영(이전 발견). 교훈: 앞으로 캐릭터 블루프린트 수정
  시 "C++ 컴포넌트 감사만으론 못 보는 로직이 있을 수 있다"는 전제로 접근할 것.
- 테스트 인프라: `kadex_test`(단독 빠른 테스트, 축선택 없이 바로 UGV/호스트 진입)/
  `kadex_lobby`(Host/Client 선택 화면 필요) 레벨 분리 완료, 아군1 vs 적군1 최소 교전 세팅
  구성.
- **아직 안 된 것**: UGV/차량(`UpdateChaosPursuit` 등 매틱 로직 `HasAuthority()` 게이팅,
  `DriveMode`/기어 리플리케이트, `physicsReplicationMode` 검토 — UGV PC가 항상 서버라 당장
  체감 버그는 없지만 설계상 구멍으로 남음), UAV 리플리케이션 편입 자체가 미착수.

**전송 계층 재결정**: NATS 폐기, UDP로 전면 통일(변경 없음, 이전 결정 유지).

**다음 단계**: ① UGV/차량 리플리케이션 마무리(남은 리플리케이션 중 유일하게 손 안 댄
영역), ② UAV 리플리케이션 편입, ③ 위 둘이 상대적으로 작은 범위라 UGV축 LIG 프로토콜 +
자체방호축 통합 착수를 병행 저울질할 시점

---

