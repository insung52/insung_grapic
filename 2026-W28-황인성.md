# 작업 내역

> 기간: 2026-07-03 ~ 2026-07-09
> 담당: 황인성

---

## 1. grapi-base 정적분석 멀티플랫폼 재검증 마무리

[260706_황인성_clangtidy_cppcheck_verification_report](2026-W27-황인성/260706_황인성_clangtidy_cppcheck_verification_report.md)

### 목적
지난주 Windows(MSVC) 기준 Clang-Tidy/Cppcheck 검증 완료에 이어, Linux/WebGL/Android/
Telechips/Renesas 등 전 플랫폼으로 스캔 범위를 확장해 플랫폼 종속 버그 및 경고를
추가 발견/수정하고 전 플랫폼 기준 base/ 경고 0건을 달성한다.

### 진행 계획
1. Linux/WebGL(LLVM 21), Android arm64/arm/x86/x64(NDK LLVM 19.0.1), Telechips/Renesas(GCC) 환경에서 재스캔
2. 플랫폼 전용 신규 경고 분류 및 실버그 확인/수정
3. 오탐 확인 및 억제 주석 추가로 잔여 경고 정리

### 결과

**신규 버그 발견 및 수정 (A-18 ~ A-21)**

| 항목 | 위치 | 내용 | 처리 |
|---|---|---|---|
| A-18 | `asset_loader.cc` | `KHR_materials_volume` 익스텐션이 조건 확인 없이 항상 등록됨 | 수정 완료 |
| A-19 | `scene_component.h` | `TemporalAntiAliasingOptions::upscaling`이 bool/float 혼용 오설계 | 수정 완료 |
| A-20 | `geometry_component.cc` (`updateGeometryLods`) | 잠재 크래시 경로 | 수정 완료 |
| A-21 | - | 미구현 기능 관련 이슈 | 미구현 상태라 조치 보류 |

**신규 대량 카테고리**

- D-29 (673건): LLVM 21 전용 `misc-const-correctness` pointee-const 경고. `-fix` 자동 수정 적용 후 44곳에서 downstream non-const 요구 충돌로 컴파일 에러 발생 -> 해당 44곳은 개별 되돌리고 NOLINT 처리, 나머지 적용 완료
- D-30: Telechips Cppcheck에서 기존 B/D 항목과 같은 패턴으로 재발견된 소량 카테고리, 분류 완료

**신규 오탐 확인 (F-10 ~ F-12)**

- F-10: `autoVariables` - `std::array` 값 복사를 주소 저장으로 오인하는 Cppcheck 오탐
- F-11: `bugprone-unchecked-optional-access` - 17건, 사전 가드로 이미 안전한데 clang-tidy가 함수 호출/반복 역참조 경계를 못 넘어 생기는 오탐
- F-12: `#ifdef USE_JOLT` 조건부 컴파일로 인한 다수 체크 오탐

**스캔 환경 이슈 추가 발견**

- VS 2026 Insiders가 `compile_commands.json`에 VS 2026 STL 헤더 경로를 포함시키고, VS 2022 번들 clang-tidy가 `__builtin_is_implicit_lifetime`(Clang 20+ 전용)을 인식 못해 파일마다 `clang-diagnostic-error` 2건이 발생하는 환경 노이즈 확인. VS 2026 clang-tidy로 스캔 시 해소됨.

**최종 결과**

| 플랫폼 | 도구 | 결과 |
|---|---|---|
| Windows (MSVC/LLVM 19) | Clang-Tidy + Cppcheck | base/ 경고 0건 |
| Linux / WebGL (LLVM 21) | Clang-Tidy | base/ 경고 0건 |
| Android arm64/arm/x86/x64 (NDK LLVM 19.0.1) | Clang-Tidy | base/ 경고 0건 |
| Telechips / Renesas (GCC) | Cppcheck | base/ 경고 0건 |

서드파티 및 정보성 메시지만 잔존. 총 20여 회 스캔 라운드(v1~v14 Windows 단일 플랫폼 + 멀티플랫폼 재검증)를 거쳐 전 플랫폼 기준 완료.

---

## 2. Kadex 언리얼 레벨 구성 - UGV 주행 / 조이스틱 카메라 / 객체 탐지

[UGV 자동/수동 주행 로직](2026-W28-황인성/ugv_driving_dev_guide.md)

[조이스틱 연동](2026-W28-황인성/joystick_camera_control_dev_guide.md)

[객체 탐지 알고리즘](2026-W28-황인성/detection_dev_guide.md)

### 목적
`titan_example` 프로젝트의 `kadex_demo` 레벨에 UGV(무인 지상 탱크) 수동/자동 주행,
조이스틱 기반 RCWS/UAV 카메라 pan/tilt 조작, RCWS/UAV 카메라 화면의 객체 탐지
바운딩 박스 표시 기능을 구현한다.

### 진행 계획
1. UGV 수동/자동 주행 물리 모델 구현 및 AI 경로 추종 알고리즘 개발
2. 조이스틱 하드웨어 인식 문제 해결 후 카메라 제어 연동
3. 기하학적 투영 + 가시선 판정 기반 객체 탐지 바운딩 박스 구현

### 결과

#### 1) UGV 수동/자동 주행

**파일 구조**

```
titan_example/Source/titan_example/Vehicles/
  UGVPawn.h/.cpp
  UGVMovementComponent.h/.cpp
  UGVAIController.h/.cpp
  UGVPathCorridor.h/.cpp        (오프 경로 감속용 스플라인 액터)
```

**물리 모델 - 두 단계 재작성**

초기에는 `CurrentSpeed`(스칼라) + `CurrentAngularSpeed`(스칼라)만 상태로 저장하고
매 틱 heading 기준으로 이동량을 재계산하는 kinematic 모델로 구현했다. 감속 중 회전해도
드리프트가 없고 수동/자동이 같은 물리를 공유하는 구조였으나, 경사/충돌 대응이 없어
전면 재작성했다.

최종 채택 방식은 `BodyMesh`에 리지드바디 시뮬레이션을 활성화하고 **가상 트랙 두 지점에
힘을 직접 주입하는 방식**이다. 수동/자동 모두 `ThrottleInput`/`SteerInput`(-1..1)
두 값만 공유하고, `TickComponent`는 이 값만 보고 힘 계산을 수행한다.

```
BodyMesh->SetSimulatePhysics(true)
BodyMesh->SetMassOverrideInKg(15000kg)
BodyMesh->BodyInstance.COMNudge = (0,0,-100)   // 무게중심 낮춰 전복 방지
BodyMesh->BodyInstance.AngularDamping = 3.0
```

- 접지 검사: 좌/우 트랙 각 전/후 2점씩 총 4점 라인트레이스. 미접지 트랙에는 힘을 안 줌 -> 이게 없으면 슬로프 경계에서 공중제비 버그 발생했었음
- 스킷 스티어 모델: `LeftThrottle = Throttle - Steer`, `RightThrottle = Throttle + Steer`. `Throttle=0, Steer!=0`이면 좌우 반대 힘으로 제자리 회전
- 횡방향 그립: 속도를 전진/횡방향으로 분해해 횡방향 성분만 감쇠하는 힘 추가. 엔진 마찰값과 독립적으로 작동해 미끄러짐 방지
- 최고속도: 하드 캡 제거, `-velocity_normalized * speed^2 * AirResistanceCoefficient` 형태의 항력으로 자연스러운 종단속도 구현. 오르막/내리막에서 중력이 힘 균형에 더해져 경사 차이가 실제로 느껴짐

> **트러블슈팅 - 마찰계수 버그**
> `EngineForceMagnitude`를 트랙당 1000만까지 줘야 겨우 움직이는 현상 발생. 원인은
> UGV나 지형 모두 `PhysicalMaterial`을 지정한 적이 없어 언리얼 기본값(마찰 0.7,
> 반발계수 0.3)이 적용되고 있었던 것. `AddForceAtLocation`으로 직접 힘을 주입하는
> 방식에서 마찰이 순전히 저항으로만 작용해 엔진힘 대부분이 정지마찰을 겨우 이기는
> 데 소모됐다. `BeginPlay`에서 런타임 전용 `PhysicalMaterial`(마찰 0.15, 반발계수
> 0)을 생성해 `SetPhysMaterialOverride`로 적용하고 `EngineForceMagnitude`를 200만,
> `BrakeForceMagnitude`를 400만으로 재조정.

**AI 추종 - Pure Pursuit Lookahead**

초기에는 `PathFollowingComponent`가 매 틱 주는 `MoveVelocity` 방향을 그대로 사용했는데,
경로점 도달 순간 목표가 한 프레임 만에 통째로 전환되며 조향이 출렁이는 문제가 있었다.

Pure Pursuit 방식으로 교체: `PathFollowingComponent::GetPath()`로 경로점 배열을 얻어,
탱크 현재 위치에서 경로를 따라 `LookaheadDistance`(기본 800cm)만큼 앞선 보간 지점을
실시간 계산해 그 지점을 조준한다. 목표가 실제 경로점이 아니라 경로선 위의 임의
보간점이라 코너 도달 전부터 서서히 목표가 이동해 이산적 전환이 없어진다.

도착 판정: 목적지에서 `DestinationAcceptanceRadius`(기본 150cm) 안에 들어오면
탱크 현재 위치 자체를 목표로 반환해 방향이 자연스럽게 0이 되어 정지.

**NavMesh 도로 선호 시스템**

Landscape 스플라인 도로 148개 세그먼트를 따라 중심 스트립만 저비용 영역(`UNavArea_Road`,
`DefaultCost = 0.2`)으로 설정해 경로탐색이 도로를 선호하게 했다.

도로 스태틱메시 전체 폭에 NavCollision을 거는 방식은 경로가 가장자리를 아슬아슬하게
스치며 온/오프로드 판정이 계속 바뀌는 문제가 있었다. 대신 각 `SplineMeshComponent`의
시작/끝/스케일 데이터를 월드 좌표로 변환해 도로 폭의 1/4 너비로 `NavModifierVolume`을
스크립트로 148개 자동 생성했다.

Tank 에이전트 반경을 800에서 200으로 낮춤: 반경 800(지름 16m)이면 도로 스트립(300~400cm)이
Recast 리전 병합 단계에서 주변 영역에 흡수되어 도로 선호가 Tank 에이전트에 반영되지
않았음.

온/오프로드 런타임 속도 차등: 접지 라인트레이스와 별개로 `ECC_GameTraceChannel1` 오버랩
쿼리로 `RoadNavMod` 볼륨 내부 여부를 판정. 오프로드 시 항력 계수 증폭 + 틱당 전진속도
1% 감쇠(`OffRoadSpeedDecayPerTick`) 적용. 엔진힘 자체는 온/오프 무관 동일 - 가속력은
유지하면서 순항 최고속도만 낮춤.

**모드 전환 재설계 - possess 독립 EUGVDriveMode**

멀티 차량 대시보드 운용을 위해 주행 모드를 possess 여부와 완전히 분리한 명시적 상태로
재설계했다.

- `EUGVDriveMode { Idle, Manual, Auto }` - `UUGVMovementComponent::DriveMode`가 유일한 소스
- `SetManualControl`: DriveMode != Manual이면 무시, `RequestDirectMove`: DriveMode != Auto이면 무시
- 콘솔 명령 `SetUGVMode Auto/Manual/Idle`로 possess 없이 즉시 전환
- WASD/Space 바인딩을 `AUGVPawn`에서 `Atitan_examplePlayerController`로 이동 - PlayerController는 항상 InputComponent가 있어 possess 없이도 입력 이벤트가 들어옴
- AIController는 항상 폰을 possess한 채로 유지 - possess 여부가 자동/수동 판정에 영향을 주지 않음

**맵 이탈 방지 - 도로 경계 텔레포트**

148개 `RoadNavMod` 볼륨을 BeginPlay에 캐싱하고 매 틱 `GetClosestPointOnCollision()`으로
도로까지 최단 거리를 계산. `MaxRoadDistance`(기본 400m) 초과 시 최근접점으로 텔레포트.

텔레포트 시 안전 처리: Pitch/Roll을 0으로 리셋(Yaw 유지), 물리 선속도/각속도 0 초기화.

> **트러블슈팅 - 텔레포트 높이 들쭉날쭉**
> `GetClosestPointOnCollision`이 돌려주는 점은 볼륨 박스 콜리전 표면 위 점이라 실제
> 지형 높이와 다름. 이 점 위에서 `ECC_WorldStatic` 라인트레이스로 지형 표면을 다시
> 구하려 했으나 여전히 들쭉날쭉. 원인은 `RoadNavMod` 볼륨이 `WorldStatic` 채널에
> Block으로 설정되어 트레이스가 볼륨 박스 자체를 먼저 맞히고 있었기 때문. 캐싱해둔
> 148개 볼륨을 `FCollisionQueryParams`에 `AddIgnoredActor`로 등록해 해결.

**Blueprint 인스턴스 고정(sticky override) 함정**

이번 작업에서 반복된 패턴: C++ 생성자의 기본값을 바꿔도 이미 배치된 Blueprint 인스턴스는
예전 값을 그대로 들고 있음. `AutoPossessAI`, `bCanEverAffectNavigation`, 입력 액션 할당,
컴포넌트 부착 관계 등에서 반복 발생. 구조적 변경(루트 컴포넌트 교체 등) 후에는 기존
배치 액터를 삭제하고 재스폰하는 게 가장 안전함.

#### 2) 조이스틱 기반 RCWS/UAV 카메라 조작

Logitech Extreme 3D Pro 조이스틱으로 TitanTruck RCWS / UGV RCWS / UAV 짐벌 카메라의
pan/tilt를 조작하는 기능을 구현했다. `ECameraControlTarget { Idle, TruckRCWS, UGVRCWS, UAVGimbal }`
상태와 콘솔 명령 `SetCameraControlTarget TruckRCWS/UGVRCWS/UAV/Idle`로 전환.

조이스틱 X/Y를 `CameraLookAction`(Vector2D) Input Action으로 받아 매 틱 pan/tilt 각도를
누적한다(절대 각도가 아닌 각속도 방식). RCWS는 기존 `URCWSComponent::AddPanTiltInput`
재사용, UAV는 `AUAVPawn::AddGimbalPanTiltInput`을 신규 추가했다.

**조이스틱 하드웨어 인식 - 시행착오**

| 시도 | 결과 |
|---|---|
| 엔진 내장 RawInput 플러그인 | 장치 인식/Slate raw 이벤트는 정상이나 Enhanced Input 레이어까지 전달 안 됨. UE 5.6+ 알려진 회귀 버그(deprecated 플러그인)로 결론 |
| vJoy + x360ce (XInput 에뮬레이션) | VC++ Redistributable 충돌 및 구버전 Win32 API 크래시로 포기 |
| JoystickPlugin (SDL2 기반, GitHub: JaydenMaalouf/JoystickPlugin) | 정상 동작. UE 5.8용 프리빌트 없어 소스 재컴파일, `EngineVersion`을 5.8.0으로 수정 필요 |

- 진단 순서: (1) 장치 인식 로그 확인 -> (2) `PlayerInputDebugger`로 Slate raw 이벤트 확인 -> (3) IMC 매핑 에디터 직접 확인 -> (4) `showdebug EnhancedInput`으로 액션값 확인. 이 4단계를 분리해서 봐야 어느 레이어에서 끊겼는지 특정됨.
- Enhanced Input 배선 시 주의: Y축 키에 Swizzle Input Axis Values(YXZ) 모디파이어를 명시적으로 걸지 않으면 두 축 모두 X로만 들어감.

부수 변경 사항:
- 마우스 커서 항상 표시: `FInputModeGameAndUI`(커서 캡처 안 함)로 변경. 조이스틱으로 카메라를 조작하고 대시보드 위젯도 클릭해야 하는 구조.
- 월드 렌더링 기본 OFF: `bDisableWorldRenderingOnStart`(기본 true) 추가. 대시보드 시나리오는 QuadCam/RCWS 씬 캡처 피드로만 보여지는 게 목적이라 메인 3D 뷰포트 렌더 비용을 절감. `SetWorldRenderingEnabled true/false` 콘솔 명령으로 토글 가능.

#### 3) 객체 탐지 바운딩 박스 (카메라 화면 오버레이)

**방식 선택**: 실제 YOLO 등 ML 모델은 채택 안 함. 씬의 모든 액터 위치/크기를 이미
알고 있으므로, 기하학적 투영 + 가시선(occlusion) 판정으로 "탐지됐다"고 가정하는
postprocessing 방식을 선택. GPU readback 불필요, 가볍고 디버그하기 쉬움.

**파일 구조**

```
titan_example/Source/titan_example/Detection/
  DetectionTypes.h                   EMilitaryFaction, FDetectedTarget
  DetectableTargetComponent.h/.cpp   탐지 대상 마커 + 레지스트리 자동 등록/해제
  DetectableTargetSubsystem.h/.cpp   UWorldSubsystem 기반 탐지 대상 레지스트리
  TargetDetectionComponent.h/.cpp    핵심 스캔 로직 (카메라별 1개)

titan_example/Source/titan_example/UI/
  DetectionOverlayWidget.h/.cpp      Slate 커스텀 페인트 바운딩 박스 그리기
```

데이터 흐름: `DetectableTargetComponent` BeginPlay에 `DetectableTargetSubsystem`에 자기
자신을 등록 -> `TargetDetectionComponent`가 0.1초마다 레지스트리 순회해 `DetectedTargets`
갱신 -> `MissionDashboardWidget`이 0.2초마다 `DetectionOverlayWidget`으로 전달해 그림.

`GetAllActorsOfClass`로 레벨 전체를 훑는 대신 레지스트리만 순회 - 병사 45명이 추가돼도
스캔 비용이 늘어나지 않음.

**화면 투영 수학**

씬 캡처 컴포넌트는 `UGameplayStatics::ProjectWorldToScreen`을 사용할 수 없어(플레이어
카메라가 아니기 때문) View/Projection 행렬을 직접 구성해 투영한다. 뷰 행렬 구성 순서/축
스왑은 `SceneView.cpp::FViewMatrices::UpdateViewMatrix`, 투영 행렬(half-FOV, aspect 적용)은
`SceneCaptureRendering.cpp::BuildProjectionMatrix`를 엔진 소스에서 직접 대조해 검증했다.

**오리엔티드 바운딩 박스**

`GetActorBounds()`는 월드 축 정렬 박스라 차량이 대각선으로 서있으면 실제보다 커진다.
`CalculateComponentsBoundingBoxInLocalSpace()`로 액터 로컬 좌표계 박스를 구하고, 8개
꼭짓점을 액터 실제 월드 트랜스폼(회전 포함)으로 개별 변환한 뒤 투영한다.

**다중 포인트 오클루전 샘플링 + Confidence 히스테리시스**

라인트레이스 하나로 중심점만 확인하면 반쯤 가려진 상황 판정이 불정확하다. 타겟 세로축을
`SamplePointCount`(기본 3)개로 나눠 각각 라인트레이스하고, `VisibleFraction`(보인 점/전체
점)에 따라 타겟별 `Confidence`(0..1)를 `FInterpConstantTo`로 서서히 이동시킨다.

히스테리시스: 신규 탐지는 `Confidence >= 0.6`(AcquireThreshold), 유지는 `>= 0.5`
(LoseThreshold). 3점 기준 2/3(0.67) > 0.6이면 탐지, 1/3(0.33) < 0.5이면 해제. 양쪽
경계에 여유가 동일하게 남도록 설계.

샘플 위치: `VerticalSampleInsetFraction`(기본 0.2)만큼 상/하단에서 안쪽으로 들여서 샘플링.
포탑 끝처럼 얇게 튀어나온 부분에 샘플이 찍히면 언덕 너머로 끝만 살짝 보여도 탐지 유지
되던 문제를 해결.

**WBP 연동**

기존 `SLineGraph`/`SCompass`와 같은 패턴: `SDetectionOverlay : SLeafWidget`이 `OnPaint`에서
`FSlateDrawElement::MakeLines`로 사각형 4변을 Faction별 색상으로 그리고, `UDetectionOverlayWidget :
UWidget`으로 감싸서 WBP 디자이너에서 드래그 배치.

오버레이 위젯은 대응하는 카메라 `Image` 위젯과 정확히 같은 위치/크기로 겹쳐야 함.
`ScreenMinUV`/`MaxUV`가 카메라 프레임 전체 기준 0..1 비율이기 때문.

> **트러블슈팅 - DrawDebugSphere 켜면 WBP Image 전부 검은 화면**
> `bDisableWorldRenderingOnStart=true` 상태에서 `DrawDebugSphere`/`DrawDebugLine`을
> 호출하면 메인 뷰포트 렌더 패스가 스킵되며 디버그 라인배처가 처리되지 않아 Slate/UMG
> 렌더링이 깨지는 현상. 이 프로젝트에서 최소 2회 독립 재현됨. `bDebugDrawDetection`은
> 기본 꺼짐으로 두고 확인 후 반드시 끔. 평소 검증은 로그만으로 충분.

현재까지 차량 3대(트럭/UGV/UAV)가 서로를 Friendly로 탐지하는 상태까지 파이프라인
검증 완료(회전/거리/오클루전 모두 정상). 실제 적군/아군 병사(`memo.md`: 적 15명/아군
30명) 연동은 다음 단계.
