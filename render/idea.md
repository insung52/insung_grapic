HDRI 및 skylight 의 강도를 0으로 하고 directional light 로만 테스트해봄.
레벨의 postprocessvolume 의 ev100 고정 해제.
그러면 directional light 의 밝기가 뭐가 됬든 노출값이 최대한 씬을 밝게 보게 세팅됨.
때문에 나뭇잎이 거의 흰색으로 밝게 보이고, 이로인해 색감 차이가 거의 보기 어려워짐.
허나 그림자 음영 차이는 여전히 존재함.
카메라의 노출값을 건드리지 않고 나뭇잎의 색감 차이를 보기 위해, 라이트 소스 컬러를 흰색에서 검은색 쪽으로 수정.
이러면 마치 노출 고정을 한것과 똑같은 현상이 발생. 색감 차이가 심해지고 어두운 부분은 더 어두워짐.

임시방편 해결법 모음집
1. 만약 씬캡쳐와 기본 렌더링의 lumen 실시간 간접광 계산이 어쩔 수 없이 다른게 맞다면, 렌더링 베이크? 같은 기능으로 미리 렌더링을 다 계산 후, 간접광 계산을 실시간으로 하지 않게 한다. 이는 동적인 ugv, 병력들의 그림자를 해결할 방법이 필요하다.
2. 씬캡쳐 -> 기본 언리얼 렌더링 색공간으로의 매핑 함수를 테스트 씬을 만들어서 계산 후 씬캡쳐 wbp image 에 포스트 프로세싱으로 사용한다.


