# 작업 내역

> 기간: 2026-07-24 ~ 2026-07-30
> 담당: 황인성

---

## 1. 씬캡쳐 vs 실제 렌더링 색감/음영 불일치 — 완전 해결

### 목적
지난 주(W30)에 `AutoExposureBias=-1.0` 보정값으로 95% 정도만 맞춰두고 보류했던 문제.
TitanTruck RCWS / UAV 짐벌 / QuadCam 등 씬캡쳐 기반 화면이 실제 렌더링(UGV RCWS)보다
색이 진하고 그림자가 어둡게 나오는 현상의 진짜 원인을 규명하고, 사후 보정값 없이
완전히 동일한 결과가 나오도록 근본 해결한다.

### 진행 계획
1. 텍스처 스트리밍 미등록 문제 재확인
2. 렌더타겟 감마 인코딩 이중 적용 문제 재조사
3. 씬캡쳐의 Lumen GI/Reflection 강제 비활성화 여부 확인
4. 최소 재현 샌드박스 구축, 실제 원인 규명 및 해결
5. titan_example에 최종 수정 적용, 임의 색상으로 재검증

### 결과

**최종 원인은 서로 완전히 독립적인 세 가지가 겹쳐 있었다:**

1. **엔진이 모든 씬캡쳐의 Lumen GI/Reflections를 기본적으로 강제 OFF시킴.**
   `SceneCaptureRendering.cpp::SetupViewFamilyForSceneCapture()`에 다음 코드가
   명시적으로 존재(주석: *"By default, Lumen is disabled in scene captures"*):
   ```cpp
   View->FinalPostProcessSettings.DynamicGlobalIlluminationMethod = EDynamicGlobalIlluminationMethod::None;
   View->FinalPostProcessSettings.ReflectionMethod = EReflectionMethod::None;
   View->FinalPostProcessSettings.LumenSurfaceCacheResolution = 0.5f;
   ```
   `ShowFlags`로는 전혀 제어되지 않는 별개 메커니즘이라 찾기 어려웠음. 수정:
   `SightCamera`/`GimbalCamera`/QuadCam 4개 카메라의 `SyncLensFromCineCamera(s)`(매 틱)에서
   `bOverride_DynamicGlobalIlluminationMethod`/`bOverride_ReflectionMethod`를 강제 override.

2. **`UTextureRenderTarget2D::GetDisplayGamma()`를 정반대 목적으로 읽는 두 시스템의 충돌
   (진짜 최종 원인).**
   - **Slate**(`SlateRHIRenderingPolicy.cpp`)는 `GetDisplayGamma()==1.0`이면 "이미 인코딩
     끝났다"로 보고 그대로 두고, `!=1.0`이면 자체적으로 감마 인코딩을 한 번 더 건다.
   - **톤매퍼**(`PostProcessTonemap.cpp`)는 반대로 `GetDisplayGamma()==1.0`이면 "아직 감마
     커브가 안 씌워졌다"로 보고 `pow(FilmColorNoGamma, 2.2/GetDisplayGamma())`를 직접 건다.

   `bForceLinearGamma=true`로 Slate 쪽을 고치면 톤매퍼가 실제 뷰엔 없는 `pow(x, 2.2)`를
   씬캡쳐에만 추가로 걸어버림 — 이 곡선은 밝은 영역엔 거의 무영향, 어두운 영역(그림자)일수록
   급격히 더 어두워져서 "그림자만 유독 어둡고 EV100을 올릴수록 차이가 커지는" 정확히 그
   증상을 만든다. 렌더타겟 프로퍼티 하나로는 두 시스템을 동시에 만족시킬 수 없다는 게 핵심.

   **최종 해법**: 렌더타겟은 `RTF_RGBA8`/`bForceLinearGamma=false`(톤매퍼가 실제 뷰와
   동일 동작)로 되돌리고, 대신 UMG에서 렌더타겟을 **일반 텍스처 브러시가 아니라 머티리얼
   브러시**(`M_SceneCaptureDisplay`, 신규)로 표시해서 Slate의 자동 재인코딩 경로 자체를
   우회. 머티리얼 안에서 정확한 sRGB 피스와이즈 디코드 커브를 직접 적용(단순 `pow(2.2)`
   근사는 EV100 극단값에서 미세한 잔차가 남아 정확한 커브로 교체):
   ```hlsl
   float3 c = max(In, 0.0);
   float3 lo = c / 12.92;
   float3 hi = pow((c + 0.055) / 1.055, 2.4);
   float3 mask = step(c, 0.04045);
   return lerp(hi, lo, mask);
   ```
   (`UTextureRenderTarget2D::SRGB` 플래그는 실제 GPU 리소스엔 반영되지 않는 죽은
   프로퍼티라 하드웨어 자동 디코드에 의존할 수 없음 — 엔진 소스 자체 주석으로 확인.)

3. **텍스처 스트리밍 미등록** (해상도 문제, 색감과는 무관, W30부터 유지). 씬캡쳐는
   `AddStreamingViewInfo()` 자동 등록 경로(`GameViewportClient::Draw()`)를 안 타므로
   `RCWSComponent`/`UAVPawn`의 `TickComponent`에서 `CaptureScene()` 직전 수동 등록 유지.

**재현/검증 과정**

문제를 titan_example의 복잡한 대시보드 로직에서 분리하기 위해 `C:\working\mine\testvehicle`
(UE 5.8 Vehicle Template 기반 일회용 샌드박스)에 화면 좌우로 씬캡쳐/실제 렌더링을 나란히
비교하는 재현 환경(`AInvestigationCameraPawn`)을 별도로 구축해서 검증했다. 1라운드
(Lumen 강제 on + `bForceLinearGamma=true`+`RTF_RGBA16f`)는 처음엔 "완벽하다"고 확인했으나,
나중에 밝은 노출에서는 클리핑에 가려 안 보였을 뿐 그림자 영역에서 여전히 어긋난다는 것을
재발견 → 2라운드에서 위 2번(감마 이중 용도 충돌)의 진짜 원인을 규명해 해결.

> **트러블슈팅 - "완벽하게 고쳐졌다"는 확인이 특정 노출값에서만 성립했음**
>
> 1라운드 수정 직후 사용자가 "노출조정 없이도 완벽하게 똑같다"고 확인했으나, 재조사에서
> EV100을 극단적으로 올려보니 그림자 영역 오차가 재현됨. 노출을 의도적으로 극단까지
> 바꿔보고 다양한 색상(민트색 큐브 등)으로 재검증하는 과정이 진짜 결정적이었다.

**최종 결과**: 위 1, 2번을 고치자 `AutoExposureBias` 등 어떤 사후 보정값도 없이 씬캡쳐와
실제 렌더링이 완전히 동일하게 나옴 — 회색/빨강/민트색으로 재검증 완료(2026-07-26,
사용자 실측 확인). 레벨의 `RCWSSightCineCamera`/`GimbalCineCamera` `AutoExposureBias`
오버라이드는 더 이상 불필요해서 제거.

**변경 파일**: `Vehicles/RCWSComponent.cpp`, `Vehicles/UAVPawn.cpp`,
`Plugins/QuadCamModule/.../QuadCamComponent.cpp`(3곳 동일 패턴), 신규
`/Game/UI/Materials/M_SceneCaptureDisplay`, `UI/Monitor1Widget.cpp`,
`UI/Monitor2Widget.cpp`, `UI/MissionDashboardWidget.cpp`,
`Plugins/QuadCamModule/.../QuadCamUIWidget.cpp`(텍스처 브러시 → 머티리얼 브러시 전환).

---

## 2. CineCamera 렌즈 세팅 미연동 + 씬캡쳐 텍스처 뭉개짐 조사

### 목적
색감 문제 해결 직후 발견된 별개의 세 가지 문제를 해결한다: CineCamera 렌즈 설정이
Play 중 반영 안 되는 문제, 씬캡쳐 화면이 실제 렌더링보다 미묘하게 뭉개지는(블러) 문제,
UGV RCWS(실제 렌더링) FOV가 씬캡쳐 화면보다 좁게 보이는 문제.

### 결과

**문제 1 — CineCamera 렌즈/필름백 설정 미반영**

`RCWSComponent`/`UAVPawn`/`QuadCamComponent` 전부 디자이너 입력용 고정 `CameraFOV`
프로퍼티만 FOV를 결정하고, CineCamera가 렌즈/필름백에서 실제 계산하는 FOV
(`UCineCameraComponent::GetCameraView()`)는 읽어서 쓰인 적이 없었음(줌 기능과의 과거
충돌로 의도적으로 이렇게 되어 있었음). CineCamera FOV를 줌 나눗셈과 함께 매 틱 적용하도록
수정, `CameraFOV`는 CineCamera 미해석 시 폴백값으로만 유지. 3개 파일 전부 적용.

**문제 2 — 씬캡쳐 화면 미묘한 블러**

텍스처 스트리밍 LOD, TAA, TSR 미상속, DOF, 렌더타겟-위젯 크기 불일치(다운샘플링) 등
여러 가설을 코드 분석/실측으로 순서대로 기각했다. 진짜 원인: 렌더타겟을 위젯 크기에
정확히 맞춰도 UMG 위젯의 실제 화면 픽셀 크기는 DPI 스케일 때문에 본질적으로 소수점
값(예: 1226×928 × Scale 0.920211)이라, 정수 크기인 렌더타겟을 표시하는 머티리얼
브러시가 매 프레임 "거의 1:1이지만 정확히는 아닌" 스케일로 리샘플링되고, 기본 Bilinear
필터링이 이 미세한 스케일 차이에서도 인접 텍셀을 섞어 흐려 보인다. 실제 렌더링(UGV RCWS)은
텍스처 샘플링 단계 자체가 없어 이 문제가 원천적으로 없었음.

수정: ① 각 씬캡쳐 렌더타겟을 표시 위젯의 실제 픽셀 크기에 맞춰 매 틱 리사이즈, ② 렌더타겟
텍스처 필터링을 Nearest로 설정(근사-1:1 스케일에서의 보간 자체를 제거). RCWS/UAV/QuadCam
(트럭+UGV 4방향) 전부 적용.

**문제 3 — UGV RCWS(실제 렌더링) FOV가 좁게 보임**

`RCWSComponent::PrimaryViewCamera`가 `AspectRatioAxisConstraint` 기본값
(`MaintainYFOV`)을 쓰고 있어 `ULocalPlayer`가 실제 표시 사각형에 맞춰 수평 FOV를
좁혔음. `bOverrideAspectRatioAxisConstraint=true` + `AspectRatioAxisConstraint=
MaintainXFOV`로 수정.

**부가 수정**: `TargetDetectionComponent`의 디버그 구체 표시(`DrawDebugSphere`)가
`bCaptureEveryFrame=true`와 충돌해 프레임 1에서 캡쳐가 얼어붙던 문제 확인, 하드
비활성화.

**미적용 범위**: `MissionDashboardWidget`/`Monitor2Widget`/`QuadCamUIWidget`(레거시/보조
대시보드)은 위젯 쪽 리사이즈 호출을 아직 안 붙임 — 백엔드(Nearest 필터, 리사이즈 함수)는
준비되어 있어 필요 시 `Monitor1Widget`과 동일 패턴으로 붙이면 됨.

---

## 3. 아군(BP_Ally) 부대 이동 + UGV 동반/회피/산개 시스템 구현

### 목적
시나리오.md #4-4~#4-6 — "UGV 자율주행에 아군 30명이 대형을 갖춰 동반 이동하고, 목적지
도착 후 각자 매복 위치로 산개, RCWS 적 포착 시 엄폐+교전"을 구현한다. NavMesh 경로탐색,
RVO 회피, 물리적 충돌 완화까지 여러 차례 갈아엎으며 반복 개선했다.

### 결과

**전체 흐름**

```
BeginAllyFormUpAndAdvance(목적지)
  └ 아군 전원 UGV 기준 대형 위치로 집결(FormingUp, 순차 스태거 출발)
       └ 전원 도착 → 2초 후 UGV 자율주행 시작 + 아군 Following 전환
            └ UGV 목적지 도착(정지) 감지
                 └ 분대장 "정지" 수신호 → 2초 후 "산개" 수신호 → 전원 Approach
                      (동시에 UGV도 대기 위치로 재출발)
                           └ UGV RCWS 적 포착 → 전원 Ambush(엄폐+사격)
```
`UScenarioStateSubsystem`이 단계 전환을 오케스트레이션하고, 실제 이동/전투 로직은
`UAllyFormationComponent`(BP_Ally 부착)가 담당.

**상태별 이동 로직**: FormingUp/Approaching/Ambush 세 상태는 NavMesh 경로 1회 계산 +
순차 웨이포인트 추종을 공유(`BeginNavPathMovement`/`TickNavPathMovement`, 처음엔
FormingUp 전용이었다가 일반화). Following은 UGV가 계속 움직이므로 매 틱 UGV의 실제 주행
경로(폴리라인) 위 arc-length 기준으로 재계산(`ComputeFormationPointOnPath`) — 예전엔
UGV 로컬 오프셋 방식이었는데 UGV 회전 시 대형 끝쪽이 과증폭되는 문제(기차 칸이 선로를
안 따르고 기관차에 고정 각도로 매달려 도는 문제)가 있어 경로 기반으로 교체.

정체 감지: 1초마다 실이동 거리를 체크해 20cm 미만이면 그 위치 기준 NavMesh 경로를
재탐색(정체 풀릴 때까지 매초 반복). 세 상태 모두에 적용.

**회피 시스템 — 반복 개선 끝에 정착한 구조**

- 아군↔아군: 손으로 짠 반응형 회피("가장 가까운 한 명만 보고 비켜가기")가 여러 명이
  좁은 곳에서 서로 얽혀 도는("개미지옥") 문제로 폐기, 언리얼 내장 RVO로 대체. 도착 후
  정지한 아군은 `AvoidanceWeight`를 0.5→1.0으로 올려 "나는 코스를 안 바꾼다"로 고정
  (기본값이면 다가오는 아군이 "쟤도 절반은 비켜줄 것"이라 오판, 좁은 곳에서 못 지나감 —
  엔진 소스 `AvoidanceManager.cpp` 블렌드 공식으로 원인 확인). Following 중엔 대형 위치가
  이미 충돌 없는 결정론적 값이라 RVO를 꺼둠. 최종 안전망으로 아군 캡슐의 Pawn 콜리전을
  Block→Overlap으로 낮추고 매 틱 `ResolveAllyOverlapPush`로 겹친 만큼 밀어냄 + 3초
  유예 후 드리프트 복구.
- 아군↔UGV: **경로 계산 시점 + 실이동 시점 2단 방어**로 정착. ① `UNavArea_UGVBody`(신규)를
  UGV 발밑에 항상 붙이고 아군 전용 쿼리 필터(`UNavQueryFilter_Infantry`)에서만 고비용
  오버라이드 — UGV 자기 경로탐색은 무영향이라 예전처럼 "출발 직전에 장애물을 떼어내야
  하는" 제약이 사라짐. ② `UUGVAvoidanceProxyComponent`(신규)가 UGV의 실제 위치/속도를
  매 틱 언리얼 RVO 매니저에 발행만 해서 아군들이 UGV를 "속도를 가진 장애물"로 자동 인식.
  - 폐기된 접근: `UNavArea_Null`(전원 통행 불가, UGV 자신도 막힘), 손으로 짠 반응형 UGV
    회피(UGV가 움직이면 궤도를 도는 버그 반복 재현).

> **트러블슈팅 - (오진) RVO가 UCrowdManager 초기화 실패로 고장났다고 진단, 꺼뒀던 적**
>
> 엔진 소스 확인 결과 `bUseRVOAvoidance`는 `UCrowdManager`를 전혀 안 쓰고 완전히 별개인
> `UAvoidanceManager`를 씀 — 오진이었음, 재활성화.

> **트러블슈팅 - 아군 회피 중심이 이상한 곳에 있음**
>
> `GetActorBounds()`의 `bOnlyCollidingComponents`가 `false`라 콜리전 없는 컴포넌트(탐지용
> 스피어 등)가 바운딩 박스를 오염시킴. `true`로 변경해 실제 콜리전 있는 형상(차체+터렛)만
> 반영되도록 수정.

**UGV 대기 위치(신규)**: Approach 단계에서 UGV가 별도 위치로 재출발할 수 있게
`SetUGVStandbyDestination(FVector)` 콘솔 명령 추가 — 현재는 콘솔 임시 구현, 정식화는
아래 4절 DataTable 설계로 이어짐.

**NavMesh 경사 조정**: 급경사 지형에 병사들이 올라가려 드는 문제 확인, `RecastNavMesh
AgentMaxSlope`를 44도 → 30도로 낮춤(`DefaultEngine.ini`).

**남은 이슈**: Approach 중 UGV 재출발과 아군 매복 이동이 겹치면서 UGV에 바짝 붙어 스쳐
지나가는 시각적 클리핑 가능성 관찰(실제 물리적으로 밀리진 않음). 개선 후보로
`RadiusPaddingCm` 상향, Approach 시작 지연, 이동 중 회피 반경 확대 등을 검토 중이나
미착수. FormUp 중 일부 개체 NavMesh 경로탐색 실패→직선 이동 폴백 1회 관찰(원인 미상).
NavAgent 높이(144cm)와 아군 캡슐 실제 높이(180cm) 불일치도 점검 필요.

**변경/신규 파일**: `Soldiers/AllyFormationComponent.h/.cpp`,
`Soldiers/NavQueryFilter_Infantry.h/.cpp`(신규),
`Vehicles/UGVAvoidanceProxyComponent.h/.cpp`(신규),
`Vehicles/NavArea_UGVBody.h/.cpp`(신규), `Vehicles/UGVAIController.h/.cpp`,
`UI/ScenarioStateSubsystem.h/.cpp`, `titan_examplePlayerController.h/.cpp`
(콘솔 커맨드: `BeginAllyFormUpAndAdvance`/`BeginAllyFollowing`/`BeginAllyApproach`/
`BeginAllyAmbush`/`SetUGVStandbyDestination`), `Config/DefaultEngine.ini`.

---

## 4. 시나리오 DataTable 시스템 — 설계안 (착수 전)

### 목적
시나리오.md 12절에서 이미 결론 났던 "시퀀서 대신 DataTable로 시나리오 흐름을 관리하자"는
방향을 실제 설계로 정리한다. 지금까지 타이밍 값들이 각 C++ 클래스에
`UPROPERTY(EditAnywhere)`로 흩어져 있어 재컴파일 없이 "시나리오 자체"를 재구성하기 어려운
문제를 해결하기 위함. **이번 주는 설계 정리만, 구현은 아직 착수 전.**

### 결과

**구현 현황 갭 분석**: #4-4(아군 동반기동)는 3절에서 거의 구현 완료. #4-5(20m 진입 →
매복 전환)는 트리거가 스펙("20m 반경 진입", 주행 중)과 다르게 "완전 정지" 감지로
구현되어 있음을 확인, 수신호도 스펙엔 1단계(산개)뿐인데 실제론 "정지→2초 대기→산개"
2단계로 확장 구현되어 있어 합의 필요. #4-6(RCWS 적 포착 트리거)은 브로드캐스트 인프라는
있으나 실제 `TargetDetectionComponent` 탐지 이벤트에 자동 연결되어 있는지 미확인(지금까지
콘솔 명령으로 수동 트리거만 검증). #4-1/2/3/7/8은 이번 세션 범위 밖.

**설계 원칙**: "시나리오 흐름"(트리거 조건/파라미터/대기시간/발동 효과 — DataTable로)과
"동작 튜닝값"(스톱사인 팔 속도, RVO 반경, NavMesh 안정화 시간 등 — 컴포넌트 프로퍼티에
그대로)을 분리. 스키마 초안(`FScenarioStepRow`: `TriggerType`/`TriggerDelaySeconds`/
`TriggerDistanceThreshold`/`EffectType`/`bAutoAdvanceEnabled` 등)과 트리거/효과 열거형
(`EScenarioTriggerType`: Manual/AfterPreviousStep/DistanceThreshold/ActorStopped/
EnemyDetected, `EScenarioEffectType`: BroadcastAllyState/SquadSignal/ShowUIMessage/
SetUGVDestination)을 정리. 기존 `BeginAllyFormUp`/`Following`/`Approach`/`Ambush` 함수는
그대로 "효과 핸들러"로 재사용(호출 주체만 콘솔 → DataTable 순회 매니저로 교체).

**한계**: DataTable이 없애주는 건 "이미 있는 스텝의 타이밍/순서/on-off 조정"이지, 완전히
새로운 트리거/효과 종류(#4-2 UAV 비행, #4-7 도주 웨이포인트 등)를 코드 없이 만드는
것까지는 아님 — 새 케이스는 여전히 `EScenarioEffectType` 추가 + 핸들러 작성 + 재컴파일
필요.

**미확정 — 합의 필요**
- #4-5 트리거를 스펙대로 "20m 진입"으로 되돌릴지, 지금 방식(정지 감지 + 2단계 수신호)을
  유지할지 — 사용자 요청으로 새로 만든 것이라 의도적 변경일 가능성.
- #4-6이 실제 RCWS 탐지 이벤트에 연결되어 있는지 라이브 확인 필요 — 안 되어 있으면
  DataTable 작업과 별개로 먼저 고쳐야 할 수 있음.
- DataTable 스텝을 선형 순서로 할지, 각 스텝이 독립적으로 자기 트리거를 감시하다 발동하는
  병행 구조로 할지(`bRunsInParallel` 플래그 필요 가능성) — 후자가 스펙(#4-3이 #4-4와 병행
  진행)에 더 맞음.

---

## 기타 메모

- 카덱스 레벨 지형 색상 문제 발견: `BP_SplineForest` 등에서 일정 거리 밖 클립맵(Lumen
  LOD로 추정) 경계에서 밝기 단차 발생, `BP_Spline_Fence`가 카메라에 더 가까운데도 도로가
  위에 그려지는 정렬 버그 관찰 — 둘 다 원인 조사 미착수.
