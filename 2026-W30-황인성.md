# 작업 내역

> 기간: 2026-07-17 ~ 2026-07-23
> 담당: 황인성

---

## 1. UGV / UAV 디자인팀 실제 모델 언리얼 적용

디자인팀으로부터 받은 UGV 실제 모델(`SK_UGV`, `BP_UGV_Vehicle`) 및 UAV 실제 모델을
`kadex_demo` 레벨에 배치하고, 기존 플레이스홀더 메시 기반 프로토타입들(`BP_UGVFromTank`,
구형 `AUGVPawn`)을 교체했다. 본 계층 / Physics Asset / 애니메이션 블루프린트는
디자인팀 납품 그대로 사용했으며, 이후 절에서 설명하는 구동계 / RCWS / 카메라 시스템을
이 새 모델 위에 이식했다.

---

## 2. Chaos 기반 UGV 구동계 완료 및 RCWS 포탑 이식

### 목적
디자인팀 실제 UGV 모델(`BP_UGV_Vehicle`, `WheeledVehiclePawn` 직접 상속)에 기존 프로토타입들이
갖고 있던 수동/자동 주행, RCWS 포탑 조준/발사, 개틀링건 스핀 기능을 이식하고,
`BP_UGVFromTank`를 완전히 퇴역 처리한다.

### 진행 계획
1. 주행 제어 및 NavAgent 설정 완료
2. RCWS 컴포넌트 세트 이식 및 배선
3. 포탑 시각 회전 (본 드라이브) 구현
4. 개틀링건 스핀업/스핀다운 구현
5. 컬링 버그 원인 파악 및 임시 대응

### 결과

**최종 구조**

`BP_UGV_Vehicle`이 수동/자동 주행 + RCWS 포탑까지 전부 갖춘 최종 UGV 액터다.
`WheeledVehiclePawn` 직접 상속이라 M1A2가 겪었던 "PlayerController가 possess해야만
물리 활성화" 문제 자체가 없다.

- 주행 제어: `AUGVAIController`가 possess한 채 상시 유지, `DriveMode (Idle/Manual/Auto)`로 제어.
  이 컨트롤러는 `SetManualControl`이라는 이름의 함수를 가진 아무 Pawn에나 리플렉션 기반으로
  범용 동작하므로 별도 C++ 수정 없이 붙었다.
- RCWS: `RCWSMount` / `RCWSSightCamera` / `RCWS` / `TargetDetection` / `DetectableTarget` /
  `RCWSFireControl` / `Muzzle` 컴포넌트 세트를 `BP_UGVFromTank`와 동일한 이름 규칙으로 추가.
  `FComponentReference`가 이름 기반 자동 해석이라 이름만 맞추면 C++ 코드 수정 없이 연결됨.

**포탑 시각 회전**

본 계층 구조: `Hull1 -> Rotation_base2 (선회) -> RCWS3 (센서/포탑 몸통) -> RCWS_Barrels4 (포신)`.
`RCWS3`에 상하 틸트를 걸어야 그 자식인 포신까지 자동으로 따라간다.
`Rotation_base2`에는 좌우 Yaw만, `RCWS3`에는 상하 Pitch만 걸었다.

`BP_UGV_Vehicle`의 `UpdateTurretVisuals` 함수가 `RCWS->GetMount()->GetRelativeRotation()`에서
Yaw/Pitch를 분리해 변수에 저장하면, `ABP_UGV_Vehicle`(AnimBP)의 `Blueprint Update Animation`이
매 프레임 이 두 변수를 복사하고 ModifyBone 노드 2개(기존 바퀴 8x2 ModifyBone 체인 뒤에 이어 삽입)가
실제 본을 구동한다.

카메라와 포탑이 항상 완전 동기화됨 (본이 마운트 회전값을 매 프레임 그대로 복사, 자체 슬루/지연 없음).
M1A2식 포탑 레이턴시 보정 로직이나 별도 조준선 위젯이 불필요해진 구조다.

**개틀링건 스핀업/스핀다운**

`RCWS_Barrels4`의 Roll을 매 틱 누적 (`BarrelSpinAngle`). 발사 감지는
`RCWS->GetCurrentData().AmmoCurrent`가 이전 틱보다 줄었는지로 판단하고,
1200rpm 기준 발사 간격(0.05초) 동안도 "발사 중"으로 유지되도록 유예 시간
(`FireDetectionGraceSeconds=0.2초`)을 둔다.

```
발사 감지 시: BarrelSpinSpeed를 BarrelSpinUpRate (3000도/초^2)로
              BarrelSpinMaxSpeed (1440도/초, 초당 4바퀴)까지 가속
발사 중단 시: BarrelSpinDownRate (400도/초^2, 가속의 약 1/7)로 서서히 감속
```

> **트러블슈팅 - write_graph_dsl이 실행 체인을 연결하지 않음**
>
> 개틀링건 로직을 DSL로 재작성했을 때 컴파일은 에러 없이 성공했지만 함수가 아예 실행되지
> 않았음. FunctionEntry부터 이어져야 할 exec 체인이 Set 노드 6개 전부에서 빠진 채로
> 생성됨(데이터 배선은 정확, "언제 실행할지" 연결만 없었음). 라이브 PIE에서 TurretYaw/GunPitch가
> 항상 0으로 고정되는 걸 보고 확인, `get_node_infos`로 FunctionEntry의 then 출력 핀이
> `connected_pins: []`인 걸 보고 원인 확정. `connect_pins`를 수동으로 6번 호출해서 해결.
> write_graph_dsl로 순차 실행문이 여러 개 있는 함수를 작성하면 컴파일 성공 여부와 무관하게
> `get_node_infos`로 FunctionEntry부터 끝까지 exec 체인이 실제로 이어져 있는지 반드시 확인해야 한다.

**포탑 컬링 버그 - 원인 확정 및 임시 대응**

RCWS 씬캡쳐뿐 아니라 에디터 뷰포트로 봐도 차체가 프러스텀 밖으로 나가면 포탑까지
통째로 사라지는 버그. 엔진 소스(`SkinnedMeshComponent.cpp`) 확인 결과:

`USkinnedMeshComponent::CalcMeshBound()`는 애니메이션된 라이브 포즈를 따라다니며 바운드를
계산하지 않는다. 물리 시뮬레이션 중인 컴포넌트는 Physics Asset의 AABB를 렌더링 바운드로
사용한다. `SK_UGV_PhysicsAsset`은 차체/바퀴용 바디만 있고 포탑 본 3개(`Rotation_base2` /
`RCWS3` / `RCWS_Barrels4`)에는 바디가 없어서, 포탑이 아무리 회전해도 바운드 계산에 포함되지 않는다.

임시방편: `VehicleMesh.BoundsScale`을 1.0 -> 4.0으로 인상. 실제 충돌 콜리전/서스펜션/질량
계산에는 전혀 영향 없는 순수 렌더링/컬링 판정용 배율.

정확한 해결책(미착수): Physics Asset Editor에서 포탑 본 3개에 시뮬레이션 안 하는 키네매틱 전용
바디를 추가하면 `CalcAABB()` 계산에 포함되어 근본 해결됨.

**RCWS 미리보기 디오라마 - UPoseableMeshComponent 전환**

기존 `ARCWSPreviewActor::SyncTurretMesh()`가 마운트에서 스태틱 메시를 복사하는 구조였는데,
`BP_UGV_Vehicle`의 `RCWSMount`는 빈 `USceneComponent`라 복사할 메시가 없어서 동작 불가.
`UPoseableMeshComponent`로 전환해서 `SK_UGV` 스켈레탈 메시를 로드하고, BeginPlay에서
포탑 3개 본 외 44개 본을 `HideBoneByName`으로 숨기고, 매 틱 `Rotation_base2` (yaw) /
`RCWS3` (pitch)를 실제 차량과 동일한 로직으로 포즈한다.

`PoseableMeshComponent::SetBoneRotationByName`은 WorldSpace / ComponentSpace만 지원하고
"부모 본 기준" LocalSpace가 없어서, 쿼터니언을 직접 합성 (자식ComponentSpace = 부모ComponentSpace
* 로컬델타쿼터니언) 하는 방식으로 구현.

> **트러블슈팅 - 미리보기 실루엣 소실 (미해결)**
>
> PIE 시작 로딩 중 UGV 전체 검은 실루엣이 잠깐 보였다가 이후 완전히 안 보이게 됨.
> `BoundsScale=4` 적용해도 해결 안 됨. HideBoneByName의 렌더 상태 반영이 첫 프레임엔 안
> 먹히거나, PoseableMesh의 정적 바운드 기반 컬링 문제로 추정 중. 다음 작업으로 미룸.

**TitanTruck 4분할 카메라 검은 화면**

`QuadCamComponent::TickComponent`가 `bAlwaysVisible || bVisible`일 때만 `CaptureScene()`을
호출하는데, `bAlwaysVisible`이 기본값 `false`라 WBP_kadex가 직접 렌더타겟을 바인딩하는
구조에서는 캡처가 한 번도 돌지 않아 렌더타겟이 항상 초기(검은) 상태로 남아있었음.
`BP_TitanTruck.QuadCam.bAlwaysVisible = true`로 즉시 해결.

---

## 3. 위성지도 캘리브레이션 및 나침반 방향 버그 수정

### 목적
실제 두 지점의 언리얼 월드 좌표 재측정값과 위성사진 픽셀 좌표를 `GeoCoordinateUtils.h`에
반영하고, 미니맵을 씬캡쳐에서 정적 위성사진으로 교체한다. 이 과정에서 발견된 나침반
방향 거울상 버그를 수정한다.

### 진행 계획
1. GeoCoordinateUtils.h 캘리브레이션 상수 갱신
2. 픽셀 - 월드 유사변환 추가 및 AMinimapCaptureActor 교체
3. 나침반 방향 버그 원인 분석 및 수정

### 결과

**캘리브레이션 상수 갱신**

두 기준 지점의 언리얼 월드 좌표를 재측정값으로 교체했다.

| 지점 | 위경도 (불변) | 기존 씬 좌표 | 신규 씬 좌표 | 위성사진 픽셀 (신규) |
|---|---|---|---|---|
| Point A | 37'54'44.30"N, 128'10'59.46"E | (8700, 19250) | (8210, 20210) | (527, 632) |
| Point B | 37'55'36.09"N, 128'10'53.53"E | (-1830, -111030) | (-2990, -110670) | (502, 26) |

`GetDistanceScaleFactor()`는 `GetSceneToRealTransform()`을 매번 재계산하는 구조라 상수
갱신만으로 속도/사거리 보정값이 자동 반영된다.

**픽셀 - 월드 캘리브레이션 추가 (미니맵용)**

`GetPixelToWorldTransform()` - `GetSceneToRealTransform()`과 동일한 복소수 나눗셈 방식,
(픽셀 좌표, 월드 좌표) 쌍으로 직접 피팅 (위경도를 경유하지 않음).
`WorldLocationToMapPixelUV()` / `MapPixelUVToWorldLocation()`으로 미니맵 마커 배치 및
클릭 -> 위경도 표시에 사용.

`AMinimapCaptureActor`에서 씬캡쳐 컴포넌트를 완전 제거하고 정적 위성사진 (`/Game/widget/m_map`,
1024x1024)만 BeginPlay에서 로드하도록 변경. 액터의 월드 배치 위치는 더 이상 계산에 쓰이지
않는다.

미니맵 클릭 -> 위경도 표시: `UMonitor1Widget::NativeOnMouseButtonDown` 오버라이드 -
클릭 좌표가 `MinimapImage` 영역 안이면 UV -> `MapPixelUVToWorldLocation` ->
`WorldLocationToLatLong` -> `FormatDMS`로 변환해서 `MinimapClickLatLongText`에 표시.

**나침반 방향 거울상 버그 수정 (핵심)**

RCWS 방위각 리본에서 오른쪽(동쪽)으로 90도 돌리면 방위각이 90(E)이 아닌 270(W)으로
표시되는 버그.

원인: 언리얼 월드가 왼손 좌표계인데 씬 좌표를 `Zs = SceneX + i*SceneY`로 오른손 복소수
평면으로 취급하면서 생긴 거울상 반전. 기준점(북쪽) 하나만 맞으면 겉보기엔 정상처럼 보여서
`GeoCoordinateUtils.h` 최초 작성 시점부터 있던 버그.

검증: 두 기준 지점 간 진짜 방위각을 표준 항법 공식(forward azimuth)으로 독립 계산한 뒤
코드 예측값과 대조 - SceneY 부호 반전 시 두 검증이 동시에 통과하고 `GetDistanceScaleFactor()`는
변화 없음을 확인.

| 검증 항목 | 기존 코드 | SceneDY 부호 반전 후 |
|---|---|---|
| A->B 방위각 독립 계산 대조 | 불일치 | 354.839 deg 정확 일치 |
| Yaw 0->90 회전 시 방위각 변화 | -90 (반대) | +90 (정상) |
| GetDistanceScaleFactor() | 1.2241 | 1.2241 (불변) |

수정: `GeoCoordinateUtils.h`의 `GetSceneToRealTransform`, `WorldLocationToLatLong`,
`SceneVectorToBearingDegrees` 세 곳에서 `SceneDY` 부호 반전. 공용 함수 하나 수정으로
아래 전체가 한 번에 수정됨:
- `RCWSComponent::RefreshAzimuthElevation`의 `AzimuthDegrees`
- `AUAVPawn`의 `HeadingDegrees` / `GetGimbalWorldHeadingDegrees`
- `Monitor1Widget`의 트럭 4분할캠 리본

미니맵 마커 회전(`SceneYawToMapScreenAngleDegrees`)과 `GetDistanceScaleFactor()`는
독립적인 별개 계산이라 영향받지 않음.

미니맵 UGV 마커 방향 수정: 기존 코드가 이전 모델 (-X가 정면이던 `AUGVPawn`/`BP_UGVFromTank`)
기준으로 `GetActorForwardVector()`를 negate하고 있었음. 현재 유일하게 남은 UGV
(`BP_UGV_Vehicle`)는 표준 +X 전방 컨벤션이라 negate를 제거하고 `GetActorRotation().Yaw`를
그대로 사용하도록 수정.

---

## 4. 전체 카메라 작동 방식 개편 및 버그 수정

### 목적
RCWS / QuadCam / UAV 카메라 시스템을 CineCamera 기반으로 통일하고, RCWS 조준 화면의
Lumen 버벅임 문제를 해결한다. 씬캡쳐의 지글거림(shimmer) 원인을 확정하고 수정한다.

### 진행 계획
1. CineCamera 아키텍처 통일 (씬캡쳐의 렌즈 설정 참조용으로만)
2. UGV RCWS 조준 화면을 실제 메인 렌더링으로 전환
3. TitanTruck/UAV까지 확장 시도 후 결과 정리
4. 씬캡쳐 TAA/Lumen 품질 개선

### 결과

#### 1단계 - CineCamera 아키텍처 통일

새 구조 (RCWS / QuadCam / UAV 세 시스템 전부 동일 패턴):

1. 디자이너는 `UCineCameraComponent` 하나만 원하는 위치에 배치하고 렌즈/DOF/노출을
   Details 패널에서 튜닝.
2. `BeginPlay`에서 코드가 `NewObject<USceneCaptureComponent2D>`를 CineCamera와 같은 부모의
   형제(sibling)로 생성하고 트랜스폼을 복사.
3. CineCamera는 이후 다시는 움직이지 않는 순수 렌즈 설정 참조용.
4. 매 틱 `SyncLensFromCineCamera()`가 `PostProcessSettings`만 캡처로 복사. FOV는 절대
   복사하지 않음(줌 입력이 캡처의 `FOVAngle`을 직접 소유).

> **트러블슈팅 - QuadCamModule.Build.cs에 CinematicCamera 모듈 누락**
>
> 플러그인은 게임 모듈과 의존성이 독립적이라 따로 `CinematicCamera` 모듈을 추가해야 함.

> **트러블슈팅 - UAV GimbalCineCamera 필드명 충돌**
>
> `AUAVPawn`은 `BP_UAV`가 직접 상속하는 베이스 클래스라 블루프린트 SCS 컴포넌트 이름과
> C++ private 필드명이 겹치면 컴파일 오류 발생. 필드명을 `ResolvedGimbalCineCamera`로 변경
> (FComponentReference가 찾는 이름 `"GimbalCineCamera"`는 그대로 유지)해서 해결.

#### 2단계 - UGV RCWS 실제 렌더링 전환 (완료, 안정 확정)

RCWS 조준 카메라에 Lumen GI를 켜면 PIE 중 정상 구간과 완전 정지 구간이 번갈아 나타나고
그 주기가 점점 길어지는 버그. `bDisableWorldRenderingOnStart=true` (메인 뷰포트 렌더링을
통째로 끔) 상태에서 Lumen 씬캡쳐가 동시에 있으면 발생. 메인 렌더링을 실시간으로 껐다
켰다 하며 버그가 즉시 나타났다 사라지는 것으로 인과관계 증명.

해결책: 메인 뷰포트를 켜두는 미봉책 대신, RCWS 조준 화면 자체를 실제 메인 렌더링으로
활용하는 재설계.

- `ULocalPlayer::Origin/Size` (스플릿스크린이 쓰는 서브렉트 필드)를 1명 환경에 그대로 적용.
  화면의 `RCWSViewImage`가 차지하는 자리에만 실제 3D 렌더링이 나오고 나머지는 검은색.
  매 프레임 `RCWSViewImage`의 실제 지오메트리를 읽어서 계산(해상도 하드코딩 없음).
- `RCWSComponent`에 `UCameraComponent* PrimaryViewCamera` 신규 생성 (SightCamera 자식, 위치/회전
  자동 상속, FOV/PostProcess만 매 틱 복사). `SightCineCamera`는 `SetActive(false)`로 비활성화.
- `titan_examplePlayerController::BeginPlay()`에서 `SetViewTarget(UGV)`으로 뷰타겟 고정.
- `bDisableWorldRenderingOnStart` 기본값 `true -> false`로 영구 확정.

> **트러블슈팅 - Hidden 슬레이트 가시성이 GetCachedGeometry()를 (0,0)으로 고정**
>
> `Hidden`은 이 프로젝트 페인트 경로에서 `Collapsed`처럼 취급됨. `RCWSViewImage`를
> `HitTestInvisible` + `RenderOpacity(0.f)`로 대체해서 해결.

> **트러블슈팅 - FGeometry::LocalToAbsolute()가 데스크톱 좌표 반환**
>
> 듀얼모니터 환경에서 창이 데스크톱 원점과 다른 위치에 있으면 틀린 좌표가 나옴.
> `USlateBlueprintLibrary::AbsoluteToViewport()`로 변환(DPI 스케일도 처리)해야 함.

> **트러블슈팅 - UGameViewportClient::LayoutPlayers()가 매 프레임 서브렉트를 덮어씀**
>
> `SyncRCWSViewportRect()`가 매 프레임 정확한 값을 쓰는 것까지 로그로 확인했는데도 렌더링은
> 계속 풀스크린이었음. `Draw()` 직전에 `LayoutPlayers()`가 표준 스플릿스크린 레이아웃으로
> 강제 리셋하고 있었기 때문. `titan_exampleViewportClient`에서 `LayoutPlayers()`를 오버라이드해
> 로컬 플레이어가 1명일 때는 스킵하도록 해서 해결.

#### 3단계 - TitanTruck/UAV 실제 렌더링 확장 시도 -> 롤백

UGV RCWS가 실제 렌더링으로 잘 되니, TitanTruck RCWS + UAV 짐벌도 각각 전용 `ULocalPlayer`를
추가로 만들어 동시에 실제 렌더링하는 방식으로 확장 시도. 구현했으나 롤백함.

롤백 이유: 3개 뷰 동시 풀 Lumen 렌더링 시 나무에 비정상적인 흰색 반사광 증폭과 줌/시점
이동 시 화면 가장자리에 알록달록 노이즈 발생. 미니멀 테스트 레벨에서도 뷰 개수에 비례해서
나타남 (1개=없음, 2개=약간, 3개=확실). Lumen의 Radiance Cache/Screen Probe가 프레임당
정해진 트레이스 예산으로 수렴하는 구조에서 동시 렌더링되는 뷰가 늘수록 노이즈가 심해지는
것으로 추정.

최종 결정: UGV RCWS만 실제 렌더링 유지, TitanTruck RCWS / UAV 짐벌 / QuadCam은 전부
씬캡쳐 방식으로 복귀.

#### 4단계 - 씬캡쳐 TAA 비활성화 원인 확인 및 수정 (완료)

TitanTruck RCWS / QuadCam / UAV 화면의 지글거림 (나뭇잎 등 고주파 지오메트리의 그림자
경계가 매 프레임 다르게 흔들림) 원인 확정:

씬캡쳐는 `USceneCaptureComponent2D::ShowFlags.SetTemporalAA`가 기본값으로 꺼진 채 시작함
(메인 뷰포트는 켜진 채 시작 - 기본값 자체가 다름). TAA/TSR은 여러 프레임에 걸친 시간적
누적으로 노이즈를 지워주는 게 핵심 역할이라, 꺼진 씬캡쳐는 매 프레임 다른 노이즈 패턴이
그대로 찍힘.

수정: `RCWSComponent.cpp`와 `UAVPawn.cpp`의 `SyncLensFromCineCamera()`에
`ShowFlags.SetTemporalAA(true)` 명시적으로 추가 (캡처 생성 시 한 번이 아니라 매 틱 강제 -
이 함수가 `PostProcessSettings`를 통째로 재할당하는 곳이라 그 근처에 넣어야 안정적).
QuadCam은 저해상도/노이즈 있는 CCTV 룩이 의도적이라 그대로 둠.

TitanTruck RCWS는 이 수정만으로 UGV RCWS (실제 렌더링)와 거의 동일한 수준으로 개선됨.

#### 5단계 - UAV 짐벌 Lumen 품질 복원 (완료)

4단계 후에도 UAV 화면만 그림자가 지글거리고 나뭇잎에 비정상적인 흰색 반사광이 남아있었음.

원인: `UAVPawn.cpp::BeginPlay()`에 이전 세션에서 QuadCam/Minimap과 같은 저가형 모니터링
피드 취급으로 남겨둔 잔재 코드:

```cpp
GimbalCamera->ShowFlags.SetLumenGlobalIllumination(false);
GimbalCamera->ShowFlags.SetLumenReflections(false);
GimbalCamera->ShowFlags.SetScreenSpaceReflections(false);
GimbalCamera->ShowFlags.SetReflectionEnvironment(false);
```

Lumen GI/리플렉션이 다 꺼지면 간접광이 없어져 오토익스포저가 전체를 밝게 보정하고,
그 상태에서 직사광 스페큘러만 남아 흰색으로 날아가 보였던 것.

수정: 4줄 삭제 (RCWS와 동일하게 프로젝트 기본값 그대로). Lumen 서페이스 캐시 해상도가
씬캡쳐에서만 기본 0.5로 떨어지는 엔진 동작도 발견, `LumenSurfaceCacheResolution=1.f`를
RCWS/UAV 양쪽 `SyncLensFromCineCamera()`에 강제 추가.

#### 6단계 - 씬캡쳐 vs 실제 렌더링 색감 불일치 (미해결, 보류)

4~5단계 후에도 TitanTruck RCWS/UAV 화면이 UGV RCWS (실제 렌더링)보다 색이 진하고 그림자가
더 어둡게 보임. CaptureSource 설정, 렌더타겟 sRGB 포맷, Lumen 서페이스 캐시 해상도,
CineCamera별 PostProcessSettings 차이 등을 전부 확인했으나 원인 아님.

유력한 실제 원인: 레벨 라이팅 세팅이 정상 범위를 크게 벗어나 있음. `HDRIBackdrop`
Intensity=150 (정상 1.0 근처), 자식 `Skylight` Intensity=900, 레벨 `PostProcessVolume`이
`EV100 Min=Max=10`으로 강제 고정해서 과도한 광원을 상쇄시키는 구조. 이 불균형한 세팅이
씬캡쳐와 실제 렌더링 두 개의 독립된 렌더 패스 사이에 아주 작은 차이도 크게 증폭시키는
것으로 분석됨. EV100 고정을 풀고 HDRI/Skylight 세기를 정상으로 되돌렸을 때 씬캡쳐와
실제 렌더링이 거의 완전히 일치하는 것을 실측으로 확인.

레벨 콘텐츠(라이팅) 조정 사항이라 나중에 처리하기로 하고 보류.
