> [보관됨 2026-08-31] "모니터 1"(구 `Monitor1Widget`)은 더 이상 안 쓰임 — 현재 실사용 UI는
> `SelfDefenseMonitor1Widget`/`SelfDefenseMonitor2Widget`/`AxisSelectionWidget` 등(사용자 확인).
> 전용 후속 문서는 아직 없음, 필요시 새로 조사.

# 모니터 1

기본적인 변동 값에 대한 텍스트 컬러 : 17B9FF 하늘색 또는 흰색, 내가 따로 표시한거에 대해서만 색상 강제.

## 헤더

현재 날짜 (2026-07-06 형식) 

현재 시간 (10:44:41 형식)

설정 버튼(설정 ui 는 아직 계획 없음)

종료 버튼(실제로 게임 종료)

## 메인 뷰 화면

이동형 지휘소 근처 지형 렌더링 화면 (임시로 titan truck rcws 화면으로 대체)

titan truck 주변 영상 (이미 구현됨, UI 시안에서는 UGV 주행영상 이라고 오타 되어있음.)

## 미니맵 포함 요소

적군 예상 위치 (빨간색 반투명 원으로 예상 위치를 넓게 표시)
이후 RCWS 카메라/UAV 카메라가 적군 감지 시 적군 위치를 빨간색 마커로 정확하게 표시

UGV 위치
UGV 진행 방향(차량 방향)  
- 화살표 또는 탱크모양 아이콘 같은걸 미니맵에 띄우고 아이콘을 회전시켜서 방향 표시
UGV RCWS 가 보고 있는 방향 (반투명 부채꼴로 시각화)
UGV 목표 지점
UGV 이동 예정 경로(현재 ugv 위치 -> ugv 목표 지점 까지의 경로들을 선으로 이어서 시각화)

UAV 는 상황 발생 시 가장 먼저 직선 이동으로 WP2 로 이동함.
- uav 이동 경로를 WP1(시작점, 파란색) -> WP2(도착점, 초록색) 마커로 표시, 흰색 점선으로 이음.

UAV 위치 (드론 모양 아이콘)
UAV 카메라의 보는 방향


이동형 지휘소 위치 (지휘소 또는 차량 아이콘)
이동형 지휘소 차량 방향
이동형 지휘소 RCWS 가 보고 있는 방향

미니맵 축적 표시 (현재 미니맵의 직교 너비를 바탕으로 계산)

- 미니맵 부가기능(나중에 구현)
- 미니맵에 커서 올린 상태로 마우스 휠로 확대/축소 할수 있는 기능 있으면 좋을듯.
- 최대 확대/축소 배율은 정해야함
- 마우스 커서 위치를 중심으로 확대/축소

## UAV 상태 정보

UAV의 고도,속도,방향 실제로 UAV에 연결

- 고도, 속도는 구현된 대로 그래프, 방향은 나침반 png 사용(compass_outer 는 고정, compass 이미지가 회전)
- compass 이미지를 보니 오른쪽 위 45도를 보는게 기본값(NE 방향) 이므로 고려해서 코딩
- 그래프의 경우 그래프 선 아래가 그림자처럼 그라데이션 처리가 되어야하는데, 이거는 어떻게 구현해야할지 토론 필요함.

고도 텍스트 (형식 : 152) (단위값은 글자 크기가 다르므로 따로 텍스트 위젯) 하늘색 텍스트

속도 텍스트 (형식 : 32.6) 하늘색 텍스트

방향 텍스트 (형식 : 312°) 하늘색 텍스트

GPS 메인, 서브 텍스트 모두 그냥 텍스트로만 고정해서 표현 예정

링크 메인 텍스트 '강함' 아마 텍스트로 고정

링크 주파 값 (형식 : 주파 : 72%) 더미로 조금씩 변하는 값 전달

임무 메인 텍스트 '임무 수행 중 고정 예정

임무 wp 값 : uav 가 목적지에 도달하기 전에는 1/4, 그 후에는 2/4로 변경

신호 세기 4개 바 지금 방식처럼 4개를 하나씩 끄는 방식으로 유지

신호 세기 텍스트 : (형식 : -62 (숫자만)) 더미로 조금씩 변하는 값

배터리 (형식 : 78% )

- 50% 이하 시 노란색, 20% 이하 시 빨간색으로 글자 색만 바꾸기?

비행시간 텍스트 형식 : 25:34 (분:초)

## UAV 영상 피드

나침반 위젯 : 트럭 cctv 영상 및 rcws 화면 등 각 카메라 별로 카메라가 보는 방향을 시각화해주는 위젯. 배틀그라운드 같은 게임에서 방향을 보기쉽게 표시해줌. 

- 방향 값 숫자(330) 는 표시하지 않고, EWSN 랑 수직 눈금만 있는 버전(트럭 4분할 cctv 영상)

- 방향값도 30 단위(30 아닐 수도 있음. 동서남북 각 방향을 3등분해서 시안처럼 W N 사이에 2개 숫자 들어가야함)로 숫자 + 큰 눈금으로 표시하는 버전 (UAV 영상 피드)

UAV 좌표값 텍스트 (N 37° 12’ 48.1”  형식으로 N E 2개, 아마 위도 경도?)

UAV 카메라 배율 선택 버튼 2개 (1.0x, 2.5x), 선택된 배율은 하늘색 텍스트

# 모니터 2

## RCWS 조준 화면

RCWS EL (위 아래 각도), 방향값 눈금 UI 처럼 눈금 + 숫자가 움직이는 위젯

EL 값 텍스트 (형식 : +04.6°)

zoom 확대 값도 움직이는 눈금 UI로 구현 + 텍스트 위젯(현재 배율 (형식 : 2.5x))

바라보는 곳 까지의 거리 측정 값 (형식 : RNG 1,253 m)

## RCWS 3D 렌더링

RCSW 3d 단독 뷰어 창 (뭔지는 모르겠는데 아마 현재 rcws 의 상태를 보기위해서 rcws 만 따로 렌더링한 3d 창? 을 보여주는 컨셉인듯. 아직은 구현 계획 없음)

## RCWS 상태 정보

모드 : 원격 제어(조이스틱으로 조종)/자동 제어(자동으로 적을 조준)

표적 추적 : 추적 중

방위각 (형식 : 330.2°)

시스템 연결 : 정상(더미, 나중에 게임 시작 시 0.5초 정도 딜레이 후 연결되는 연출 생기면 그때 작업)

고각 : (형식 : +04.6°)

전원 상태 : 더미값, +-0.3v 정도로 느리고 가끔씩 변동하면 현실적일듯 (형식 : 정상 27.6V)

탄약 현황 : rcws 시스템 구현 후 연동 (형식 : 475 / 600)

경고/알람 : 없음 (더미)

안정화 : 정상 (더미)

시스템 온도 : rcws 발사할때 값이 오르는? 그런 연출 있을수도. 아직 계획 없음 (형식 : 48°C)