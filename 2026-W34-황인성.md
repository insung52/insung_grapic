# 작업 내역

> 기간: 2026-08-14 ~ 2026-08-20
> 담당: 황인성

---

## 1. 리눅스 패키지 빌드 프레임 폭락 — 원인 규명 및 해결

### 목적
리눅스 패키지 빌드를 풀스크린(QHD, 2560×1440)으로 띄우면 11fps까지 떨어지는 문제의 원인을
찾아 해결한다. 창모드+저해상도는 100~200fps로 정상, 동일 조건 Windows PC에서는 재현 안
됨 — 리눅스(SDL2) 플랫폼 고유 이슈로 좁혀서 조사.

### 결과

**증상 요약**: `kadex_lobby`(거의 빈 레벨)와 `kadex_test`(오브젝트 다수)에서 동일하게
느림 → 씬 복잡도와 무관한 병목. 이 조사는 원래 `RtspEncoder` 플러그인(UGV/자체방호축 다중
카메라 RTSP 스트리밍)의 지연 최적화 작업 도중 발견됐다.

**배제된 원인들**: 디스크(NVMe SSD 확인), RAM 부족/스와핑(Swap 항상 0), GPU 연산 병목
(`nvidia-smi` 기준 사용률 10~40%, 한가함), 씬 렌더링 복잡도(Unreal Insights 렌더 패스
시간 전부 정상 범위), Vulkan Present/VSync 자체(`r.vsync 0` 등 다 테스트해도 증상 그대로),
GPU 전력상태(P-state, Persistence mode 켜도 결과일 뿐 원인 아님). **RTSP 네트워크 전송도
배제** — `RtspEncoder` 플러그인을 통째로 비활성화(모듈 의존성 제거 + `TITAN_RTSP_ENABLED=0`)한
빌드로도 동일 증상이 재현돼서, "RTSP 서버 서브시스템이 원인 아닐까"라는 당시 유력했던
작업 가설이 배제됨.

**결정적 증거 — `perf record -g` 콜스택**(RHIThread, 10초 샘플링, 52508 샘플): 압도적
1위(68.40%)가 렌더링도 GPU 드라이버도 아니라 소켓 write 경로
(`__x64_sys_writev → unix_stream_sendmsg`(AF_UNIX, 로컬 IPC) → `skb_copy_datagram_from_iter`
→ `copy_page_from_iter`). 유저스페이스 호출 체인에서 `libxcb`(X11 프로토콜 클라이언트
라이브러리) 심볼이 잡혀서, RHIThread가 X 서버와 직접 통신 중임이 콜스택으로 확정됨 —
렌더러 자체 코드와 NVIDIA 드라이버 코드는 합쳐서 5% 미만.

**원인**: 이 시스템은 GNOME(Wayland) 데스크톱이지만, X11 전용 앱도 그냥 돌아가게 해주는
호환 레이어 **Xwayland**가 백그라운드에 항상 떠있음. UE Linux 클라이언트(SDL2)는 아무
오버라이드가 없으면 SDL2 자체의 기본 드라이버 우선순위를 그대로 따르는데, **SDL2는 역사적으로
X11을 Wayland보다 먼저 시도**한다(Wayland 백엔드가 한참 늦게 추가돼 오랫동안 "차선책"
취급이었기 때문). Xwayland가 항상 떠있으니 SDL이 X11을 골라도 창은 문제없이 뜨고 —
**에러 없이 조용히 "느리게" 성공**해버려서 프로파일링 전엔 아무도 눈치채지 못했음. 이
Xwayland 경유 경로에서는 Vulkan WSI Present가 GPU 다이렉트 버퍼 공유(DRI3) 대신 X11
프로토콜 소켓을 통한 소프트웨어 카피로 동작한 것으로 보이며, 그 복사 비용이 프레임
해상도(픽셀 수)에 정비례해서 커짐 — 관찰된 모든 증상(해상도 비례, 씬 복잡도 무관, GPU
한가함+RHIThread 한 코어만 100%, RTSP/네트워크/GPU전력 무관)과 정확히 일치.

실증 테스트: `SDL_VIDEODRIVER=wayland`로 강제 전환 → 풀스크린 QHD에서 200fps 이상 회복
확인. **참고**: 이건 UE 패키징이나 프로젝트 코드의 버그가 아니라, 오버라이드가 없을 때
SDL2한테 선택을 통째로 맡기는 구조에서 SDL2 기본 우선순위 자체가 이렇게 생겨먹은 것.

**해결**: UE 엔진이 이미 제공하는 SDL 비디오 드라이버 오버라이드 우선순위(커맨드라인 >
env var > INI > SDL 기본값, `LinuxPlatformApplicationMisc.cpp` 확인)를 그대로 활용 —
패키징 때마다 새로 생성돼서 덮어써지는 실행 스크립트(`titan_example.sh`) 자체를 고치는
방식은 채택하지 않음.
1. **기본값**: `Config/Linux/LinuxEngine.ini`에 `[Linux.SDL] VideoDriver=wayland` 추가(프로젝트
   커밋, 패키징 시 자동 쿡) — `./titan_example.sh`를 그냥 실행해도 기본으로 네이티브
   Wayland로 뜸. 다만 무조건 wayland를 강제하는 정적 설정이라, Wayland 컴포지터가 없는
   순수 X11 머신에서는 SDL 초기화가 실패함(폴백 없음).
2. **X11 전용 환경 대비**: `titan_example_x11_fallback.sh`(프로젝트 루트에 커밋) — 내부에서
   `-sdlvideodriver=x11` 커맨드라인 오버라이드(INI보다 항상 우선)로 wayland 강제를 명시적으로
   뒤집음. LIG처럼 Wayland 없는 구형 환경에 배포할 때 사용. **주의**: UE 패키징이 자동으로
   포함해주는 파일이 아니라서, 패키지 결과물 폴더에 매번 수동으로 같이 복사해서 배포해야 함.

**남은 것**: 순수 X11(Xorg) 세션(로그인 화면에서 "Ubuntu on Xorg")에서도 정상 속도가
나오는지 확인해서 문제가 "Xwayland 번역 계층" 자체인지 "X11 백엔드 자체"인지 한 번 더
구분하는 작업 미착수. 왜 이 NVIDIA 드라이버+Xwayland 조합에서 DRI3 다이렉트 버퍼 공유가
안 되고 소프트웨어 카피로 폴백되는지의 근본 원인은 낮은 우선순위로 보류(우회책으로 실사용엔
문제없음).

**사용한 진단 도구**: `htop`/GNOME System Monitor, `nvidia-smi`/`nvtop`, Unreal Insights
(`-trace=cpu,gpu,frame,bookmark,rendercommands`), `perf top`/`perf record -g`(최종 원인
특정에 결정적), `strace -p <PID> -c/-e trace=.../-k`, `ss -tnp`/`lsof -p <PID> -a -i/-U`.

**관련 파일**: `titan_example/Config/Linux/LinuxEngine.ini`,
`titan_example/titan_example_x11_fallback.sh`, `titan_example/RTSP_Perf_Investigation.md`
(이 조사의 원본 진행 로그, 시행착오 포함).

---

## 2. UGV RTSP 영상 수신 가이드 — 저지연 디코딩 방법 (레퍼런스 문서)

### 목적
UGV(RCWS/CCTV) RTSP 스트림을 낮은 지연으로 수신·디코딩하는 방법을 정리해 통제기(수신 측)
소프트웨어 개발자에게 참고 자료로 공유한다. 통제기 자체의 개발/유지보수는 이 프로젝트
범위가 아니지만, 여러 수신 방식을 직접 실측 비교한 결과를 문서화.

### 결과

**결론**: GStreamer + 하드웨어 디코드(NVIDIA GPU 기준 NVDEC) 조합을 권장. 같은 스트림을
OpenCV/ffmpeg나 libVLC로 받았을 때보다 종단 지연이 **5배 이상 낮음**(수백 ms → 수십 ms).

**스트림 사양**: RTSP, **TCP interleaved만 지원**(UDP 미지원). H.264 High Profile Level
3.2, B프레임 없음(`frameIntervalP=1`, 재정렬 불필요). VUI에 `bitstream_restriction_flag=1`,
`max_num_reorder_frames=0`, `max_dec_frame_buffering=3`으로 "재정렬 대기 불필요"가 스트림
자체에 명시됨. RCWS 1개(1226×928, 최대 60fps) + CCTV 4개(240×135 @ 30fps). Shared media —
여러 클라이언트가 붙어도 서버 부하는 하나만 발생, 라이브(`a=range:npt=now-`)라 seek 불가.

**권장 파이프라인**:
```
gst-launch-1.0 rtspsrc location=rtsp://<host>:8554/ugv/rcws latency=0 drop-on-latency=true protocols=tcp ! \
  rtph264depay ! h264parse ! nvh264dec max-display-delay=0 ! d3d11videosink sync=false
```
핵심 옵션 4가지 — `rtspsrc`의 `latency`(기본 2000ms(!), 모르고 쓰면 매우 느려짐, 로컬/
저지터 환경이면 0으로), `drop-on-latency`(지터버퍼가 latency 넘기면 오래된 패킷 버림),
`nvh264dec`의 `max-display-delay`(기본 -1 auto, 0으로 명시하면 디코더가 프레임을 안 붙잡고
바로 내보냄), sink의 `sync`(저지연이 매끄러운 재생보다 중요하면 false). 전부 GStreamer
문서에 정식으로 노출된 프로퍼티라 "어디서 지연이 생기는지 몰라서 못 고치는" 상황 자체가
잘 안 생기는 게 핵심 장점.

**실측 결과**(로컬호스트, RCWS 스트림, 양쪽 화면에 UTC 타임스탬프를 같이 띄워 동시 캡처):
`latency=0`만 적용 시 149ms → `sink sync=false` 추가 시 ~100ms → `max-display-delay=0`+
`drop-on-latency=true`+서버 쪽 추가 튜닝까지 적용 시 **68ms(30~100ms 변동)**. 원래 대비
85~90% 개선.

**서버 쪽에 이미 반영된 저지연 설정**(참고용): NVENC 인코더 버퍼 최소화
(`nExtraOutputDelay=1`), 튜닝 프리셋 `ULTRA_LOW_LATENCY`, VUI에 재정렬 불필요 명시,
GStreamer 서버(`gst-rtsp-server`) 미디어 latency=0 + rate-control 비활성화.

**하드웨어 요구사항**: `nvh264dec`은 NVIDIA GPU(NVDEC) 필요 — 없으면 Intel은
`qsvh264dec`/`vah264dec`, AMD는 `vah264dec`, 하드웨어 디코드 자체가 없으면 소프트웨어
디코더(`avdec_h264`)도 지터버퍼 제어만으로 cv2/libVLC보다는 나은 결과 기대 가능. 어떤
디코더를 쓰든 `rtspsrc`의 `latency`/`drop-on-latency`와 sink의 `sync=false`는 동일하게
적용 가능 — 가장 효과가 컸던 설정.

**다른 방식 시도 기록(참고, 기각)**:
- **OpenCV(cv2, ffmpeg 백엔드)**: `VideoCapture()` 자체가 포맷 프로빙에 1~3초 소요, 저지연
  옵션(`fflags=nobuffer` 등)을 다 적용해도 **정확히 11프레임(약 367ms) 고정 지연**이
  안 없어짐(원시 소켓 seq 단위 정밀 측정, 편차 0 — 지터가 아니라 고정 파이프라인 깊이).
  SPS에 VUI `bitstream_restriction` 정보가 없어서 디코더가 레벨 기준 보수적 기본 버퍼링을
  적용하고 있던 것이 원인 일부(현재는 서버 쪽 수정됨, 13→11프레임 일부 개선됐지만 전부
  설명 안 됨). cv2/ffmpeg엔 이 잔여 지연을 직접 제어할 노출된 옵션이 없었음.
- **libVLC**: RTSP 처리가 ffmpeg가 아니라 live555라 내부 동작이 더 불투명. 연결 초기
  해상도를 잘못 추측했다가 디코더를 통째로 재시작하는 현상 확인(그 과정에서 200ms 이상
  지연 발생, 재시작 시점 지연이 "새 기준점"으로 고정돼 안 줄어듦). 사후 강제 따라잡기도
  불가능 — `set_rate()`(배속) 호출 시 네이티브 크래시, 스트림이 논시커블이라 seek 자체가
  API 레벨에서 거부됨. 최종적으로 ~400ms대 지연이 거의 그대로 유지됨.
- **핵심 차이**: cv2/libVLC 둘 다 "어딘가에 고정된 버퍼링이 있는 건 확실한데 제어할 옵션이
  없다"는 공통된 벽에 부딪힘. GStreamer는 지연 관련 핵심 요소가 전부 정식 프로퍼티로
  문서화돼 있어 삽질 없이 낮출 수 있었음 — 이게 5배 이상 지연 차이로 이어진 근본 이유.

**요약 권장사항**: ① GStreamer+하드웨어 디코드로 수신 파이프라인 구성, ② `rtspsrc`의
`latency`를 기본값(2000ms)에서 낮추기(가장 큰 영향), ③ 디코더의 프레임 지연 프로퍼티
최소화, ④ Sink `sync` 옵션 검토, ⑤ 서버 SDP VUI 재정렬 불필요 명시를 디코더가 신뢰하는지
확인, ⑥ cv2/libVLC를 불가피하게 써야 한다면 수백 ms대 지연을 감안.

---

## 기타 메모

- 두 문서 모두 이번 주 RTSP 관련 작업(`titan/structure/rtsp/` 폴더로 정리된 지연 최적화/
  통합 트랙) 과정에서 나온 결과물 — 1절은 렌더링 파이프라인 병목, 2절은 통제기 측 수신
  가이드로 각각 별개 주제.
