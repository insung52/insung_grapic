# 작업 내역

> 기간: 2026-07-10 ~ 2026-07-16
> 담당: 황인성

---

## 1. RCWS 자동/수동 조준·발사 메커니즘 구현

### 목적
TitanTruck 및 UGV RCWS(원격제어 무기 스테이션) 기관총에 탄도학 기반 자동 조준,
자동 발사, 수동 조준 가이드, 예광탄 VFX, 피격 데미지 판정을 구현한다.
기존 `URCWSComponent`(팬/틸트/줌/거리계)와 `UTargetDetectionComponent`(바운딩박스
탐지)는 그대로 두고, 신규 `URCWSFireControlComponent`가 이 둘을 참조해서 "어디를
조준하고 쏠지"만 전담한다.

### 진행 계획
1. 안정화(2축 안정기 시뮬레이션), 표적 선정, 탄도 해법 구현
2. 자동 조준 + 수동 조준 가이드 UI 구현
3. 발사체 풀링 및 데미지 파이프라인 연동
4. 조작 모드 시스템, 수동 줌, 탐색 스윕, 예광탄 VFX, 사운드 추가

### 결과

**파일 구조**

```
Source/titan_example/Vehicles/
  RCWSFireControlComponent.h/.cpp   안정화/자동조준/자동사격/발사/모드 전담
  RCWSProjectile.h/.cpp             풀링 발사체 (UProjectileMovementComponent 기반)
  RCWSTypes.h                       ERCWSControlMode, FRCWSStatusData

Source/titan_example/UI/
  AimPointWidget.h/.cpp             탄도 보정 수동 조준 가이드 십자선
```

#### 1) 안정화 (ApplyStabilization)

차체(hull)가 매 틱 회전한 만큼을 역방향으로 상쇄해서 시선이 월드 공간상 고정 방향을
유지하게 한다. `bStabilizationEnabled` 상태와 무관하게 항상 실행되며, 조이스틱/자동조준
입력은 이 안정화 출력 위에 단순히 `AddPanTiltInput`으로 얹히는 구조다.

보정 속도는 `MaxStabilizationCorrectionRateDegPerSec`(기본 60도/초)으로 제한되고, 한
틱에 다 못 따라가면 부족분을 다음 틱에 이월해 점진적으로 따라잡는다. 이 "레이트 제한
체이스" 패턴은 이 프로젝트 전체에서 자동조준 슬루잉, 줌 램프에도 동일하게 재사용된다.

#### 2) 탄도학

씬 스케일 보정: 프로젝트 씬이 `GetDistanceScaleFactor()`(약 1.227배) 축소 상태라
탄속과 씬 중력을 둘 다 같은 비율로 줄여 비행시간과 궤적 비율을 실제와 동일하게
유지한다. 투사체 자신의 `ProjectileGravityScale`만 건드리고 월드 전역 중력은 절대
변경하지 않는다(UGV 물리가 실제 중력에 의존하기 때문).

사격 해법(`SolveBallisticElevationDegrees`): 저각(직사) 해법만 사용. 판별식이 음수면
(사거리 밖) 직선 시선각으로 폴백. 리드(예측 사격)는 현재 위치로 1차 풀어서 비행시간
추정, 그 시간 * 속도로 미래 위치 예측, 재풀이(1회 반복)하는 방식.

#### 3) 자동 조준 (UpdateAutoAim)

`SelectNearestEnemyTarget()`으로 `Faction == Enemy` 중 시선 위치 기준 최단거리 표적
하나를 고른다. `MaxAutoAimSlewRateDegPerSec`(기본 45도/초)로 레이트 제한된 슬루잉으로
포탑을 돌리고, 오차가 `AutoAimLockToleranceDegrees`(기본 1도) 이내가 되면 `bIsLockedOn =
true`(자동 발사의 게이트). 표적이 없을 때는 탐색 스윕으로 폴백한다.

#### 4) 수동 조준 가이드 (AimPointWidget)

현재 조작 모드와 무관하게 항상 계산된다. `TargetDetectionComponent::ProjectWorldPointToCameraUV`
(원래 private를 public static으로 승격 재사용)로 탄도 보정된 조준 방향을 RCWS 시야
스크린 UV로 변환해 `SAimPointMarker : SLeafWidget`이 십자선을 그린다.
`SDetectionOverlay` / `SLineGraph` / `SCompass`와 동일한 "Slate 커스텀 페인트를 UWidget
으로 감싸는" 패턴 재사용.

#### 5) 발사체 풀링 (ARCWSProjectile)

1200rpm(초당 20발) 기준 spawn/destroy 낭비를 막기 위해 `ProjectilePoolSize`(기본 64개)
풀링 적용. 발사 시 숨김 해제 + 콜리전 활성화, 명중 또는 `MaxFlightTimeSeconds`(5초)
초과 시 비활성화(스폰/파괴는 세션 내 1회).

데미지는 `UGameplayStatics::ApplyPointDamage`로 표준 파이프라인을 사용한다. `BP_Enemy`에
이미 `Event AnyDamage` 그래프가 있어 코드 수정 없이 자동 반응한다.

무력화 처리(`SetIncapacitated`): 사망한 대상이 `UDetectableTargetSubsystem`에서 자기
자신을 언레지스터해서 모든 스캐너의 탐지 목록에서 진짜로 사라진다. 자동조준은 다음 틱에
자연스럽게 다음 근접 대상으로 넘어간다.

> **트러블슈팅 - 발사 후 씬 캡처 화면들이 검게 깜빡임**
> `ARCWSProjectile::bDrawDebugTrail`(매 틱 디버그 스피어 draw)이 기본값 true였는데,
> 카메라 렌즈 앞을 지나갈 때 오토 익스포저가 반응하는 것으로 추정. 기본값을 false로
> 변경해서 해결. 이전 세션에서도 DrawDebugSphere 관련 씬 캡처 버그를 겪은 적이 있어
> 오토 익스포저 설정 자체를 건드리는 것은 보류함.

> **트러블슈팅 - 투사체끼리 충돌해서 총구 앞에 피격 이펙트**
> `CollisionComponent` 오브젝트 타입이 `ECC_WorldDynamic`이고 응답을 "전체 Block +
> Visibility/Camera만 Ignore"로 설정해서 WorldDynamic 채널이 Block인 채였음. 투사체끼리
> 서로 막고 있었던 것. `SetCollisionResponseToChannel(ECC_WorldDynamic, ECR_Ignore)` 추가.

> **트러블슈팅 - 148개 NavModifierVolume과 발사체 충돌**
> Custom 콜리전 프로파일에서 명시 안 된 채널은 기본값 Block으로 남는다는 함정. 148개
> 볼륨 전부에 `WorldDynamic: ECR_Ignore`를 MCP 배치 스크립트로 추가. PIE 도중 실행하면
> 경로가 임시 UEDPIE 월드로 풀려서 다수 실패하므로 Play 정지 후 재실행 필요.

#### 6) 조작 모드 시스템 (ERCWSControlMode)

초기 설계는 단일 축 4단계(수동/추적/조준/자동사격) 선형 순환이었으나, 두 개의 독립된
축으로 분리했다.

| 모드 | 동작 | 표시 색상 |
|---|---|---|
| Remote | 조이스틱 수동 조작 | 흰색 |
| AutoSurveillance | 탐색 스윕 전용, 적 감지돼도 조준 안 함 | 하늘색 (#17B9FF) |
| AutoAim | 자동 조준 + 거리비례 줌인, 발사는 안 함 | 노란색 |
| AutoFire | 자동 조준 + 락온 시 자동 발사 | 빨간색 |

조이스틱이 떠난 RCWS가 Remote였으면 AutoSurveillance로 전환, 조이스틱이 새로 향한
RCWS는 무조건 Remote로 전환(`SyncRCWSControlModeForCameraTarget`). 이를 통해 "조이스틱을
갖지 않은 RCWS가 탐색이라도 하고 있는" 상태를 자동으로 유지한다.

#### 7) 수동 줌 6단계 - 로그 공간 램프

```cpp
TArray<float> ManualZoomLevels = { 0.5f, 1.f, 2.f, 4.f, 8.f, 16.f };
```

인접 단계가 정확히 2배씩이라 log2 공간에서 등간격(-1,0,1,2,3,4). 매 틱 현재 실제 값에서
목표 log2 값으로 레이트 제한 체이스하는 방식으로 상태를 저장하지 않아 연타로 목표가
몇 번 바뀌어도 항상 "그 순간의 실제 값"에서 이어진다.

> **트러블슈팅 - 줌 버튼 연타 시 배율 버벅거림**
> 초기 구현은 "시작값/경과시간/총소요시간 저장 후 Lerp" 방식이었는데, 연타로 목표가
> 바뀔 때마다 총소요시간이 재계산되며 방금 보여주던 값과 Alpha가 어긋나 앞뒤로 튀었음.
> 현재 방식(로그 공간 레이트 제한 체이스)으로 재작성해서 해결.

> **트러블슈팅 - 0.5배율에서 화면 암전**
> `CameraFOV`(90도) / `MinZoomLevel`(0.5) = 정확히 180도. 원근 투영 행렬이 특이점에
> 걸려 렌더링이 깨지는 현상. `SetZoomLevel` 안에서 `FMath::Min(CameraFOV / ZoomLevel, 170.f)` 클램프 추가.

#### 8) 예광탄 VFX

모든 탄에 작은 실린더 메시(`NoCollision`)를 붙이고 `M_RCWSRound` 머티리얼의
`EmissiveColor` VectorParameter 밝기만 교체해서 일반탄/예광탄을 구분한다(`TracerInterval`
기본 4 = 일반탄 3발 + 예광탄 1발 반복). 예광탄일 때만 Niagara 리본 트레일
(`NS_bullet`, 사용자가 직접 받아온 외부 애셋)을 `Activate/Deactivate`로 켜고 끈다.

> **트러블슈팅 - 예광탄이 안 보임**
> 원래 쓰던 `NS_Jump_Trail` 복제본이 근거리 FPS 스케일용이라 RCWS 교전거리(수백m)에서는
> 사실상 서브픽셀이었음. `TracerTrailScale = FVector(4,4,4)`를 추가해 Niagara 그래프를
> 건드리지 않고도 크기를 조정 가능하게 함. 이후 사용자가 NS_bullet 자체에서도 직접 크기
> 조정해서 해결. 실제 원인은 별도로 Niagara 모듈 미등록(빌드 실패)과 투사체 자기충돌(9.8절)
> 도 겹쳐 있었음.

> **트러블슈팅 - Niagara 모듈 누락 빌드 실패**
> `NS_bullet` 연동 시 `NiagaraComponent.h` 관련 컴파일 에러. Niagara 플러그인 애셋은
> 이미 프로젝트에 있었으나 `titan_example.Build.cs`에 `"Niagara"` 모듈 의존성이 없었음.
> 추가 후 정상 빌드(`.Build.cs` 변경은 Live Coding 적용 안 됨, 전체 리빌드 필요).

#### 9) 사운드 거리 감쇠

`SpawnSoundAtLocation` 후 `AdjustAttenuation()` 호출 방식은 효과가 없었다. 이 함수 내부
에서 `Play()`를 호출해 "어테뉴에이션 적용 여부"를 확정한 뒤 리턴하기 때문에, 이후
`AdjustAttenuation()`은 플래그를 바꾸지 못한다. 발사 직전에 `NewObject<USoundAttenuation>`
으로 런타임 애셋을 생성해서 `SpawnSoundAtLocation`의 파라미터로 직접 전달하는 방식으로
수정.

---

## 2. 모니터 1/2 UI - Figma 디자인 시안 기반 WBP 전면 구현 및 값 연동

### 목적
Figma PNG 시안(모니터1: 군용 자체방호 통제기 모의기, 모니터2: RCWS 뷰어)을 기준으로
`WBP_kadex` 단일 캔버스에 두 모니터를 전부 구현하고, 헤더/메인뷰/4분할캠/미니맵/UAV
상태정보/UAV 영상피드/RCWS 뷰어의 모든 위젯을 실제 값에 연동한다. `BindWidgetOptional`
패턴으로 이름만 맞추면 WBP 그래프 작업 없이 C++이 자동으로 값을 채워준다.

### 진행 계획
1. `UMonitor1Widget` 신규 클래스 설계 및 BindWidgetOptional 이름 목록 확정
2. 방위각/고각/줌 눈금 리본(`ScrollingRulerWidget`, `CompactHeadingRibbonWidget`) 구현
3. 미니맵 오버레이(`VehicleMarkerWidget`, `RadialAreaWidget`, `PolylineWidget`) 구현
4. `LineGraphWidget` 그라데이션 품질 개선
5. RCWS 3D 프리뷰 액터 + 크로마키 투명 배경 구현
6. 발견된 버그 수정 (방위각 갱신 타이밍, DetectionOverlayWidget 이름 불일치, 선 두께 등)

### 결과

**신규 파일 구조**

```
Source/titan_example/UI/
  Monitor1Widget.h/.cpp          모니터1+2 전부 포함 (Monitor2Widget 흡수됨)
  ScrollingRulerWidget.h/.cpp    방위각/고각/줌 눈금 리본 (4곳 공용)
  CompactHeadingRibbonWidget.h/.cpp  4분할캠 미니 방위 리본 (별도 클래스)
  VehicleMarkerWidget.h/.cpp     미니맵 삼각형 차량 마커
  RadialAreaWidget.h/.cpp        미니맵 FOV 부채꼴 / 적 예상 위치 원
  PolylineWidget.h/.cpp          미니맵 경로선 (실선/점선)
  RCWSPreviewActor.h/.cpp        RCWS 3D 모델 디오라마 씬 캡처 액터
```

#### 1) Monitor1Widget / WBP 구조

처음엔 `Monitor1Widget` / `Monitor2Widget` 두 클래스로 나눠 `WBP_kadex` /
`WBP_kadex1` 두 WBP를 `SetAnchorsInViewport`로 좌/우 절반씩 분할하는 방식이었으나,
창 크기에 따라 분할이 어긋나는 문제로 3840 폭 단일 캔버스(`WBP_kadex`) 방식으로
되돌렸다. `UMonitor2Widget` 클래스는 `UMonitor1Widget`에 흡수해 삭제했다.

모니터2(RCWS 뷰어)가 보여줄 RCWS는 `GetCameraControlTarget()`(TruckRCWS / UGVRCWS)
기준으로 자동 전환된다(기본값 UGV).

`BindWidgetOptional`은 이름이 틀리거나 타입이 달라도 에러 없이 그 항목만 조용히
스킵한다. "값이 안 뜨면 이름부터 확인"이 이 패턴의 유일한 디버깅 절차다.

**주요 BindWidgetOptional 이름 목록 (발췌)**

| 위젯 이름 | 타입 | 비고 |
|---|---|---|
| MainViewImage | Image | 트럭 RCWS 화면 (임시 대체) |
| MainViewDetectionOverlay | DetectionOverlayWidget | MainViewImage와 동일 위치/크기로 겹침 |
| TruckFrontRibbon~RightRibbon | CompactHeadingRibbonWidget | 4분할캠 미니 방위 리본 |
| MinimapImage | Image | |
| MinimapTruckMarker / MinimapUGVMarker / MinimapUAVMarker | VehicleMarkerWidget | Canvas Panel 직계 자식으로 배치 필수 |
| MinimapTruckFOVCone / MinimapUGVFOVCone / MinimapUAVFOVCone | RadialAreaWidget | SpanDegrees는 코드가 매 갱신마다 덮어씀 |
| MinimapUGVPath | PolylineWidget | bDashed=false |
| MinimapUAVRoute | PolylineWidget | bDashed=true |
| UAVAltitudeText / UAVAltitudeGraph | TextBlock / LineGraphWidget | |
| UAVCompassImage | Image (T_compass) | 이름만 맞추면 SetRenderTransformAngle로 자동 회전 |
| UAVSignalBar1~4 | Image (T_Rectangle 계열) | 켜진 바는 원본 그대로, 꺼진 바만 M_IconFlatTint MID |
| RCWSViewImage | Image | |
| RCWSDetectionOverlay | DetectionOverlayWidget | RCWSViewImage와 동일 위치/크기로 겹침 |
| AzimuthRibbon | ScrollingRulerWidget | bWrapMode=true, MajorTickInterval=30 |
| ElevationRibbon | ScrollingRulerWidget | Vertical, bWrapMode=false, MinValue=-20, MaxValue=20 |
| ZoomRibbon | ScrollingRulerWidget | ValueBreakpoints=[0.5,1,2.5,5,10], bInvertAxis=true |
| AmmoText | TextBlock | 형식 475 (최댓값 없이 현재 탄약만) |
| RCWSPreviewImage | Image | M_ChromaKey MID 자동 적용 |
| RCWSPreviewReflectionImage | Image | RCWSPreviewImage와 동일 MID 공유, WBP에서 Y스케일 -1 / Opacity 0.2 설정 |

#### 2) 눈금 리본 - ScrollingRulerWidget / CompactHeadingRibbonWidget

**ScrollingRulerWidget**: 방위각(wrap 360), 고각(non-wrap -20~20), 줌(비선형
5단계 브레이크포인트), UAV 영상피드 리본 4곳을 하나의 클래스로 커버. 현재값이
항상 화면 중앙 고정 포인터에 오고 눈금이 그 아래로 흘러간다. 주요 파라미터:

- `bWrapMode`: true면 값이 360에서 0으로 순환, false면 MinValue/MaxValue 밖 눈금 미출력
- `ValueBreakpoints`: 비선형 줌 눈금 전용. 배열을 채우면 인덱스 공간 균일 배치, 라벨만 실제 값으로 역변환
- `bShowMidTicks` / `bShowSpine` / `bMirrored`: EL/ZOOM용 세부 스타일 분기
- `MajorTickOpacity` / `MidTickOpacity` / `MinorTickOpacity`: 단계별 투명도 차등
- `LabelDecimalPlaces` / `LabelSuffix`: ZOOM 리본만 "2.5x" 형식으로 출력
- `TickLabelGap`: Major 눈금 끝~라벨 사이 간격

**CompactHeadingRibbonWidget**: 4분할캠 미니 리본 전용. 현재값에 가장 가까운 8방위
문자만 `HighlightColor`(흰색), 나머지는 `DimColor`(회색 반투명)로 이진 색상 전환.
ScrollingRulerWidget은 라벨이 눈금 아래에 따로 있는 구조라 "한 줄 같은 베이스라인"이
필요한 이 자리와 안 맞아서 별도 클래스로 분리.

> **트러블슈팅 - 방위각 리본이 UGV 조향에도 안 움직임**
> `CurrentData.AzimuthDegrees`가 `AddPanTiltInput` 호출 시점에만 갱신되고 있었음.
> RCWS 마운트는 차체에 상대 회전으로 붙어있어서 차체가 조향으로 회전하면 카메라 월드
> 방위도 자동으로 바뀌는데, 조이스틱 입력이 없으면 `AddPanTiltInput`이 호출 안 되니
> 방위각이 마지막 조이스틱 입력 시점에 멈춰있었음. `RefreshAzimuthElevation()`으로
> 분리해서 `TickComponent`에서 매 틱 호출하도록 변경.

> **트러블슈팅 - 리본이 끊겨 보임**
> ScrollingRulerWidget 자체는 매 프레임 다시 그려지는 Slate 커스텀 페인트라 FPS 제한이
> 없음. 끊겨 보인 진짜 원인은 `Monitor1Widget`이 값을 `RefreshIntervalSeconds`(0.2초)마다
> 만 업데이트하고 있었던 것. `SetCurrentValue()` 호출만 `RefreshRibbonValues()`로 분리해서
> 매 프레임(스로틀 없이) 실행하도록 변경.

> **트러블슈팅 - WBP 디자이너에서 확대해도 선 두께가 그대로**
> `FSlateDrawElement::MakeLines`의 두께 파라미터는 의도적으로 스크린 픽셀 단위로 해석되고
> 내부적으로 `/ DrawElement.GetScale()`이 적용되어 줌 무관하게 항상 화면상 N픽셀이 됨.
> 두께 값에 `AllottedGeometry.Scale`을 미리 곱해서 넘기면 내부 나눗셈이 상쇄되어 다른
> 요소들처럼 줌에 비례해서 커짐. 이 프로젝트의 선을 그리는 커스텀 페인트 위젯 전부
> (ScrollingRulerWidget / CompactHeadingRibbonWidget / CompassWidget / RadialGaugeWidget /
> VehicleMarkerWidget / RadialAreaWidget / PolylineWidget / DetectionOverlayWidget /
> LineGraphWidget) 에 동일하게 적용.

> **트러블슈팅 - WBP Details 패널 값 변경 후 컴파일 전에는 반영 안 됨**
> `RebuildWidget()`은 위젯이 처음 만들어질 때(컴파일 시점)만 호출된다. 기본 위젯
> (Image, TextBlock 등)은 `SynchronizeProperties()`가 `PostEditChangeProperty` 때 자동
> 호출되어 실시간 반영됨. 위 9개 커스텀 페인트 위젯 전부에 `SynchronizeProperties()`를
> 추가해서 Details 패널 값 변경이 컴파일 없이 WBP 캔버스에 바로 반영되도록 수정.

#### 3) 미니맵 오버레이 위젯

- `VehicleMarkerWidget`: 삼각형 단색 마커. `MarkerColor`만 지정, 회전은 WBP
  `RenderTransform Angle`에 헤딩값 바인딩. Canvas Panel 직계 자식으로 배치해야
  `CanvasPanelSlot::SetPosition`으로 매 틱 위치를 갱신할 수 있다.
- `RadialAreaWidget`: `SpanDegrees=360`이면 꽉 찬 원(적 예상 위치 반투명 빨간 원),
  360보다 작으면 그 각도만큼의 부채꼴(RCWS/UAV FOV 콘). `FacingDegrees`만 갱신하면
  됨. FOV콘의 `SpanDegrees`는 `URCWSComponent::GetCurrentFOVDegrees()`(`SightCamera->
  FOVAngle`을 그대로 읽음) / `AUAVPawn::GetCurrentFOVDegrees()`로 매 갱신마다 실시간
  연동(WBP에서 수동 설정 불필요).
- `PolylineWidget`: 정규화 UV 포인트 배열을 이어 그림. `bDashed=true/false`로 실선
  (UGV 이동 경로) / 점선(UAV WP1->WP2) 둘 다 커버.

UGV 마커 방향 버그: `GetActorRotation().Yaw`를 그대로 쓰면 반대 방향 표시. UGV 메시가
-X를 정면으로 제작되어 있어서(UGVMovementComponent.cpp가 `GetForwardVector()`를 4곳에서
negate하던 것과 동일한 이유). `(-GetActorForwardVector()).Rotation().Yaw`로 수정.

#### 4) LineGraphWidget 그라데이션 품질 개선

초기 구현은 얇은 사각형 여러 겹(밴딩) 방식이었다. `FSlateDrawElement::MakeCustomVerts`
기반 정점 그라데이션으로 교체 - 곡선 위쪽 정점은 선 색상, 아래쪽(바닥)은 투명으로
설정하면 GPU가 알아서 보간해서 밴딩이 없어진다. 그라데이션 알파 기준도 "곡선~바닥
상대값"에서 "위젯 높이 기준 절대값"으로 수정 - 값이 낮을 때도 알파가 항상 최댓값이라
얇은 띠에 색이 진하게 뭉쳐 보이던 문제 해결.

히스토리 샘플링도 초당 1회(1분 창)에서 초당 30회(10초 창)로 변경. `UIUpdateIntervalSeconds`도
`1/30`으로 맞추지 않으면 히스토리는 30fps로 쌓여도 화면에 넘겨주는 시점이 느려서
여전히 끊겨 보임.

#### 5) 나침반 - 텍스처 오버레이 방식

`CompassWidget`(손그림 Slate) 대신 디자인팀이 준 `T_compass`(회전 레이어) /
`T_compass_outer`(고정 테두리) 텍스처 2장 오버레이를 사용한다.

> **트러블슈팅 - Property Binding으로 Render Transform > Angle 연결이 안 됨**
> `FWidgetTransform`은 구조체 프로퍼티라 그 서브필드(`Angle`)는 Property Binding을
> 지원하지 않고 Sequencer 키프레임만 지원한다(엔진 소스 `WidgetTransform.h` 확인).
> Details 패널에 바인딩 동그라미 아이콘이 아예 없는 게 정상. `UAVCompassImage` 이름만
> 맞추면 C++에서 `SetRenderTransformAngle(HeadingDegrees - 45.f)`를 매 프레임
> `RefreshSmoothValues()`에서 직접 호출해서 회전시킨다(텍스처 기본 방향이 NE=45도
> 오프셋 필요).

#### 6) RCWS 3D 모델 시각화 - RCWSPreviewActor

실제 차량이 있는 씬과 완전히 분리된 위치(레벨 Z ~ -6000)에 포탑 메시 복제본 하나 +
고정 앵글 씬 캡처 하나만 있는 "디오라마" 씬을 별도로 구성. `Tick`마다 활성 RCWS의
마운트 `GetRelativeRotation()`을 복제본에 그대로 반영해서 차체 기준 상대각으로 포탑
방향을 보여준다(월드 기준이 아닌 차체 기준을 쓰는 이유: 디오라마에는 움직이는 차체가
없으므로 월드 기준값을 먹이면 주행 중 포탑이 이유 없이 빙빙 돌아 보임).

배경은 크로마키 방식 투명 배경(진짜 알파 전파는 프로젝트 전역 토글이라 Lumen/TSR 계획과
충돌 리스크 있어 불채택):
- 배경 구체에 `M_ChromaKeyBackdrop`(단색 마젠타 Unlit, TwoSided=true)
- `RCWSPreviewImage` UImage에 `M_ChromaKey` MID 적용 - KeyColor 근처 픽셀의 Opacity를
  SmoothStep으로 0으로 만들어 투명하게 처리

> **트러블슈팅 - 디오라마 배경 Unlit 이미시브가 검게 찌부러짐**
> 씬 캡처의 오토 익스포저가 조명을 받는 포탑 메시(밝음)에 맞춰 자동 보정되면서 상대적으로
> 은은한 이미시브 배경이 통째로 검게 압축됨. `Capture->ShowFlags.SetEyeAdaptation(false)`
> + `PostProcessSettings.AutoExposureMethod=AEM_Manual`로 해결.

> **트러블슈팅 - 그라데이션 배경이 아닌 두 색이 딱 잘린 것처럼 보임**
> 초기 버전에서 그라데이션 정규화 기준을 수동 cm 값(`GradientHeight=500`)으로 나눴는데,
> 구 실제 스케일이 반지름 1500cm인 경우 500cm 구간만 그라데이션이 되고 나머지는 단색으로
> 클램프됨. `ObjectRadius` 노드(메시 바운딩 구 반지름 자동 읽기)로 교체해서 스케일과
> 무관하게 항상 최상단~최하단 전체가 그라데이션되도록 수정(최종적으로 크로마키로 전환해
> 이 배경은 폐기).

#### 7) DetectionOverlayWidget 이름 불일치 / CornerRadius

> **트러블슈팅 - DetectionOverlayWidget 배선 후에도 바운딩 박스가 안 그려짐**
> `WBP_kadex`가 이전에 `MissionDashboardWidget`을 부모로 만들어졌을 때 위젯 이름을
> `TruckRCWSDetectionOverlay`로 지었는데, 부모를 `Monitor1Widget`으로 바꾼 뒤에도 이름이
> 그대로 남아있었음. `Monitor1Widget`이 기대하는 `MainViewDetectionOverlay`와 달라서
> `BindWidgetOptional`이 에러 없이 조용히 실패하고 있었던 것. WBP에서 이름만 바꿔서
> 해결. 예전 이름 - 현재 이름 매핑: `TruckRCWSDetectionOverlay` -> `MainViewDetectionOverlay`,
> `UGVRCWSImage` -> `RCWSViewImage` 등.

`CornerRadius`(float, 기본 0) 프로퍼티 추가: Slate 레벨 클리핑은 라운드 모서리를
지원하지 않으므로(FSlateClippingZone은 quad만 지원), 박스 외곽선 각 변을 12구간으로
나눠 모서리 경계 밖으로 벗어나는 점을 원호 위로 끌어당기는 방식으로 구현
(`BuildRoundedBoxOutline` / `ClampToRoundedRect`). CornerRadius=0이면 기존과 동일.

#### 8) 신호세기 바 자동 연동 및 기타

`UAVSignalBar1~4`(`T_Rectangle` 계열 Image): 켜진 바는 원본 그대로(그라데이션 PNG는
사실 완전 불투명 RGB 그라데이션이라 틴트 방식으로는 회색 구현이 안 됨), 꺼진 바만
`M_IconFlatTint` MID로 교체해서 단색 어두운 회색으로 처리. 신호 강도는 `SignalDbm`
에서 매 틱 파생(`-100~-50dBm -> 0~100%`)하여 숫자와 막대가 항상 일치.

씬 축척 불일치(Scale ~1.227) 관련 거리/속도/RNG/미니맵 스케일바 5곳이 raw `/100` 변환만
쓰고 있어 위경도(보정 적용)와 기준이 안 맞음을 확인. 나중에 `GetDistanceScaleFactor()`
헬퍼 하나 추가하고 5곳에 곱해주면 되는 단순 수정이라 기록만 해두고 미수정.

FOV 기본값 조정: `URCWSComponent::CameraFOV` 40 -> 90, `AUAVPawn::CameraFOV` 60 -> 120
(1배율 시야각이 좁아 보인다는 피드백 반영, EditAnywhere라 에디터에서 자유롭게 재조정 가능).

---

## 3. M1A2 탱크 에셋에 기존 UGV 기능 연동 (BP_UGVFromTank)

### 목적
마켓플레이스 M1A2 탱크 애셋(`BP_M1A2` / `BP_Tank` / `BP_VehicleBase` 상속 체인)을
복제해서, 기존 `AUGVPawn`(C++ 리지드바디 방식)에 구현된 수동/자동 주행, AI Pure Pursuit,
NavMesh 도로 선호, 오프로드 감속, RCWS 자동조준/발사, WBP 대시보드 연동 기능을 Chaos
Vehicle 기반 탱크 에셋에 적용한다.

### 진행 계획
1. M1A2 복제 및 기본 물리/구동 연동
2. Possess 없이 물리 활성화되는 AI 컨트롤러 설계
3. AI Pure Pursuit 경로 추종, 오프로드/도로 경계 처리
4. RCWS/포탑 연동 및 WBP 대시보드 일반화

### 결과

**복제 배경**

처음에는 블렌더로 UGV 전용 스켈레탈 메시를 처음부터 만들고 C++ Chaos Wheeled Vehicle로
직접 구성하려 했으나, 본 축 컨벤션 문제, Root Body 분리 시 75cm 오프셋 버그, 바퀴 16개
SuspensionState 붕괴 버그 등으로 극도로 오래 걸리는 디버깅 루프에 빠졌다. 이미 검증된
M1A2 Chaos Vehicle Blueprint를 복제해서 불필요한 기능만 제거하는 방식으로 전환했다.

**기본 구성**

- `BP_UGVFromTank`: `BP_M1A2` 복제, 원본 건드리지 않음
- `BP_UGVWheel_FromTank`: `BP_TankWheel_Chaos_M1A2` 복제, 독립 튜닝 가능
- `VehicleMovementComp.Mass`: 30000 -> 3000(실제 UGV 스케일)

**Possess / 물리 활성화 디버깅 (가장 오래 걸린 부분)**

> **트러블슈팅 - AI 컨트롤러가 possess해도 물리 시뮬레이션이 절대 켜지지 않음**
>
> `BP_VehicleBase::ReceivePossessed` 이벤트가 `CastToPlayerController` 성공 시에만
> `SetSimulatePhysics(true)` 체인을 실행하는 구조였다. 즉 진짜 `APlayerController`가
> possess해야만 물리가 켜짐.
>
> 시도한 방법들:
> - `VehiclePossessed()` 리플렉션 직접 호출 → 불충분
> - 물리 활성화 네이티브 함수를 AI 컨트롤러에서 직접 호출 → `ResetVehicle()`이 매 틱
>   `RecreatePhysicsState()`를 호출하는 루프에 빠짐
> - `AUGVAIController`를 `AAIController` 대신 `APlayerController`로 재작성 → 여전히 실패
>
> 진짜 원인: `APlayerController::IsLocalController()`가 `ULocalPlayer`가 없으면(헤드리스
> 봇은 항상 없음) false를 리턴하고, Chaos Vehicle 물리가 이 값에 의존.
>
> 최종 수정: `AUGVAIController::IsLocalController()`를 오버라이드해서 무조건 true 리턴.
> 싱글플레이/논네트워크 게임이라 안전한 최소 수정.

결과적으로 `AUGVAIController`는 실제 Player/뷰포트 없이 존재하는 "헤드리스 PlayerController"
로 동작한다. `AutoPossessAI` / `AIControllerClass` 설정은 엔진이 타입을 강제하지 않아
그대로 작동.

**구동 로직**

Chaos Vehicle에는 `RequestDirectMove`(AI 이동 훅)가 없어(엔진 소스 확인), 이름 기반
리플렉션으로 대체했다. `SetManualControl(float ForwardInput, float TurnInput)`이라는
이름의 UFUNCTION을 가진 폰이면 `FindFunction + ProcessEvent`로 호출한다.

후진 버그: `SetThrottleInput`에 음수를 넣으면 내부에서 0~1로 클램프된다. 정답은
`bReverseAsBrake=true` 설정에서 음수 입력은 `SetBrakeInput`으로 분리 전달해야 엔진이
자동으로 후진 기어로 전환.

**AI 자동주행 - Pure Pursuit 포팅**

`APlayerController` 기반으로 바뀌면서 `MoveToLocation` / `PathFollowingComponent` 등
`AAIController` 전용 API를 잃었다. `UNavigationSystemV1::FindPathToLocationSynchronously`로
직접 경로 계산(동기, 즉시 전체 경로 확보). Pure Pursuit 조향/스로틀 로직은 기존
`UUGVMovementComponent::FindLookaheadTarget`을 거의 그대로 포팅(`UpdateChaosPursuit` /
`FindChaosLookaheadTarget`).

**조향 토크 - 진짜 조향력의 정체**

사용자가 `MaxTurningSpeed` / `YawInputRate`를 올려도 조향이 빨라지지 않는 문제가 있었다.
엔진 소스(`ChaosVehicleMovementComponent.cpp`)를 추적한 결과:

```cpp
TotalTorque += VehicleUpAxis * YawInput * TorqueControl.YawTorqueScaling;
AddTorqueInRadians(TotalTorque, true, true);
```

실제 회전력은 `TorqueControl.YawTorqueScaling`이 만들며, 엔진 기본값이 0 / false다.
`MaxTurningSpeed` / `YawInputRate`는 입력 램프업 속도만 조절하고 토크 크기와 무관하다.
`YawTorqueScaling`을 기존 3에서 20으로 올려서 조향 반응성 개선.

**오프로드 감속 + 도로 이탈 강제 복귀**

기존 `AUGVPawn`에 있던 두 기능을 `AUGVAIController`로 포팅:

- 오프로드 감속: `RoadSurfaceTag` 태그 컴포넌트와의 오버랩 체크(단순화된 1점 체크,
  Chaos엔 트랙별 힘 주입 개념 없음). 도로 위가 아니면 매 틱 전진 속도의 1%
  (`OffRoadSpeedDecayPerTick`)를 직접 깎음.
- 도로 이탈 강제 복귀: `RoadNavMod` 볼륨들을 `BeginPlay`에서 캐싱, 매 틱 최근접점
  거리 계산. `MaxRoadDistance`(기본 400m) 초과 시 그 지점 지면으로 텔레포트 + 속도/각속도
  리셋.

**RCWS/포탑 연동**

M1A2의 기존 `AC_WeaponTank` 컴포넌트(포탑/포신 조준 시스템)를 그대로 재사용한다.
`TargetPoint`(월드 좌표 조준 목표)를 매 틱 RCWS 시선 방향 기준으로 계산해서
`AC_WeaponTank->SetTargetPoint()`에 넣으면 `TurretGunRotation()`이 알아서 부드럽게
회전시킨다.

`BP_UGVFromTank`에 신규 추가한 RCWS 관련 컴포넌트들은 전부 `FComponentReference`
이름 기반 설계라 C++ 수정 없이 이름만 맞춰 추가하면 바로 작동한다.

자동발사 게이팅 개선: 기존엔 RCWS 마운트(카메라) 명령 방향만으로 `bIsLockedOn`을
판정했다. 이제 `GetMuzzleWorldRotation()`(실제 포신 소켓 현재 트랜스폼)을 추가로
확인, `AutoFireTurretAlignmentToleranceDeg`(기본 2도) 이내일 때만 발사한다.

반동을 실제 포탑에도 적용: 발사 시 같은 랜덤 킥 값을 `AC_WeaponTank`의 내부 배열
변수(`TurretRotCur[0]` / `GunRotCur[0]`)에 리플렉션으로 직접 적용. 다음 프레임부터
`RInterpTo_Constant`가 원래 목표로 되돌리면서 킥+복귀 효과가 별도 로직 없이 구현됨.

> **트러블슈팅 - 트랙/서스펜션 애니메이션이 멈춤**
> RCWS 작업에서 `BP_UGVFromTank`에 `Event Tick`을 새로 만들면서, 부모(`BP_VehicleBase`)의
> 기존 `Event Tick`(트랙/서스펜션 로직 전부 담당)을 덮어써버렸다. 블루프린트는 자식이
> `Event Tick`을 새로 만들면 "Parent: Event Tick" 노드를 명시적으로 넣지 않는 한 부모
> 로직이 자동 이어지지 않는다(C++ 가상함수와 다름). MCP 툴로 "Parent:" 노드를 생성할
> 수 없어서, 우리 Tick 체인 맨 앞에 `SetDeltaSeconds(DeltaSeconds)` -> `VehicleTick()`
> 호출을 직접 추가하는 방식으로 해결.

**WBP 대시보드 일반화**

`Monitor1Widget`이 기존 `AUGVPawn` 하드코딩 구조였던 것을 `BP_UGVFromTank`용
`UGVTankRef`를 추가하고, RCWS / TargetDetection / FireControl / UGVStatus 전부 "UGVRef
없으면 UGVTankRef에서 `FindComponentByClass`로 대신 찾기" 패턴으로 일반화했다.
RCWS 조준 화면, 모드 텍스트, 줌 리본, 조준점 마커가 모두 자동 연동된다.

실제 포탑이 현재 보는 방향 표시: `GetMuzzleWorldRotation()` 기준 먼 지점을 RCWS 시야
카메라 화면 좌표로 투영해서 WBP에 이미 배치된 크로스헤어 PNG를 매 프레임 이동시킨다
(`RCWSTurretReticleImage`, `BindWidgetOptional`, 언스로틀 `RefreshSmoothValues()`에 배치).
