# 작업 내역

> 기간: 2026-07-31 ~ 2026-08-06
> 담당: 황인성

---

## 1. RCWS 이펙트 Niagara 전환 + 사운드 시스템 구현

### 목적
RCWS 발사/피격 이펙트를 Cascade와 혼재된 상태에서 Niagara 전용으로 정리하고, TitanTruck/
UGV/UAV 3개 시점을 오가며 운용하는 상황에 맞는 오디오 청취 포커스 시스템을 새로 설계,
UGV 엔진음/UAV 프로펠러음/병력 발소리까지 MetaSound 레이어 크로스페이드로 구현한다.

### 결과

**이펙트 Niagara 전환**: `URCWSFireControlComponent::MuzzleFlashEffect`(Cascade) 삭제,
`MuzzleFlashNiagaraEffect` 하나만 유지. `ARCWSProjectile::ImpactEffect`/`EnemyImpactEffect`
타입을 `UParticleSystem*` → `UNiagaraSystem*`로 변경. 기존 `USoundBase*` 사운드 프로퍼티는
`UMetaSoundSource`도 같은 상속 계열이라 코드 수정 없이 MetaSound 그대로 할당 가능함을 확인.

**오디오 청취 포커스 시스템**: 3개 차량이 물리적으로 떨어져 있어 오디오는 항상 리스너(귀)
위치 기준 계산되는데, 요구사항은 "선택한 차량 위치로 리스너 자체를 옮기는 것"(개별 사운드
뮤트가 아님). 기존 `ECameraControlTarget`(조이스틱 조작 대상 enum)을 청취 대상 표현에도
재사용하되, **조작 대상과 완전히 독립적인 별도 상태**(`AudioListenerTarget`)로 분리(UGV를
몰면서 UAV 소리만 듣는 것도 가능해야 한다는 요구). `SetAudioListenerTarget()`이 각 타겟의
실제 액터를 찾아 `SetAudioListenerOverride`로 리스너를 옮기고, `Idle`은
`ClearAudioListenerOverride()`로 전체 무음. `Monitor1Widget`에 라디오 버튼처럼 동작하는
`UCheckBox` 3개(켜진 버튼 재클릭 시 무음)로 UI 연동.

**UGV 엔진음 / UAV 프로펠러음**: 단순 피치시프트는 배음 구조 전체가 밀려 올라가는 "다람쥐
목소리" 효과라 부적절 — 저/중/고 RPM 레이어를 MetaSound 그래프 안에서 볼륨 크로스페이드하는
방식 채택, C++은 정규화된 `[0,1]` 비율만 계산해 `SetFloatParameter`로 넘김. UGV는 실제
배치된 `BP_UGV_Vehicle`이 `AUGVPawn`이 아니라 `AWheeledVehiclePawn`(Chaos Vehicle) 직계
서브클래스임을 `.uasset` 파일 문자열로 확인, 아무 Pawn에나 붙일 수 있는 독립 컴포넌트
`UVehicleEngineAudioComponent`(신규)로 구현 — 비율은 속도보다 RPM 우선
(`GetEngineRotationSpeed() / GetEngineMaxRotationSpeed()`, 기어별 RPM 차이·아이들 RPM까지
반영). UAV는 반대로 `AUAVPawn`이 실제 네이티브 클래스임을 확인, 기존 `CurrentPropSpinRateDegPerSec`
(ABP가 프로펠러 시각 회전에 쓰는 값)를 그대로 재사용해 별도 비행状태 분기 없이 지상에선
자연히 무음.

**병력 발소리**: `BP_Ally_kadex`/`BP_Enemy_kadex`가 순수 블루프린트라 네이티브 프로퍼티를
못 붙여 독립 `UFootstepAudioComponent`(신규)로 구현. 타이머 근사치(기본)와 AnimNotify 기반
(`UAnimNotify_Footstep`, 신규) 두 트리거 모드 지원 — 실제 로코모션 애니메이션엔 노티파이가
하나도 없어서 정확한 동기화가 필요하면 에디터에서 수동 배치 필요. 랜덤 배리에이션은 직전
재생 사운드와 같은 걸 연속으로 고르지 않도록 처리(순수 균등 랜덤은 "규칙적으로 랜덤"인 티가
남음).

**MetaSound 레이어 크로스페이드 공용 구조**: Wave Player 노드(레이어별, Mono) + `Map Range`로
구간별 gain 계산 + `Multiply`/`Add`로 합산. 공용 입력 파라미터를 좁은 범위(±2~4 semitone)로
피치에도 살짝 걸어 자연스러움 보강(큰 값은 다시 "다람쥐 목소리"가 되므로 지양).

**트러블슈팅 - 리스너가 멀리 갔다 오면 소리가 영구 무음**: 언리얼 오디오 엔진이 루핑
사운드가 들리는 범위를 벗어나면 자동 가상화(Virtualize)하는데, 기본 `VirtualizationMode=
Restart`에서 MetaSound는 복귀 시 내부 그래프 상태가 제대로 안 살아나는 경우가 있었음.
`MS_UGVEngine`/`MS_UAVEngine`의 `Virtualization Mode`를 `Play When Silent`로 변경해 해결.

**트러블슈팅 - 회전 기준이 차체(hull)라 좌우 방향이 안 맞음**: 리스너가 차량 루트(차체)
회전에 붙어있어서, 터렛/짐벌만 차체와 독립적으로 돌아간 상태에서 화면과 소리의 좌우가
어긋남. 리스너를 차체가 아니라 **실제로 보고 있는 카메라 컴포넌트**(`GetSightCamera()`/
`GetGimbalCamera()`)에 붙이도록 변경 — `SetAudioListenerOverride`가 붙여둔 컴포넌트 회전을
매 프레임 실시간으로 읽으므로 한 번만 붙이면 계속 정확함.

**트러블슈팅 - UAV 프로펠러 소리가 아예 안 남**: `AUAVPawn`에 컴포넌트 자신의 `Sound`와는
별개인 `PropellerSound`라는 죽은 프로퍼티가 남아있어서 코드가 그쪽만 읽고 있었음(컴포넌트는
`bAutoActivate(false)`로 꺼둔 채). UGV와 동일한 구조로 통일해 해결. 교훈: 컴포넌트를 감싸는
새 프로퍼티를 만들 땐 사용자가 직관적으로 채우는 필드와 실제 코드가 읽는 필드가 반드시
일치해야 함.

**변경/신규 파일**: `Vehicles/RCWSFireControlComponent.h/.cpp`, `Vehicles/RCWSProjectile.h/.cpp`,
`Vehicles/VehicleEngineAudioComponent.h/.cpp`(신규), `Vehicles/UAVPawn.h/.cpp`,
`Soldiers/FootstepAudioComponent.h/.cpp`(신규), `Soldiers/AnimNotify_Footstep.h/.cpp`(신규),
`titan_examplePlayerController.h/.cpp`, `UI/Monitor1Widget.h/.cpp`.

---

## 2. 아군 + UGV 동반 이동·전투 시스템 — 버그 근본원인 추적, UAV 프레이밍, 시나리오 스텝 테이블

### 목적
지난 주(W31) 구현한 아군 동반기동/회피/산개 시스템이 실사용 중 보인 "찔끔찔끔 정지-재출발",
"도리도리(제자리 회전)", "텐트 병목 정체", "한 점으로 뭉침" 등 이동 버그를 근본 원인까지
추적해서 고치고, UAV 카메라가 아군+적군을 실제로 프레임 안에 담도록 재작성하며, 시나리오
단계 전환을 DataTable 기반으로 완성한다.

### 결과

**대형(포메이션) 전면 재설계**: 30명을 6개 분대(각 5명)로 나눠 분대별 1열 종대 배치.
1·2분대가 UGV 양옆(RCWS/UAV 시야에 걸리도록 위치 조정), 3·4/5·6분대가 순차로 뒤에 배치.
`FormationOffsetFromLeader`(UGV 로컬 좌표)로 저장.

**이동 버그 — 근본 원인까지 추적한 4건**:
- "찔끔찔끔" 정지-재출발: 이진 데드존을 선형 감속 램프(`ApproachSlowdownDistanceCm`) + 하드
  정지(`StopDistanceCm`) 2단 구조로 교체.
- "도리도리"(제자리 회전): `StopDistanceCm` 미만이면 조향(`AddMovementInput`) 자체를 스킵.
- 텐트 병목 정체: 최종 도착용 `ArrivalToleranceCm`(150cm)을 중간 웨이포인트 통과 판정에도
  잘못 재사용해서 촘촘한 웨이포인트 여러 개를 한 틱에 건너뛰던 버그 →
  `WaypointPassToleranceCm`(30cm)으로 분리.
- **웨이포인트마다 감속(이번 라운드 최대 발견)**: 도착 감속 램프가 중간 웨이포인트에도 걸려서
  매 웨이포인트마다 속도가 0 근처로 떨어졌다 재가속 — `MoveToward`에
  `bApplyArrivalSlowdown` 플래그 추가, 마지막 웨이포인트일 때만 true.
- **"한 점으로 뭉침"(근본 원인 완전 제거)**: `ResolveAllyOverlapPush`의 "NavMesh 밖으로
  밀려나면 되돌리는" 재투영 안전장치가 범인 — 로그 실측 결과 전체 보정 발동의 68%가 이
  경로였고, 1회 보정량(25~55cm)이 실제 겹침 푸시(2.6cm/틱)보다 10~20배 커서 좁은 통로에서
  여러 아군을 같은 지점으로 끌어당기고 있었음. 완전히 제거.

**UGV 회피 3단 방어 — 결정적 발견**: `RecastNavMesh-Default`의 `RuntimeGeneration`이
`Static`으로 설정돼 있어서 UGV 발밑의 동적 `NavArea_UGVBody` 모디파이어가 애초에 실제
경로탐색에 반영되지 않고 있었을 가능성이 높음 → `DynamicModifiersOnly`로 변경. 장애물 바운즈
소스도 엔진 기본 `FailsafeExtent`(2m 대칭 입방체, 실차체보다 작고 비대칭 오프셋 불가)에서
`NavObstacleBox`(신규, 실측 차체 크기 기반) 컴포넌트로 교체. RVO 프록시 반경 계산에
`NavObstacleBox`가 새어 들어가던 버그 수정, 테스트용 토글(`bEnableAvoidanceProxy`) 추가.
물리적 안전망으로 `NavObstacleBox`에 아군(Pawn) 채널만 Block하는 콜리전 신규 추가.

**UGV 에스코트 속도 제한(신규)**: 아군과 동반 기동하는 구간에만
`EscortMaxSpeedKmh`(기본 20km/h)로 제한. 액셀 감소/브레이크 증가를
`EscortSpeedRampBandKmh` 폭 안에서 하나의 연속 선형 함수로 통합(원래 완전히 분리된 두
분기라 급브레이크 느낌이 났던 것을 목표 속도 지점에서 0으로 정확히 만나도록 수정).

**시나리오 스텝 테이블 시스템** (`/Game/Scenario/DT_ScenarioSteps`): `FScenarioStepRow`/
`EScenarioTriggerType`/`EScenarioEffectType`(W31 설계안을 실제 구현) +
`UScenarioStateSubsystem`의 매 틱 평가 루프. 트리거 조건/대기시간/효과를 재컴파일 없이
DataTable 값만으로 조정 가능. 전체 체인: UAV 이륙 → 정찰/아군집결 동시 진행 → UGV 목적지
2000cm 임계 → 분대장 정지 수신호 → (줌아웃 + 적군 등장 + 7초 후 산개) 병행 → 아군 산개 +
UGV 재출발 → RCWS 자동경계 → 적 감지 시 자동사격+아군 엄폐사격 → 전멸 시 완료. 이번 라운드
추가된 트리거: `UAVArrived`/`UAVEnemyDetected`/`DistanceThreshold`/`ActorStopped`, 이펙트:
`BeginAllyFormUpAndAdvance`/`SetUGVAutoSurveillance`/`SetUGVAutoFire`/
`BeginEnemyEngagementApproach`. 사용자 요청으로 아군 집결 시작 트리거를 "UAV가 적을 발견한
뒤"에서 "UAV 이륙과 동시"로 앞당김.

**UAV WideEngagementView 프레이밍 전면 재작성**: 기존 "적/아군 무게중심 평균 + 고정
줌레벨" 방식은 원근 카메라 특성상 가까운 쪽이 크게 보여서 먼 쪽이 프레임 밖으로 밀려나는
문제가 있었음. `ComputeFramingForPoints`(신규) — 카메라 기준 각 대상까지의 실제 각도(요/
피치)를 구해 그 범위를 전부 포함하는 조준 방향과 필요 FOV를 매 틱 계산(세로 화각 요구량을
가로 화각 기준으로 환산해 통일). `EnemyEvidence` 팩션(신규, 낙하산은 교전 대상이 아니므로
`Friendly`/`Enemy` 어디에도 안 걸림 — 엄격 동등 비교만 쓰는 코드베이스 전체에서 추가
필터링 없이 자동 제외).

**적군 AI 이동 실패 버그**: `Gun`(무기 메쉬) 콜리전 프로파일이 `BlockAll`이라 다른 적군
Pawn까지 물리적으로 막고 있었음(적군은 아군과 달리 밀어내기 안전망이 없어 두 적군 총
메쉬가 살짝 겹치면 영구히 낌). `BP_Enemy_Base` 템플릿의 `Gun` 콜리전을 Pawn 채널만
Ignore로 수정 — 신규 배치분엔 확실히 적용, 기존 15명 인스턴스는 MCP 배열 프로퍼티 편집
불안정성(아래 트러블슈팅)으로 일부만 반영돼 재확인 필요.

> **트러블슈팅 - MCP 편집 툴 함정 (다음 세션 필독으로 기록)**
>
> Vector/구조체 프로퍼티는 다중 필드를 한 번에 보내면 일부만 적용되고 조용히 드롭될 수
> 있음(호출 자체는 성공 반환) — 필드별로 분리 호출 후 재검증 필요. 배열 프로퍼티
> (`collisionResponses.responseArray` 등)는 "덮어쓰기"가 아니라 현재 값과의 "디프"처럼
> 동작해서 큰 배열을 한 번에 갈아끼우면 에러/부분반영/무시가 발생할 수 있음 — 특히 **레벨에
> 이미 배치된 인스턴스** 대상일 때 문제, 블루프린트 클래스(CDO) 대상은 대체로 안정적. 새
> 컴포넌트를 클래스에 추가해도 이미 배치된 기존 인스턴스는 자동으로 기본값을 안 물려받음.
> 결론: 물리적으로 중요한 콜리전 설정은 에디터 값에만 의존하지 말고 C++이 런타임에 명시적으로
> 강제하는 편이 안전.

**변경/신규 파일**: `Soldiers/AllyFormationComponent.h/.cpp`,
`Vehicles/UGVAvoidanceProxyComponent.h/.cpp`, `Vehicles/NavArea_UGVBody.h/.cpp`,
`Vehicles/UGVAIController.h/.cpp`, `UI/ScenarioStepTypes.h`(신규),
`UI/ScenarioStateSubsystem.h/.cpp`, `Vehicles/UAVPawn.h/.cpp`
(`ComputeFramingForPoints`/순항고도 램프/`GimbalReconMaxPitchDegrees`), `Config/DefaultEngine.ini`
(`RecastNavMesh RuntimeGeneration`).

---

## 3. 아군 캐릭터 애니메이션 시스템 — 설계 및 즉시 적용 수정 (진행 중)

### 목적
아군이 속도에 맞춰 대기/걷기/뛰기를 자연스럽게 전환하고, 총을 들고 조준한 자세와 내린
자세를 시나리오 상태에 맞춰 토글하며, 앉기/무릎/엎드림과 자연스럽게 어우러지도록 설계한다.
30명 동시 존재 시 성능/제작비용을 감당 가능한 수준으로 유지. **전체 설계는 논의 중이며 일부
즉시 적용 가능한 수정만 이번 주에 반영함.**

### 결과

**즉시 적용된 수정**: `BS_Movement`의 `AxisToScaleAnimation`을 `BSA_None` → `BSA_X`로
변경(실제 이동 속도에 맞춰 애니메이션 재생 속도가 늘고 줄게 함 — 저속 구간에서 발이 거의 안
움직이는데 캡슐만 미끄러지던 문제의 근본 원인). `FormUpMoveSpeed`를 300(Walking) →
600(Run_Forward)으로 상향("총 내리고 뛰어서 이동" 요청 반영). UGV 에스코트 속도 거버너
(`EscortMaxSpeedKmh`)가 씬 스케일 보정(`GetDistanceScaleFactor()`)을 빠뜨려 "20km/h" 설정이
실제로는 씬 기준 20(≈실제 24.3km/h)까지 허용되던 버그 수정 — 다른 코드와 동일하게 보정
적용.

**핵심 설계 원칙**: "판단은 C++, 표현은 ABP" — `AllyFormationComponent`가 시나리오 상태에
따라 캐릭터 BP의 bool 변수(`IsHoldingWeapon?`/`IsCrouching` 등)를 세팅하면 ABP는 그대로
받아 블렌드만 함(기존 `IsProne` 리플렉션 세팅 패턴을 그대로 확장). 상체 무기 자세 토글은
Layered Blend Per Bone 노드가 신규로 필요(하체 `BS_Movement`와 상체 `AS_RifleIdle`(든 자세,
이름과 달리 확인됨)/`AS_RifleAim`(내린 자세, 이름이 잘못 붙어있음을 확인) 크로스페이드) —
**AnimGraph 내부 노드 작업은 사용자가 에디터에서 직접 담당하기로 확정**(MCP 툴로 신뢰성
있게 조작 불가). 엄폐 포즈는 `AS_Crouching`(이동 사이클 없는 단일 포즈, "웅크리고 걸어다니는"
용도 아님을 확인)을 쓰되, 향후 엄폐 포즈가 늘어날 것을 대비해 `ECoverPoseType` enum +
`EditInstanceOnly` 프로퍼티로 레벨 디자이너가 인스턴스별로 고를 수 있게 설계 제안.

**시나리오 단계 → 자세 매핑 표**를 확정: FormingUp/Following은 뛰기+내림, Approaching은
걷기+조준 전환, Ambush는 정지+조준 유지. 개체별 애니메이션 위상차(30명이 동시에 Idle 루프
시작해 완전히 싱크되는 문제)는 `BeginPlay`에서 랜덤 오프셋을 1회만 세팅하는 표준 해법으로
설계.

**미해결/사용자 확인 필요**: `AS_Run_Forward`가 low-ready(총 내림) 자세 대체 애니메이션이
세트 안에 없음(원본 키프레임 편집 도구가 MCP 툴셋에 없어 리포즈 불가) — 우선순위 낮음으로
보류. `IsLookingAround`가 실제 AnimGraph에서 소비되는지, 스켈레톤이 다른 `AS_Enemy_Looking_Around`
(아군에 이미 종속성으로 물려있음)가 실제로 재생되는지는 에디터에서 라이브 확인 필요.

---

## 4. 전체 아키텍처 설계 확정 (`architecture_decisions.md` / `protocol_icd.md`)

### 목적
`system_architecture_design_spec.md`(원본 PDF)와 기존 개발 가이드를 대조해서 데모 전체
구조 — 5개 SW(UGV 에뮬레이터/통제기SW, 자체방호 에뮬레이터/통제기SW, 상위체계)와 3계층
통신 프로토콜을 확정한다. 실제 구현이 아니라 "살아있는 초안" 설계 문서 작업.

### 결과

**한눈에 보기**: 총 5개 SW(상위체계는 파이썬 스텁으로 대체, 실물은 모비젠/LIG 영역).
UGV 통제기SW는 타사 제작 가능성이 있으나 우리도 만들기로 확정 — 그래서 §2 프로토콜을 타사가
그대로 구현해도 되는 ICD 수준으로 격상(`protocol_icd.md`). 3계층: (A) 상위체계↔통제기SW,
(B) 통제기SW↔에뮬레이터, (C) 에뮬레이터↔에뮬레이터. **(A)(B) 둘 다 v1은 NATS(TCP)로 통일**,
RTSP만 별도, UDP는 도입 안 함(PoC에서 필요성 실측되면 후순위 분리). 코드베이스는 에뮬레이터
1벌(공유) + 통제기SW 1벌(공통 플러그인+축별 얇은 프로젝트) 2벌뿐 — "SW 5개"는 코드베이스가
아니라 런타임 프로세스 개수.

**Layer C(에뮬레이터↔에뮬레이터)**: 전용서버 방식 폐기(에뮬레이터가 "현실을 대체"하는
개념이라 중계 서버가 존재할 자리가 없음), 리슨서버로 확정 — 부하가 더 적은 UGV PC를 기본
호스트로. 호스트/클라이언트도 런타임 선택 가능. RCWS 발사는 액터 리플리케이션이 아니라
히트스캔+Multicast RPC로(현재 구현은 실제 비행 Actor라 마이그레이션 필요, 5절 참고). UAV도
리플리케이션 대상에 포함(이번 주 확정 — 처음 목록에서 누락됐던 것뿐).

**Layer B(통제기SW↔에뮬레이터)**: 원 설계서의 HDMI/USB 로컬 직결안 폐기, 네트워크 분리
(NATS)로 통일. RTSP는 UE NVENC(zero-copy) + `gst-rtsp-server` 하이브리드로 결정(유료 OWL
플러그인 대체). 명령은 가능한 한 이미 구현된 함수/enum에 매핑.

**Layer A(상위체계↔통제기SW)**: NATS JetStream(연결 끊겨도 메시지 유실 안 되게). **판단/보고
게이트 원칙 확정**: 위험하거나 비가역적인 행동(발사/교전)에만 승인 게이트, 저위험/가역적
행동(정찰/이동/카메라 조작)엔 안 넣음 — `RCWSFireControlComponent::bEngagementAuthorized`
(기본 off)가 발사 자체의 게이트, 조준·추적까지는 자유.

**`protocol_icd.md`(필드 단위 ICD)**: 공통 봉투 포맷(`{cmd, seq, ts, payload}`, seq로
유실/재정렬 감지), 좌표는 위경도(WGS84)로 확정(기존 `GeoCoordinateUtils.h` 재사용, 월드좌표는
와이어 프로토콜에 절대 안 나옴). UGV축 5스트림/자체방호축 7스트림 RTSP 채널, 연결/BIT·조이스틱
입력·텔레메트리·단발성 명령/이벤트 메시지 스키마 전체 정의. `bEngagementAuthorized`는 축마다
별도 승인으로 확정(UGV축 #4-6과 자체방호축 #4-8이 서로 다른 시점/대상을 교전하므로).

**재사용 가능한 설계 원칙 6가지 정리**: 분리 우선(구조적 경계), 자동 vs 게이트, 트리거
소스와 이벤트 로직 분리, 기존 함수/enum에 올라타기, 되돌리기 싼 결정은 나중으로, 불확실한
하드웨어에 의존하지 않는다.

**남은 미확정/블로킹**: UGV 통제기SW 타사 제작 여부 최종 확인(우선순위 높음, 우리끼리 결정
가능), RTSP PoC(진행 중, 7절), NATS 인프라(완료, 6절), 리플리케이션 구현 착수(감사 완료,
5절) — 외부(LIG/모비젠) 확인 없이는 못 끝내는 항목(Raw DB/Fabric 인터페이스 스펙 등)은
저우선순위로 대기.

---

## 5. 리플리케이션 준비도 감사 (`replication_audit.md`)

### 목적
설계 문서(4절 §1.3)의 리플리케이션 대상 목록을 기준으로, 실제 `titan_example` 코드/레벨이
멀티플레이(리슨서버) 전환에 얼마나 준비돼 있는지 Unreal MCP + C++ 전수 grep으로 조사한다.
**코드/레벨은 수정하지 않은 순수 조사.**

### 결과

**TL;DR**: 리플리케이션 관련 코드가 프로젝트 전체에 **사실상 0건**(`GetLifetimeReplicatedProps`/
`DOREPLIFETIME`/`HasAuthority`/`Server_`/`NetMulticast` grep 전부 0). 지금 켜진 리플리케이션은
전부 `ACharacter`/`APawn` 엔진 기본값, RCWS/발사/탐지/시나리오 시스템은 전부 로컬 전용.

**지금 당장 유효한 블로커 2개**(미래 얘기가 아니라 지금 배치된 액터에 바로 해당):
1. `ATitanTruck::BeginPlay`가 `GetPlayerController(this,0)->Possess(this)`로 자기 자신을
   하드코딩 possess — 리슨서버에서 클라이언트가 권한 없이 `Possess` 호출하는 상황이 됨.
2. `AUGVAIController::IsLocalController()`가 `return true` 강제 오버라이드(주석에 "싱글플레이라
   항상 true" 사유 명시) — 지금 배치된 UGV를 이 컨트롤러가 직접 조종 중이라 즉시 유효한
   블로커.

**정정 기록**: 최초 조사에서 "실제 배치된 UGV = `AUGVPawn`+힘-기반 커스텀 컴포넌트"로 오판했다가,
사용자 확인 후 재조사해서 실제로는 `BP_UGV_Vehicle`이 `AWheeledVehiclePawn`(Chaos Vehicle)
직계 서브클래스임을 `search_subclasses`로 재검증 — `AUGVPawn`/`UUGVMovementComponent`는
임시 구현이라 삭제 예정인 레거시 코드였음(감사 범위에서 제외).

**항목별 조사**: RCWS/RCWSFireControl(발사 트리거·자동조준 표적 선정 등 전부 로컬, 발사체도
히트스캔이 아니라 실제 비행 Actor를 각 프로세스가 로컬 스폰), UGVMovementComponent/
UGVAIController(Chaos Vehicle 조작이 리플렉션 기반 `SetManualControl` 호출, `IsLocalController`
하드코딩이 향후 물리 리플리케이션 옵션의 전제를 깨는 지점), titan_examplePlayerController
(입력 핸들러 전부 "로컬에서 찾아 직접 호출" 패턴, 서버 RPC 경로 없음), 시나리오 시스템
(`UScenarioStateSubsystem`이 `GameInstanceSubsystem`이라 프로세스마다 독립 인스턴스, 아군/적군
45명은 런타임 스폰이 아니라 전부 레벨 사전 배치), GameMode/PlayerController(네트워킹을 조금도
염두에 두지 않은 완전 싱글플레이 전제 — `DefaultPawnClass=None`, 커스텀 `GameState` 없음).

**§8 구현 TODO**로 최우선(리슨서버 켜기 전 필수)/RCWS·발사/UGV·차량/시나리오/UAV/추가조사
필요 항목을 체크리스트로 정리 — 실행은 다음 라운드로 이월.

---

## 6. NATS/JetStream 인프라 세팅 + 상위체계 파이썬 테스트 스텁

### 목적
`protocol_icd.md`에서 확정한 NATS 기반 통신 계층을 실제로 세팅하고 검증하며, 실물 상위체계가
연결되기 전까지 그 역할을 대신할 파이썬 스텁을 만든다.

### 결과

**NATS/JetStream 세팅**(`titan/infra/nats/`): JetStream 활성화 서버 설정, 재사용 가능한
기동 스크립트, 스트림 생성(멱등, 재실행 안전) 스크립트 구성. `protocol_icd.md`의 subject
구조(`console.<axis>.>`/`hq.cmd.<axis>`/`hq.rpt.<axis>`)를 그대로 따라 4개 스트림 생성 —
`console.*` 3종(cmd/evt/telemetry)을 축 하나당 스트림 하나로 묶은 이유는 관리 단위를 축
기준으로 맞추는 게 향후 보존정책 조정에 직관적이기 때문. 인증 없음(로컬 개발 전용 의도적
결정, 배포 확정 후 별도로 잠글 것).

**검증**: core NATS 라이브 구독 통과. **JetStream 재연결 캐치업 통과(핵심 요구사항)** —
durable consumer로 구독 중 연결 종료 → 그 사이 메시지 3건 발행 → 같은 durable 이름으로
재연결 시 순서/내용 그대로 전부 수신 확인. durable 이름을 재연결 시에도 동일하게 써야 성립함을
확인 — 클라이언트(UE) 구현 시 고정된 durable 이름 사용 필요. core `nc.publish()`로 보낸
메시지도 subject가 매칭되면 자동으로 스트림에 캡처됨을 확인, 발행 측은 JetStream을 의식할
필요 없이 그냥 publish만 하면 됨.

**상위체계 파이썬 테스트 스텁**(`titan/structure/hq_stub/`): `hq.cmd.<axis>`에 `HQ_*` 발행,
`hq.rpt.<axis>` 구독해 `RPT_*` 수신·로그하는 순수 파이썬 스텁 — 실물 상위체계(모비젠/LIG)가
붙으면 교체(신호 소스 교체 원칙). `manual`(엔터로 단계별 트리거, #4-6 교전승인만 Y/n 질의)/
`auto`(1.5초 간격 자동 재생) 두 모드. 로컬 NATS로 전체 체인(#4-1~#4-8) 왕복 확인, JetStream
모드도 확인(스트림은 인프라 쪽에 이미 있어야 동작). Windows 콘솔 기본 코드페이지(cp949)에서
한글 출력 시 `UnicodeEncodeError` 발생 — stdout/stderr UTF-8 강제 재설정으로 처리.

**변경/신규 파일**: `titan/infra/nats/`(`nats-server.conf`/`start-nats.ps1`/`setup-streams.js`/
`test-pubsub.js`/`package.json`), `titan/structure/hq_stub/`(`envelope.py`/`messages.py`/
`nats_client.py`/`logger.py`/`scenario.py`/`main.py`).

---

## 7. RTSP 송출 PoC (진행 중, 런타임 미검증)

### 목적
`protocol_icd.md`에서 결정한 "UE NVENC + gst-rtsp-server" 하이브리드 방식으로 RTSP 영상
송출 플러그인(`RtspEncoder`)을 신규로 만들어 최소 재현(회전 큐브 스트리밍)까지 검증한다.
**이 세션은 `titan_example`을 직접 빌드하지 않는 스탠딩 룰이 있어, 컴파일은 사용자가
진행하며 반복적으로 에러를 넘겨받아 수정하는 방식으로 진행했다. 현재 상태: 컴파일 성공,
플러그인 로드 실패 이슈까지 수정 완료, 런타임(실제 스트리밍) 검증은 아직 미완료.**

### 결과

**설계 변경 — CUDA 안 씀**: `protocol_icd.md`엔 "CUDA-D3D11 interop"라 적혀 있었으나, 실제로는
프로젝트가 `DefaultGraphicsRHI_DX12`라 렌더링 텍스처가 `ID3D12Resource`임을 확인 — NVIDIA
Video Codec SDK의 `NvEncoderD3D12`(D3D12 텍스처를 NVENC에 직접 먹이는 공식 래퍼)로 CUDA
interop 없이 구현. CUDA Toolkit은 설치는 했으나 실제 코드에서 링크/include 안 함(문서에
반영 필요 — "NvEncoderD3D12, zero GPU-copy 아님이지만 zero CPU-copy"로 정정).

**D3D12 zero-copy 흐름**: SceneCapture 렌더타겟을 UE의 네이티브 D3D12 커맨드 리스트에 직접
`CopyResource`로 복사(전용 커맨드 큐 없이 UE 큐에 얹어 순서 보장 자동화) → 매뉴얼 fence로
NVENC에 복사 완료를 GPU 레벨에서 신호(CPU 스톨 없음) → `EncodeFrame()` 호출.

**컴파일 트러블슈팅 (다수, 최종 전부 해결)**:
- UE의 `AllowMicrosoftPlatformTypes.h`/`Hide` 패턴은 "짧게 빌려쓰고 반납" 용도라 벤더 코드
  (NVIDIA 샘플)가 파일 전체에서 `TRUE`/`FALSE`를 계속 쓰는 상황엔 안 맞음 — 벤더 헤더는
  Allow/Hide로 안 감싸고 원본 그대로 두고, UE/벤더/GLib이 실제로 섞이는 파일에서만
  `check`/`TEXT`/`GError`(UE 전역변수와 GLib 타입명이 정확히 충돌) 같은 진짜 충돌 식별자만
  `push_macro`/`pop_macro`로 국소 처리.
- `bUseUnity = false` 추가 — 벤더 파일의 잔여 매크로가 Unity Build로 다른 파일에 새어나가는
  것 차단.
- glib 헤더의 이식성 체크(`__GNUC__` 등)가 MSVC 경고 레벨에서 에러로 승격되는 문제는
  `UndefinedIdentifierWarningLevel = Off`로 이 모듈만 예외 처리.
- **5라운드 만에 찾은 진짜 원인 — "식별자 'RtspEncoder'" 구문 에러**: 매크로 이스케이핑 등
  여러 정황 추리가 전부 헛다리였고, `cl.exe /P`(전처리만)로 실제 전처리 결과를 직접 까본
  결과 `RtspEncoderModule.h`가 `IMPLEMENT_MODULE` 매크로가 정의된 `Modules/ModuleManager.h`를
  빼먹어서 매크로 확장이 전혀 안 되고 있었던 단순한 헤더 누락이었음. 교훈: 애매한 컴파일러
  증상이 정황 추리로 안 잡히면 바로 전처리 결과를 까보는 게 훨씬 빠름.
- **플러그인 로드 실패**: GStreamer DLL을 implicit link로 붙여놔서 모듈 로드 시점에 바로
  찾으려 들어 실패 → delay-load로 전환. 그래도 `gst_is_initialized()` 시점에 크래시 →
  `FPlatformProcess::AddDllDirectory()`가 진짜 Win32 API가 아니라 UE 자체 내부 캐시였음(delay-load
  헬퍼엔 무영향) — `PushDllDirectory()`(실제 `::SetDllDirectory()` 호출)로 교체해 해결.

**GStreamer 설계**: `RtspServerSubsystem`은 `GameInstanceSubsystem`이라 PIE 수명에 맞춰
RTSP 서버가 뜨고 내려감. `gst_rtsp_media_factory_set_shared(TRUE)`로 여러 클라이언트가 붙어도
NVENC 세션은 하나만 유지. 콘솔 커맨드 `RtspPoc.SpawnTestActors [count]`로만 opt-in 스폰(다른
트랙 안 건드리는 원칙).

**다음 단계**: 사용자가 UE 에디터에서 실제 컴파일 → 에러 있으면 전달받아 §7 우선순위대로
수정 → 되면 PIE + 콘솔 커맨드로 회전 큐브 1개 스트리밍 → VLC 재생 확인 → 7개 동시 스폰으로
5/7스트림 스케일 테스트(GPU NVENC 사용률/CPU/프레임레이트 관찰).

**신규 파일**: `Plugins/RtspEncoder/`(`RtspEncoderModule`/`RtspServerSubsystem`/
`NvencD3D12Encoder`/`RtspStreamComponent`/`RtspPocTestActor`/`RtspPocCommands` +
`ThirdParty/NvCodec/`(NVIDIA SDK 샘플 vendored, 라이선스 확인 필요 — 최종 재배포 조건은
법무 검토 시).

---

## 기타 메모

- 세션 3분리 확정: ① structure 빌드업/환경설정(NATS·RTSP), ② 기존 titan_example 유지보수 +
  에뮬레이터 업그레이드(이동형지휘소/UGV 모드 분리), ③ 원격 통제기SW 신규 언리얼 프로젝트
  개발(기존 WBP/위젯 코드 이식) — 이번 주 작업이 이 세 트랙에 걸쳐 병행 진행됨.
  `titan/structure/sessions_idea.md` 참고.
- 낙하산 착지 시 나무에 자연스럽게 걸리는 형태로 변형하는 연출 아이디어는 여전히 미착수.
- UAV 20m 고도 관련 메모 추가(`titan/memo.md`) — 세부 내용 미정, 다음 라운드 확인 필요.
